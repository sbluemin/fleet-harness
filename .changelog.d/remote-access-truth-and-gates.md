---
branch: remote-access-truth-and-gates
---

### fleet-console
#### Added
- A freshly created access link now states what it opens right beside the link itself, so the warning is in front of you at the moment you copy the string rather than only at the top of the section. A monitoring-only link cannot run commands, so it carries no such line.
  ko: 방금 만든 액세스 링크가 무엇을 여는지를 링크 바로 옆에서 말합니다. 섹션 머리에만 있던 경고가 문자열을 복사하는 그 순간 눈앞에 놓입니다. monitoring 전용 링크는 명령을 실행하지 못하므로 그 문장이 붙지 않습니다.
