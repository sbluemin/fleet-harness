### fleet-console
#### Added
- [fleet-console] Make War Room read the same way at every density: the left column drops its queue caption and empty-state line so the awaiting, running, background, and idle sections always stand - each one reporting its own count and its own "none" - while the fleet map drops the dashed zone ring for a Theater status marker set in the middle of its zone, drifts every marker in place (running wide and quick, the rest narrow and slow), opens the card Quick-Look at full reading size on marker hover or focus without stealing the pointer from the marker, and morphs panels into the spots their markers will take when the density threshold is crossed - and back out of them on the way up, with reduced motion skipping the flight entirely.
  ko: War Room이 어느 밀도에서나 같은 방식으로 읽힙니다. 좌측 열은 대기열 캡션과 빈 상태 문구를 걷어내고 대기·실행 중·백그라운드·유휴 섹션이 각자의 건수와 각자의 "없음"을 말하며 항상 서 있습니다. 작전지도는 점선 구역 원주를 걷어내고 구역 한가운데에 Theater 상태 표석을 세우며, 모든 마커가 제자리에서 유영하고(실행 중은 넓고 빠르게, 나머지는 좁고 느리게), 마커 hover나 포커스에 카드 Quick-Look을 판독 크기 그대로 띄우되 포인터는 마커에 그대로 남기고, 밀도 임계를 넘을 때 패널이 자기 마커가 설 자리로 변형되어 들어갔다가 올라올 때 되돌아 나옵니다(reduced motion에서는 이 비행을 생략합니다).
