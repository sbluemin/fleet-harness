---
branch: launch-provider-glyphs
---

### fleet-console
#### Changed
- The Claude (Gateway) launch menus now group enabled models under provider bands with the matching provider glyph, show short model names without the provider prefix, and open reasoning-effort choices in a nested submenu on model hover while a plain model click still launches with the fallback.
  ko: Claude (Gateway) 실행 메뉴가 이제 켠 모델을 프로바이더 밴드와 해당 글리프 아래로 묶고, 프로바이더 접두 없는 짧은 모델 이름을 보여 주며, 모델에 마우스를 올리면 추론 강도는 중첩 서브메뉴로 고르고 모델만 클릭하면 폴백으로 실행합니다.
