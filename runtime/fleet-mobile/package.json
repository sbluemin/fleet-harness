{
  "name": "@dotobokuri/fleet-mobile",
  "version": "1.78.0",
  "description": "Fleet Console mobile shell.",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "android": "expo run:android",
    "android:build:debug": "node scripts/build-android-debug.mjs",
    "android:build:release": "node scripts/build-android-release.mjs",
    "android:verify:debug": "node scripts/verify-android-debug.mjs",
    "android:verify:release": "node scripts/verify-android-release.mjs",
    "android:distribute": "node scripts/distribute-firebase.mjs",
    "ios": "expo run:ios",
    "ios:build:release": "node scripts/build-ios-release.mjs",
    "ios:verify:release": "node scripts/verify-ios-release.mjs",
    "ios:distribute": "node scripts/distribute-testflight.mjs",
    "build": "expo export --platform android --output-dir dist/bundle",
    "generate:android": "expo prebuild --platform android --clean --no-install",
    "generate:ios": "expo prebuild --platform ios --clean --no-install",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:android": "expo prebuild --platform android --clean --no-install && cd android && ./gradlew :fleet-console-view:testDebugUnitTest"
  },
  "dependencies": {
    "expo": "57.0.11",
    "expo-build-properties": "57.0.9",
    "expo-camera": "~57.0.3",
    "expo-modules-core": "57.0.10",
    "react": "19.2.8",
    "react-native": "0.86.2"
  },
  "devDependencies": {
    "@types/react": "^19.2.7",
    "typescript": "6.0.3",
    "vitest": "4.0.18"
  }
}
