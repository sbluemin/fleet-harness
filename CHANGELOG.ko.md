# Changelog

All notable changes to this project will be documented in this file.
This format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

## [1.98.0] - 2026-09-18

### fleet-console

#### Changed
- 이제 에이전트가 Console 을 쓰는 것이 화면에 보입니다. 에이전트가 읽거나 바꾸는 Operation·그룹·Theater·저장소/파일 패널이 「콘솔 사용」 배지와 같은 펄스로 잠깐 감싸이고, 보낸 메시지와 답에는 에이전트 이름이 붙으며, 만든 그룹과 시작한 Operation 에는 누가 했는지가 남습니다.
- Console use 도구가 Console 의 자리(사이드바·Operation 패널·Session Analyst·Quick Launch·저장소·파일 탐색기)에 맞춰 10종으로 재편되었습니다: `console_context`, `console_operations`, `console_organize`, `console_operation`, `console_send`, `console_panel`, `console_analyst`, `console_launch`, `console_repo`, `console_file`. 에이전트가 그룹 안에 이름을 붙여 Operation 을 바로 시작하고, 그룹 이름을 바꾸거나 빈 그룹을 지울 수 있습니다.
- 에이전트가 Console use 로 Session Analyst 에게 물은 질문이 그 Operation 의 분석가 패널에 에이전트 이름과 함께 나타나고, 나중에 패널을 열어도 대화가 남아 있습니다.
- 에이전트가 Operation 을 닫으면 직접 닫았을 때와 같은 실행 취소 배너가 뜨고 어느 에이전트가 닫았는지 알려 줍니다.

#### Fixed
- 에이전트나 다른 창이 만들거나 이름·색을 바꾸거나 지운 그룹이 새로고침 없이 사이드바에 바로 반영됩니다.

#### Removed
- Console use 의 자동화 도구(`console_automation`)와 별도의 사건·영수증·세션 종료·Theater 목록 도구가 제거되었습니다. 이전에 만든 자동화는 일시정지 상태로 남습니다.

## [1.97.1] - 2026-09-18

### fleet-console

#### Fixed
- Operation 브라우저의 페이지 이동이 Console의 키보드 포커스를 자동으로 가져가지 않습니다. 브라우저를 직접 사용하려면 클릭하세요.

## [1.97.0] - 2026-09-16

### fleet-console

#### Added
- 콘솔 사용이 Console에서 할 수 있는 일을 더 많이 다룹니다. 콘솔 사용이 허용된 에이전트는 Operation을 재개·닫기(되돌리기 창 유지)·이름 변경·뷰 전환하고, 그룹과 액센트를 정하고, 전체 대화·백그라운드 잡·명령 카탈로그를 읽고, 자기가 시작한 Operation의 입력 질문에 답하고, 세션 분석가에게 물어보고, 누가 Console·컴퓨터·브라우저를 쓰는지 볼 수 있습니다.
- 에이전트가 판단이 필요할 때 Operation 하나를 화면 앞에 세우고 그 이유를 캡션 아래 한 줄로 보여 줍니다.
- 콘솔 사용이 허용된 에이전트는 모든 Theater의 저장소와 파일을 읽을 수 있습니다(상태·변경과 diff·로그·검색·worktree·폴더 트리·파일). 쓰기는 여전히 그 Theater의 Operation이 합니다.

#### Changed
- Operation 브라우저가 Fleet Desktop 전용 기능이 됩니다. 탭은 Desktop 창 안의 실제 브라우저 뷰로 그려지며, 그 창이 내 Console을 보든 원격으로 접속한 Console을 보든 같습니다. Console이 더 이상 Chrome 설치를 찾지 않으므로 브라우저 엔진 경로 설정은 사라지지만, Google Chrome 쿠키 가져오기는 그대로 쓸 수 있고 이제 Fleet Desktop이 실행 중인 컴퓨터의 Chrome 프로필을 읽습니다.
- 일반 브라우저 탭이나 휴대폰에서 Console을 열면 브라우저 버튼이 비활성화되고 Fleet Desktop이 필요하다는 안내가 보이며, 에이전트의 브라우저 도구도 같은 답을 받습니다. 그런 화면이 연결되어 있는 동안 Desktop의 브라우저는 탭을 닫고 멈추며, Desktop 창만 남으면 다시 열립니다.
- 에이전트가 Console Use·Computer Use·Operation 브라우저를 사용하는 동안, Operation은 자기 작업을 알리는 방식 그대로 이를 보여줍니다. 창을 도는 빛 대신 캡션의 작업 줄이 그 기능의 색으로 흐르고, 브라우저 패널의 캡션에도 같은 줄이 흘러 두 패널이 한 사건으로 읽힙니다. 물든 캡션과 맥동하는 배지는 그대로입니다.

#### Fixed
- Claude Code가 대화 도중 MCP 도구를 추가할 때 게이트웨이 모델이 "Unsupported Anthropic content block type: tool_addition" 오류로 무한 재시도하던 문제를 수정했습니다.
- 브라우저 탭이 여러 개일 때 하나를 닫으면 남은 탭이 빈 화면 대신 바로 보입니다.
- 우측 레일의 확장 표면처럼 브라우저 위에 열리는 패널이 이제 브라우저에 가려지지 않고 앞에 보이며, 에이전트가 조작 중일 때의 테두리도 페이지 뒤로 숨지 않습니다.
- Console이 업데이트되어 새 버전으로 돌아오면 열어 두었던 화면이 옛 버전에 머무르지 않고 새 버전으로 다시 불러와집니다. 내 창에서 업데이트한 원격 Console도 마찬가지입니다.

#### Removed
- 저장소 패널이 같은 체크아웃에서 다른 Operation이 작업 중이라는 경고를 더 이상 표시하지 않습니다.

### fleet-desktop

#### Added
- Operation 브라우저가 Desktop 창 안의 실제 브라우저 뷰로 그려집니다. 페이지가 화면 해상도 그대로 선명하고 스크롤·입력이 즉각 반응하며, 에이전트는 같은 탭을 브라우저 도구로 계속 조작합니다. 일반 브라우저 탭으로 Console을 열면 예전처럼 스트리밍 미리보기가 보입니다.

#### Fixed
- 보고 있던 Console이 재시작한 뒤에도 호스트 목록에 "관리형 콘솔" 줄이 남아, 원격 Console이 내 Console처럼 보이거나 돌아갈 길이 사라지는 일이 없습니다.

## [1.96.1] - 2026-09-16

### fleet-console

#### Fixed
- Operation 브라우저 미리보기가 페이지가 멈춰 있을 때 깜빡이지 않고, 프레임을 더 높은 품질로 보내 글자와 색이 항상 선명합니다.
- WSL에서 Windows Chrome을 엔진으로 쓸 때도 Google Chrome 쿠키를 가져올 수 있습니다. 이전에는 Chrome이 필요하다는 안내만 나왔습니다.

## [1.96.0] - 2026-09-16

### fleet-console

#### Changed
- Operation 브라우저 미리보기가 정지 상태에서 선명해집니다. 스크롤이나 애니메이션이 멈추면 글자와 색을 무손실로 보여 주고, 프레임을 패널에 맞춰 늘리지 않고 픽셀 그대로 그립니다.
- 세션이 끝난 채 꺼둔 패널을 열면 곧바로 재개됩니다. Resume를 한 번 더 누르지 않아도 됩니다.

#### Fixed
- 캔버스를 확대하거나 축소한 상태에서도 Operation 브라우저의 클릭·호버·주석이 정확한 위치에 닿고, 미리보기도 확대한 만큼 선명하게 찍힙니다.

## [1.95.0] - 2026-09-15

### fleet-console

#### Added
- 설정 → 실험 기능 → AI 확장에서 자동으로 찾은 브라우저 엔진을 확인하거나 Chrome 또는 Chromium 실행 파일 경로를 직접 지정할 수 있습니다.
- 컴퓨터 사용 설정에서 Cua Driver 또는 SkyComputerUse를 선택할 수 있으며, 기본값은 SkyComputerUse로 유지됩니다. 백엔드를 바꾸면 현재 사용을 중지하며 기능을 다시 켜야 합니다.
- Cua Driver에서 반환할 트리 범위를 제한하고 바뀌지 않은 화면의 요소 핸들을 재사용하며 전체 트리 없이 특정 조건을 검증해 반복 관측을 줄입니다.
- Operation 브라우저: 에이전트 Operation마다 세션 옆에 자기 브라우저 패널을 열 수 있고, 에이전트는 사용자와 같은 탭에서 Fleet 브라우저 도구(이동·페이지 읽기·클릭·입력·스크린샷·콘솔·네트워크 로그)를 씁니다.
- 브라우저 패널에서 페이지에 주석을 답니다. 요소를 클릭해 번호 댓글을 남기거나 펜·화살표·사각형으로 그린 뒤 첨부하면, 표시된 스크린샷이 클립보드에 올라가 그 Operation의 입력창(채팅 컴포저 또는 터미널 CLI)에 붙여넣어집니다. 보내는 것은 Enter입니다.
- 에이전트가 브라우저를 쓰는 동안 Operation의 브라우저 버튼과 companion 패널이 그것을 보여 줍니다. 버튼은 에이전트 제어 색으로 채워지며 링이 퍼지고, 패널은 움직이는 윤곽선과 색조 캡션을 갖습니다. 첫 브라우저 호출부터 턴이 끝날 때까지 이어집니다.
- Console Use와 Computer Use도 같은 방식으로 보입니다. 에이전트가 쓰는 동안 Operation 창의 바깥선을 그 채널 색의 빛이 돌고, 캡션이 물들며, 캡션 배지가 맥동합니다. War Room 카드를 포함한 모든 배치에서 같습니다. Computer Use는 턴이 끝나면 기기를 놓습니다.
- companion 캡션이 곧 탭 스트립입니다. 파비콘, 새 탭, Google Chrome 프로필의 쿠키 가져오기, 툴팁이 있는 뷰포트 글리프가 한 줄에 있습니다.
- 로컬 Chromium(Google Chrome·Chromium·Microsoft Edge 또는 Playwright Chromium)이 필요합니다. WSL에서는 WSL 안에 Chrome이 없으면 Windows에 설치된 Chrome을 씁니다(Windows에 Node.js가 필요하며 Fleet Desktop의 런타임도 됩니다). Chrome을 찾지 못하면 브라우저 버튼이 흐려지고 툴팁이 무엇을 설치할지 알려 줍니다.

#### Changed
- Session Analyst 패널이 대화와 아티팩트에 전체 높이를 씁니다. 캡션 띠가 사라지고 초기화·아티팩트·대화로 돌아가기·내보내기·지우기가 입력 상자 위 한 줄의 아이콘으로 옮겨졌습니다. 초기화와 지우기는 두 번 눌러야 실행되며, 아티팩트를 보면서도 계속 질문할 수 있습니다.
- 그룹에 속한 Operation의 패널 캡션에 그룹이 색 칩으로 표시되어 어느 그룹인지 한눈에 알 수 있고, 사이드바 그룹 헤더의 색 점은 사라졌습니다.
- Console 상단의 Cruise·Tactical·War Room 스위치가 모드별 글리프로 바뀌고 검색과 Zen mode는 오른쪽에 구분되어 놓입니다. 모드 도구는 켜진 글리프에 마우스를 올리거나 누를 때 그 아래에 펼쳐지며, 도구가 기본값에서 바뀌면 글리프에 작은 점이 표시됩니다.
- 이제 모든 Operation이 사이드바에 브랜치를 보여주고, 겨누면 상태·위치·시작 시각 카드가 열립니다. 「Operation 위치 표시」는 실험 기능에서 빠졌고 따로 켤 설정이 없습니다.
- 우측 사이드바 패널과 확대 작업 표면에서 제목 줄이 사라지고 각 패널의 내용이 표면을 가득 채웁니다. 확대 표면에는 부유하는 닫기 버튼만 남습니다.

## [1.94.1] - 2026-09-15

### fleet-console

#### Changed
- War Room 사이드바 줄에서 그룹 배지를 내렸습니다. 무대에 선 패널의 캡션이 이미 그룹을 말해 주므로, 그 자리는 긴 Operation 제목이 가져갑니다.

## [1.94.0] - 2026-09-14

### fleet-console

#### Changed
- Repository의 탐색과 Git 도구를 분리하고 커밋 문맥과 스테이지 후 다음 파일 선택을 유지해 변경 검토를 이어가기 쉽게 다듬었습니다.
- War Room 사이드바 줄에서 Theater 배지를 내렸습니다. 덱이 이미 Theater별로 Operation을 묶어 보여 주므로, 그 자리는 긴 Operation 제목이 가져갑니다.

## [1.93.0] - 2026-09-13

### fleet-console

#### Added
- 에이전트가 Mac 앱의 창이 열려 있는지 확인하고, 명시적으로 다시 열어 컴퓨터 사용을 이어갈 수 있습니다.

## [1.92.0] - 2026-09-13

### fleet-console

#### Added
- Mac을 사용할 Operation을 선택하고 세션을 다시 시작하지 않아도 접근을 허용하거나 거둘 수 있습니다.
- Computer Use로 에이전트가 Mac 앱을 읽고 조작할 수 있으며, 어떤 Operation이 컴퓨터나 Console을 사용 중인지 확인할 수 있습니다.
- Console Use로 허용된 에이전트와 퀘이커 부관이 Operation을 시작하고 지시하며, 결과를 확인하고 반복 작업을 자동화할 수 있습니다. 에이전트의 접근 권한은 각 Operation 메뉴에서 관리합니다.
- 퀘이커 부관을 상단 바에 두고 필요할 때 답변을 읽거나 대화를 이어갈 수 있습니다.
- 퀘이커 부관마다 자기 메뉴에서 콘솔 사용·컴퓨터 사용을 허용할 수 있고(기본 꺼짐), 부관이 무엇을 할 수 있는지 권한 칩으로 확인할 수 있습니다.

#### Changed
- Cowork와 Session Analyst의 모델과 강도를 대화마다 고르지 않고 Settings › 실험 기능 › AI 확장에서 한 번 정합니다(기본 Sonnet · medium).
- 채팅·Cowork·Session Analyst·부관단 입력박스가 전송 버튼이 안에 든 한 줄로 바뀌고, 채팅 입력박스는 모델과 강도를 박스 위에 표시합니다.
- 각 Operation의 메뉴에서 세션 관찰, Console Use, Computer Use를 관리할 수 있습니다.
- 도움말에서 Console과 Desktop 버전을 확인하고, Console 업데이트를 적용하거나 최신 Desktop 다운로드를 열 수 있습니다.
- 「Operation 위치 표시」를 켜면 사이드바의 Operation에 마우스를 올려 상태, 작업 폴더, 시작 시각을 확인할 수 있습니다.
- 「Operation 위치 표시」를 켜면 Cmd+K 검색 결과에서 각 Operation의 브랜치와 작업 폴더를 확인할 수 있습니다.
- 부관의 답변을 읽다가 후속 대화로 이어가도 일관된 읽기 화면을 유지합니다.
- 완성된 부관 답변을 키보드로 읽고, 답변을 닫으면 이전 입력 위치로 돌아갈 수 있습니다.
- 채팅뷰·세션 분석가·코워크·퀘이커 부관 대화가 마지막 문답을 먼저 보여 주고, 이전 대화는 한 번 누르거나 위로 굴리면 볼 수 있습니다.
- 답이 오는 동안 세션 분석가·코워크·퀘이커 부관이 쌓이는 단계 목록 대신 지금 하는 일 한 줄만 조용히 보여 줍니다.
- 퀘이커 부관의 답이 표·인용·코드 블록까지 Console의 다른 화면과 같은 조판으로 읽힙니다.

#### Fixed
- Chat과 Terminal 보기 사이를 전환해도 Claude Code 모델 선택이 일관되게 유지됩니다.

#### Removed
- 일회성 리퀴드 글래스 소개 카드를 제거했습니다. 해당 외관 옵션은 설정에서 계속 사용할 수 있습니다.

### fleet-desktop

#### Added
- Operation 안에서 이동하거나 크기를 조절하고 최대화할 수 있는 실시간 앱 미리보기로 에이전트의 작업을 확인할 수 있습니다.

#### Changed
- 도움말 메뉴에서 Desktop 버전과 새 업데이트를 확인할 수 있습니다.

## [1.91.0] - 2026-09-12

### fleet-console

#### Added
- 「Operation 위치 표시」로 각 Operation의 브랜치와 작업 폴더를 봅니다. 설정 > 실험 기능에서 켭니다.

## [1.90.0] - 2026-09-12

### fleet-cli

#### Changed
- fleet-ai-gateway MCP resources로 모델 라우팅 지침과 실시간 모델 현황을 받습니다.
- 실행 중인 Console 없이 Wiki 도구는 `fleet-codex`, Gateway 모델 조회는 `fleet-console-use`로 씁니다.
- OpenCode Go의 DeepSeek Flash가 1M 토큰 컨텍스트의 DeepSeek V4.1 Flash로 바뀌었습니다.

#### Removed
- OpenCode Go 모델 선택에서 Ox Alpha Free를 제거했습니다.

### fleet-console

#### Added
- `fleet-console-use`로 Theater, Operation, Gateway 모델 도구를 쓰며, 플러그인 에이전트는 자체 도구와 함께 이 호스트 MCP를 붙일 수 있습니다.
- 플러그인은 Admiral MCP 도구를 `fleet-{pluginId}`로 제공할 수 있습니다. Codex Wiki 도구는 `fleet-codex`입니다.
- 어디서든 Ctrl+Backquote로 Global Shell을, Cmd/Ctrl+Shift+E로 Repository를 여닫습니다. Settings에서 단축키를 바꿀 수 있습니다.
- Settings에서 Console 키보드 단축키를 바꿉니다. 도움말, 명령 팔레트, 패널 힌트가 따르며 겹치는 키는 바꾸거나 기본값으로 되돌릴 수 있습니다.
- Zen mode는 탐색 크롬을 숨기고 Map을 키우며 열린 작업은 유지합니다. 단축키와 상시 종료가 있습니다.

#### Changed
- Agent 세션이 fleet-ai-gateway MCP resources로 모델 라우팅 지침과 실시간 모델 현황을 받습니다.
- 채팅, Analyst, Codex cowork의 에이전트 턴을 평평하게 했습니다. 진행 시계, 접힘 줄, 결말 문구가 그 상태를 보여 줍니다.
- 채팅의 생각 중 꼬리가 움직입니다. 점이 순환하고, 도구 호출 사이에 생각 글리프가 회전합니다.
- Theater가 없으면 사이드바가 무엇인지 설명하고 폴더 선택을 열며 설정 가이드로 안내합니다. 가이드는 명령 팔레트에서도 열 수 있습니다.
- OpenCode Go가 DeepSeek V4 Flash 대신 DeepSeek V4.1 Flash를 제공합니다.
- Cmd+K 팔레트 이름이 「이동」이며 Operation, 파일, Codex 문서로 갑니다. Cmd+K와 Cmd+P 행이 더 촘촘합니다.

#### Fixed
- Repository 스테이지 섹션이 긴 미스테이지 목록 위에 겹쳐 그려지지 않습니다.

#### Removed
- OpenCode Go 모델 목록에서 Ox Alpha Free를 제거했습니다.
- 실험 기능 런치 컨텍스트 팩과 관련 설정, 실행 전 Wiki 및 커밋 자동 수집을 제거했습니다.

## [1.89.0] - 2026-09-11

### fleet-cli

#### Added
- `fleet` 런처가 Console Settings에서 고른 내장 서브에이전트 옵트아웃을 따릅니다.

### fleet-console

#### Added
- Settings > 하네스 > Claude Code에서 설치된 Claude Code의 내장 서브에이전트를 보고 새 세션에서 끌 수 있습니다.
- 검색 팔레트에 Operation / 명령 스위치가 있습니다. Operation 행에서 인라인 동작을 열고, 범례가 팔레트 키를 보여 줍니다.
- 명령의 빈 화면이 구역 홈이 되어 글리프, 단축키 힌트, 표시된 파괴적 닫기를 보여 줍니다.

#### Changed
- 열린 팔레트에서 `Cmd/Ctrl+K`는 닫거나 명령에서 Operation으로 되돌립니다. `Cmd/Ctrl+P`는 명령으로 전환합니다.
- 명령이 영어와 한국어 모두에서 맞습니다. Operation 검색은 같은 퍼지 규칙을 쓰며, 활성 Theater가 먼저, 최근 Operation이 위에 섭니다.

#### Fixed
- Repository 패널이 새로고침, 동기화, 스테이징, 브랜치 선택, 커밋 이동 때 깜빡이지 않습니다.

## [1.88.0] - 2026-09-09

### fleet-console

#### Changed
- 채팅과 CLI 뷰를 전환하면 끝날 때까지 기다리지 않고 진행 중인 응답을 중단합니다.
- Repository 개편: 저장소·워크트리 전환기가 트리 앞에 서고, Fetch/Pull/Push/Stash가 한 작업 줄을 공유하며 패널이 좁아지면 접힙니다.
- Repository 트리가 워크트리, 브랜치, 원격, 태그, 스태시를 같은 단계로 보여 주며, 현재 브랜치에 체크와 ahead/behind 수를 표시합니다.
- Repository 컨트롤이 한 벌의 글리프를 씁니다. 결과는 상시 상태바 대신 잠시 뜨는 토스트로 보입니다.
- 변경 파일 목록이 한 줄 행으로, 경로가 잘려도 파일명은 보입니다.
- 커밋 검사기 파일 트리가 파일 탐색기 문법을 쓰고, 그 커밋의 diff 또는 그 시점 파일을 엽니다.

#### Fixed
- Quick Launch의 `@`·`/` 덱과 Theater/모델 팝오버가 뒤 화면을 흐리게 합니다.
- Repository 선택기 행에서 저장소 이름과 브랜치 라벨이 의도한 크기를 유지합니다.

## [1.87.1] - 2026-09-08

### fleet-cli

#### Fixed
- Codex(GPT) 게이트웨이 모델의 Claude Code 세션이 다시 답변합니다.

### fleet-console

#### Fixed
- Codex(GPT) 게이트웨이 모델로 도는 Operation의 채팅이 다시 동작합니다.

## [1.87.0] - 2026-09-08

### fleet-console

#### Changed
- Repository 커밋 독을 접힘·절반·전체 높이로 다시 짭니다. 비교는 열린 채이며, 행을 클릭해 마치고 Esc로 취소합니다.

## [1.86.0] - 2026-09-07

### fleet-cli

#### Added
- Cursor의 Fable 5.1을 게이트웨이 모델로 제공합니다. Low부터 Max 추론과 Cursor API 풀에서 차감되는 Max Mode 1M이 있습니다.

#### Changed
- 스트리밍 Claude Code 세션에서 GPT-6 Astra가 권한과 순차 호출을 지키면서 읽기 전용 도구와 다른 작업을 겹칩니다.
- gateway_models가 로그아웃이거나 모델이 없는 공급자를 빼고, quota 소비 순위를 숫자로 매깁니다.

### fleet-console

#### Added
- Settings > AI Gateway에서 켜면 Cowork 모델 메뉴에서 GPT-5.6 Luna(Codex)를 고를 수 있습니다.
- Cursor의 Fable 5.1을 게이트웨이 모델로 제공합니다. Low부터 Max 추론과 Cursor API 풀에서 차감되는 Max Mode 1M이 있습니다.
- 퀘이커 부관단이 공통 모델·강도 선택기, 첫 출근 소개 말풍선, 마지막 부관을 여는 Ctrl/Cmd+Shift+Q를 갖습니다.
- 부관 대화가 전체를 보여 주며, 여러 줄 입력, 멈추기, 복사, Quick Launch로 보내기, 출처 칩, 비용 줄을 갖습니다.
- 부관이 입력을 기다리는 Operation을 말풍선으로 알리고, 누르면 그 Operation을 엽니다.
- 포커스된 부관을 방향키로 옮기고 Space로 정박합니다. 지저귐이 보조 기술에 읽힙니다.

#### Changed
- AI Gateway 모델 로스터를 프로바이더별로 묶고, 머리글에 소진 순위를 두며 「모델 추가」 팔레트 크기를 조절할 수 있습니다.
- 스트리밍 Claude Code 세션에서 GPT-6 Astra가 권한과 순차 호출을 지키면서 읽기 전용 도구와 다른 작업을 겹칩니다.
- Operation 메뉴가 액센트 9색을 한 줄 스와치로 보여 주고, 겨눈 색 이름을 라벨 옆에 읽습니다.
- 패널 액센트가 캡션 제목 글자를 물들입니다. 제목 앞 액센트 막대는 사라졌습니다.
- 캡션 더보기 버튼이 창 컨트롤과 같은 hover를 쓰고, 메뉴가 열린 동안 켜진 채 있습니다.
- Codex Cowork 도크가 수정 스레드입니다. 지시마다 턴이 쌓이고 스텝 원장, 리뷰 도크, 실패·시간 초과·꺼진 모델 안내가 보입니다.
- Cowork 컴포저와 모델 메뉴가 Console 컴포저 문법을 쓰며, 모델 칩 옆에 강도 트랙이 있습니다.
- Codex 컴포저가 두 읽기 표면 위에 뜹니다. 입력 중 안내가 접혔다가 비우면 돌아옵니다.
- Drydock 제안자가 도구 식별자 대신 읽을 수 있는 출처 표식으로 보입니다.
- Quick Launch 프롬프트 다듬기가 Operation 멘션을 지원합니다. 후속 편집은 초안을 적용할 때까지 대화, 행선지, 첨부를 유지합니다.
- 부관이 설정, Quick Launch, 대화상자 위에 서지 않고 빠져나옵니다.
- Console이 부관 세션을 조용히 거둔 뒤에도 다음 질문이 짧은 알림과 함께 새 세션으로 이어집니다.
- 플러그인이 어디서나 「퀘이커 부관단」으로 불립니다. 부관은 기본적으로 Console 언어로 답합니다.
- Quick Launch 이미지 첨부가 프롬프트 오른쪽 위, 다듬기 옆에 있습니다. 멘션·고정 토글은 실행 옆에 붙습니다.
- 퀘이커 멘션 답이 말풍선에서 Markdown으로 보이며 스크롤 전에 자랍니다. 「이어 묻기」와 「답 복사」가 「전문 보기」를 대신합니다.
- Settings 확대 화면을 제거했습니다. 원격 접속은 Settings 패널에서 바로 관리합니다.
- 설정의 모델 선택기가 프로바이더 밴드, 컨텍스트 창 정보, 강도 세그먼트를 갖춘 하나로 통일됩니다.
- 설정이 플러그인과 코어 카드에서 같은 행, 스위치, 「기본값」 버튼이 있는 슬라이더를 씁니다.
- 퀘이커 부관단 출근이 다른 설정 선택기와 같은 세그먼트입니다. 「실험」 배지가 모든 카드에서 같습니다.
- 설정이 처음부터 넓게 열려 AI Gateway 로스터를 포함해 제목은 왼쪽, 컨트롤은 오른쪽에 둡니다.
- 표시 언어가 「겉모습」의 첫 카드입니다. 「언어」 칩과 선택 테마의 금색 띠는 사라졌습니다.
- 사이드바 칩이 캡션처럼 Operation 액센트를 제목 색으로 보여 줍니다. 왼쪽 액센트 막대는 사라졌습니다.

#### Fixed
- 캡션 메뉴가 더보기에서 안쪽으로 열려 뷰포트 안에 머뭅니다. Enter로 포커스가 들어가고, 좌우 화살표로 액센트 스와치를 오갑니다.
- Operation 메뉴 선택 행에 초록·분홍 테두리가 생기지 않습니다. 캡션 더보기 말풍선이 열린 메뉴 뒤에 남지 않습니다.
- Quick Launch에서 실행 버튼이 컨트롤 행에 남습니다. 긴 모델 이름은 줄어 Run이 둘째 줄로 밀리지 않습니다.

## [1.85.0] - 2026-09-06

### fleet-cli

#### Added
- OpenCode Go에 Grok 4.6, GLM 5.3, 이미지 전달을 지원하는 DeepSeek V4 Flash Vision Exp 등 현세대 모델을 추가했습니다.

#### Changed
- 모델 순위를 CursorBench와 LiveBench 비교 집합으로 매기고, 불완전한 측정치는 제외합니다.

### fleet-console

#### Added
- 설정에 「실험 기능」이 생기고 기본 꺼짐인 옵트인 네 가지를 둡니다. 프롬프트 다듬기, 런치 컨텍스트 팩, 세션 관찰, Scuttlebutt 부관의 Console 읽기입니다.
- 기존 선택을 유지하면서 OpenCode Go 모델 선택기에 그 현세대 모델을 추가했습니다.

#### Changed
- AI Gateway 모델 설정이 로스터 하나와 「모델 추가」 팔레트 하나를 쓰며, 프로바이더마다 로그인과 소진 순위가 있습니다. 별도 API key 카드는 사라졌습니다.
- Repository가 History와 Changes 중심으로, 저장소·워크트리 선택기, 범위 검색, 체크아웃별 커밋 초안을 제공합니다.
- 「상태별로 보기」(Alt+S)가 Theater를 대기·실행 중·유휴로 묶습니다. 「레일로 좁히기」가 사이드바를 접고, War Room에서는 기본입니다.
- Skills 탐색이 차분한 목록, 통합 상세·설치 시트, 범위 선택, 키보드 탐색, 분명한 불러오기 실패 안내를 씁니다.

## [1.84.0] - 2026-09-05

### fleet-cli

#### Added
- fleet gateway Codex 선택기에 GPT-6-Astra를 제공합니다. 272K·524K·1M과 각 Fast 짝이 있고, GPT-5.6도 그대로입니다.

### fleet-console

#### Added
- Console Codex 선별에 GPT-6-Astra를 제공합니다. 272K·524K·1M과 각 Fast 짝이 있고, GPT-5.6도 그대로입니다.

## [1.83.0] - 2026-09-03

### fleet-cli

#### Changed
- OpenCode Go가 Muse-Spark-1.2-Contributor 대신 Muse-Spark-1.3-Contributor를 제공하며, 1M 컨텍스트와 추론 강도 단을 유지합니다.

### fleet-console

#### Added
- 파일 트리에서 Space로 열지 않고 훑어봅니다. Enter는 열기, Escape나 Space는 닫기, 방향키는 다음 파일입니다.
- 스크롤하는 동안 열린 조상 폴더가 파일 트리 상단에 고정됩니다. 고정된 폴더를 누르면 그곳으로 돌아갑니다.
- 파일 트리 행에 마우스를 올리면 훑어보기, 상대 경로 복사, 파일 관리자에서 보기가 나타납니다.
- 문서 헤더의 경로 조각을 누르면 그 폴더가 열립니다. 복사 버튼은 상대 경로를, Alt+클릭은 절대 경로를 복사합니다.
- 설정에서 좌측 사이드바 불투명도와 유리 흐림을 정합니다. 우측 사이드바 불투명도 옆에 있습니다.

#### Changed
- Cruise 캔버스 축소가 함대 지도에서 멈추며, 지도를 그대로 둔 채 바다만 옅어지지 않습니다.
- 파일 트리 헤더가 필터 한 줄입니다. 정렬, 숨김 파일, 새로고침은 메뉴 하나에 있습니다.
- 파일 트리 Git 상태가 M/U/D 글자 대신 이름 색조와 행당 점 하나로 보입니다.
- 열린 문서가 밑줄 탭입니다. 닫기는 hover에 나타나고, 디스크에서 바뀐 파일은 점이 찍혀 클릭하면 다시 읽으며, 가운데 클릭으로 닫고, 넘친 탭은 목록으로 엽니다.
- 파일 트리가 불러오기 자리 표시, 빈 폴더 안내, 실패 시 다시 시도, 스크롤 가장자리 페이드를 보여 줍니다.
- Console OpenCode Go가 Muse-Spark-1.2-Contributor 대신 Muse-Spark-1.3-Contributor를 제공하며, 1M 컨텍스트와 추론 강도 단을 유지합니다.

## [1.82.0] - 2026-09-02

### fleet-cli

#### Added
- Antigravity Gemini 3.8 Flash와 낮음, 중간, 높음 추론 강도를 추가합니다.

#### Fixed
- Antigravity Gemini 모델에서 튜플 형태 도구 스키마를 허용합니다.

### fleet-console

#### Added
- AI Gateway 모델 선택기에 Antigravity Gemini 3.8 Flash를 추가합니다.
- Cruise를 판독 한계 아래로 줄이면 함대 지도가 섭니다. Theater 구역, 클릭하는 Operation 점, Theater를 올리는 표석입니다.

#### Changed
- 채팅 턴마다 살아 있는 줄이 하나입니다. 생각 중 점이 집계 꼬리에서 순환하고, 흐르는 답에 캐럿이 붙습니다.
- 채팅 원장의 문자 글리프를 균일한 모노라인 아이콘으로 바꿉니다.
- 채팅의 백그라운드 작업이 입력창 위 선반에 있고, 대화 위 시트로 열립니다. Escape나 대화 클릭으로 접습니다.
- War Room 덱 밀도는 1.0x~2.0x입니다. 0.4x 지도 밀도는 퇴역하고 Cruise 함대 지도가 맡습니다.
- 채팅 작업 선반을 입력창에 맞춥니다. 짧은 지시는 더 간결한 작성 영역을 씁니다.

#### Fixed
- Session Analyst 아티팩트의 한글을 번들 Console 서체로 그립니다.
- 고정폭 UI 한글을 OS 폴백 대신 번들 코딩 서체로 그립니다.
- Chat Mode에서 빠른 Shell 작업이 실행부터 완료까지 한 행으로 읽힙니다.
- Whites 테마의 진행 중·실패 스텝 테두리가 녹회색이 아니라 상태색으로 보입니다.

## [1.81.0] - 2026-09-01

### fleet-console

#### Added
- 접힌 패널이 화면 가장자리에 얇은 선을 남깁니다. hover로 미리 보고, 클릭하면 다시 엽니다.

#### Changed
- Session Analyst 아티팩트가 네 테마 모두에서 Console 디자인 언어를 씁니다.
- Session Analyst 채팅이 채팅 원장을 씁니다. 스트리밍 중 도구 한 줄, 끝난 턴 접힘, 답은 구분선 아래입니다.
- 분석가 입력창이 채팅과 같습니다. 입력 위, 좌표 레일 아래이며, 스트리밍이 시작되면 하단으로 내려앉습니다.
- Cruise / Tactical / War Room 스위치가 커맨드 밴드 정중앙에 있으며, 창이 너무 좁을 때만 왼쪽으로 갑니다.
- 세션 검색이 커맨드 밴드 중앙, 모드 스위치 오른쪽에 모든 화면에서 있습니다.
- 사이드바와 Activity Rail을 각 패널 안에서 접습니다. Cmd+B와 Cmd+Alt+B는 같은 상태를 토글합니다.
- Repository 컨트롤이 쉬는 상태에서도 채움과 테두리를 갖추며, hover 전까지 맨글자가 아닙니다.
- Repository 패널 밴드가 한 면을 공유합니다. 파일 상태 글자는 틴트 칩이 됩니다.
- 설정 설명이 제목 옆 ? 뒤로 갑니다. hover로 미리 보고, 클릭으로 고정하며, Esc로 닫습니다. 경고는 인라인에 남습니다.

#### Fixed
- 강도 트랙이 최저 단에서 손잡이 뒤에 brass 조각을 남기지 않습니다.
- 긴 분석이 2분에서 죽지 않습니다. 진행을 알리는 한 계속 이어집니다.
- 분석가 근거 인용이 원문 마크업 대신 칩으로 보입니다.

#### Removed
- 커맨드 밴드의 Theater·Operation 브레드크럼이 사라졌습니다. 전환과 이름 변경은 사이드바에서 합니다.
- 커맨드 밴드의 패널 접기 버튼 두 개가 사라졌습니다. 정체, 캔버스 모드, 검색, 시스템 컨트롤만 남습니다.
- 사이드바 카드 헤더와 인라인 Operation 필터가 사라졌습니다. 새 Theater는 목록 맨 아래와 커맨드 밴드에 남습니다.

## [1.80.0] - 2026-08-31

### fleet-console

#### Added
- 채팅 세션에서도 Artifact를 게시할 수 있습니다. 컴포저 덱에 artifact 스킬이 섭니다.
- 우클릭으로 Claude Operation을 채팅으로 바로 실행합니다. 메뉴가 그 표면을 기억하며 Quick Launch와는 따로입니다.

#### Changed
- Session Analyst 아티팩트가 Console 테마로 렌더되어 제목, 표, 코드, 인용이 제품처럼 읽힙니다.
- 채팅 입력창이 고른 읽기 폭을 따릅니다. 컴포저 컨트롤로 패널 전폭까지 넓힐 수 있습니다.
- Activity Rail이 도구별로 패널 폭을 기억해, 하나를 넓혀도 나머지가 따라 넓어지지 않습니다.
- 설정이 기본으로 테마 격자를 2열로 세울 만큼 넓게 열립니다.

#### Fixed
- 아티팩트를 받으면 테마를 유지한 자립 문서가 저장됩니다. 복사는 원본 소스를 줍니다.
- 느린 Windows 시작이 준비까지 기다리며, 실패 시 뒤에 Console을 남기지 않고 정리합니다.
- 레일 크기 손잡이를 더블클릭하면 지금 연 패널 폭만 초기화합니다.
- 사이드바의 활성 Theater가 brass 스파인, 채워진 배지, 밝은 이름으로 표시됩니다.

## [1.79.0] - 2026-08-30

### fleet-cli

#### Added
- Claude Code의 Codex 게이트웨이 모델이 자동·수동 압축, 영속 재개, 일반 텍스트 폴백을 포함한 OpenAI 네이티브 압축을 씁니다.

#### Changed
- `fleet`가 승인 게이트를 세운 auto 모드로 Claude Code를 실행합니다. 이전처럼 하려면 Settings > 하네스에서 「승인 프롬프트 건너뛰기」를 켜세요.

### fleet-console

#### Added
- Claude Code의 Codex 게이트웨이 Operation이 자동·수동 압축, 영속 재개, 일반 텍스트 폴백을 포함한 OpenAI 네이티브 압축을 씁니다.
- 사이드바에 Theater 수, 새 Theater, 인라인 제목 필터가 있는 카드 헤더가 생깁니다.
- 설정의 하네스 항목이 에이전트 실행 방식을 모읍니다. Claude Code 승인과 프롬프트, 공통 세션 설정, 에이전트 CLI 목록입니다.
- 「승인 프롬프트 건너뛰기」는 기본이 꺼짐입니다. auto 모드는 터미널과 `fleet`에서 승인 게이트를 유지하고, 채팅은 그 화면 없이 실행됩니다.

#### Changed
- Cruise, Tactical, War Room에서 작전 해도가 뷰포트를 채웁니다. 사이드바와 Activity Rail은 그 위 유리 카드로 뜹니다.
- 커맨드 밴드가 한 판입니다. Cruise, Tactical, War Room 스위치가 왼쪽 클러스터에 합류합니다.
- Activity Rail에는 패널이 한 번에 하나만 있습니다. 다른 패널을 열면 교체되며 해도를 밀어내지 않습니다.
- 새 Claude Code 세션이 승인 게이트를 건너뛰지 않고 auto 모드로 열립니다. 이전이 필요하면 하네스 옵트인을 켜세요.
- 터미널 설정에는 글꼴, 채팅 읽기 폭, 그리기만 남습니다. Claude Code 프롬프트와 에이전트 CLI 목록은 하네스로 옮겼습니다.
- File Explorer 뷰어가 레일의 독립 열이며, 캡션이 문서 이름과 이력, 다시 읽기, 줄바꿈, 확대를 싣습니다.
- 레일 열이 분할선 하나를 공유합니다. 끌거나 화살표로 옮기며, 표면마다 자리를 기억합니다.
- Codex 문서가 레일의 독립 열이며, 캡션에 이력과 확대가 있습니다.
- Repository 레일 아이콘이 Shell처럼 작업면을 캔버스에 엽니다. 다시 누르면 치웁니다.
- 설정이 Activity Rail 톱니 뒤로 옮겼습니다. 검색은 모든 설정과 명령 팔레트에 닿고, 겉모습을 고치는 동안 콘솔이 옆에 남습니다.
- 레일 톱니 메뉴가 사라졌습니다. 플로팅과 불투명도는 겉모습에 있고, 분할선 더블클릭으로 기억된 폭을 초기화합니다.
- 레일 패널 불투명도가 테마 카드의 「우측 사이드바 불투명도」로 옮겼습니다. 전용 레일 패널 카드는 은퇴했습니다.

#### Fixed
- 리퀴드 글래스를 켜도 좌측 사이드바에 가짜 격자 모자이크가 비치지 않습니다.

#### Removed
- 데스크톱 전용 설정 페이지와 커맨드 밴드 설정 버튼이 은퇴했습니다. 옛 /settings 링크는 새 페인으로 가고, 폰 설정은 그대로입니다.

## [1.78.0] - 2026-08-30

### fleet-console

#### Changed
- Activity Rail 설정이 아이콘 열 위 톱니 뒤에 있으며 Map 위에 띄우기, 불투명도, 폭 초기화, 패널 닫기가 있습니다.
- 전체 화면 터미널 앱이 대체 화면에서 자체 스크롤을 쓰고, 종료하면 일반 기록으로 돌아갑니다.
- Agent Operation과 전역 Shell이 안쪽 여백 없이 터미널 본문 전체를 씁니다.
- Shell과 Agent 터미널이 24비트 색을 알립니다. 다크 리퀴드 글래스 터미널이 스모크 면으로 캔버스를 비춥니다.

#### Fixed
- 세션에 포커스가 있는 동안 Session Analyst 패널이 또렷하게 남습니다.
- 새 소식 언어 선택기가 Console 표시 언어를 덮어쓰지 않습니다.
- 사이드바 표면을 닫았다가 다시 열어도 전역 Shell 세션과 터미널 내용이 남습니다.

#### Removed
- 레일 패널이 첫 줄 컨트롤을 가리는 헤더를 띄우지 않습니다.

## [1.77.1] - 2026-08-29

### fleet-console

#### Fixed
- 설치본에서 Codex가 다시 열립니다.
- 설치본에서 터미널의 공급자 제목이 브라우저 페이로드에 나가지 않습니다.

## [1.77.0] - 2026-08-29

### fleet-console

#### Added
- 채팅 컴포저에 능력 덱이 열립니다. `/`는 명령과 스킬, `@`는 서브에이전트입니다. 행을 고르면 보내지 않고 입력을 완성합니다.
- 덱은 이 표면이 끝까지 책임질 `/clear`, `/compact`, `/context`, `/reload-skills`를 제공합니다.
- 명령은 대화 턴이 아니라 기록의 자기 줄로 실행됩니다. `/compact`는 실제로 되찾은 문맥 비율을 계기로 보여 줍니다.
- `/clear`는 한 번 확인한 뒤 채팅 화면과 에이전트 기억을 함께 비웁니다.
- `/context`는 에이전트에게 인쇄시키지 않고 문맥 계기를 엽니다.
- 터미널 설정에 CJK 폴백 글꼴이 생겨, 터미널 글꼴에 글리프가 없을 때 한글·일본어·중국어를 그립니다.
- 설정에서 퀘이커 부관 크기를 정합니다. 끄는 동안 미리 보고, 한 번에 기본 크기로 되돌릴 수 있습니다.
- 캔버스 위 확대 작업면에서 문서와 터미널을 나란히 열고, 분할선을 끌어 폭을 정합니다.
- 플러그인이 레일 패널뿐 아니라 확대 작업면도 제공할 수 있습니다.

#### Changed
- 명령 이름을 끝까지 치면 그 명령이 덱의 첫 행에 섭니다.
- 채팅 뷰가 마크다운 답을 제외하고 터미널 글꼴을 써서, CLI와 채팅의 서체가 같습니다.
- Cruise, Tactical, War Room에서 패널이 많아도 터미널 입력이 반응합니다.
- 라이트 테마는 리퀴드 글래스를 쓰지 않습니다. 다크 테마로 돌아올 때까지 스위치는 꺼진 채 잠기며, 선택은 다크용으로 남습니다.
- 부관이 저마다의 크기로 화면 가장자리, 간격, 모션 축소 때 줄을 맞춥니다.
- 네 테마의 선택 컨트롤이 요란한 아웃라인 대신 옅은 워시와 작은 brass 표시를 씁니다.
- Shell이 Theater별 Operation이 아니라 콘솔 전체의 터미널로 확대 작업면에 섭니다. 열 때 활성이던 Theater에서 시작합니다.
- 레일 Shell 아이콘을 다시 누르면 끝내지 않고 치웁니다. 세션과 작업 디렉터리는 그 자리에 기다립니다.
- Codex가 독립 플러그인이 되어 다른 패널처럼 설치·갱신됩니다.
- War Room Map이 진입 연출 뒤 스캔 선을 반복하지 않고 멈춥니다.

#### Fixed
- 라이트 테마에서 채팅·분석가 라이브 줄의 스트리밍 물결을 되살렸습니다.
- 채팅 Thinking 줄의 스트리밍 물결을 되살렸습니다.
- `/doctor`, `/batch` 같은 번들 스킬이 첫 `/`부터 스킬 칸에 섭니다.
- 세션 도중에 스킬을 다시 읽으면 덱이 바로 갱신됩니다.
- 채팅과 터미널의 한글이 번들 등폭 서체로 그려져, 옆의 라틴 문자와 굵기·폭이 맞습니다.
- 채팅 뷰가 열리는 동안에도 Console이 요청에 응답합니다.
- Codex 목차가 지금 읽는 절을 따르며, 끝까지 내리면 마지막 절을 가리킵니다.
- 세로로 긴 패널에서 파일 탐색기 긴 목록을 스크롤해도 빈 칸 없이 이어집니다.
- War Room이 무대에 올린 패널 위에 어두운 비네트를 덮지 않습니다.

#### Removed
- 재시작 후 되살아나던 저장 Shell 패널을 없앴습니다. Shell은 그 콘솔과 수명을 함께합니다.

## [1.76.1] - 2026-08-28

### fleet-console

#### Fixed
- 채팅 컴포저에 포커스가 있어도 Alt+방향키로 패널을 옮길 수 있습니다.
- 채팅 백그라운드 작업 제목이 Windows에서 non-ASCII로 깨지지 않습니다.
- Codex 크게 읽기에서 제목과 본문이 모든 읽기 폭에서 한 중앙 축에 맞습니다.

## [1.76.0] - 2026-08-26

### fleet-console

#### Added
- Codex를 크게 읽을 때 머리줄에 찾기, 링크 복사, 원문, 읽기 폭이 있습니다. Cmd+K로 크게 보기를 유지한 채 항목을 바꿉니다.
- 읽고 있는 문서가 주소에 실려, 새로고침, 뒤로, 앞으로, 링크 복사가 같은 자리를 지킵니다.
- 크게 읽기가 키보드 포커스를 받습니다. Space와 PageDown으로 본문을 읽고, J/K로 섹션을, Cmd+[와 Cmd+]로 이력을, Cmd+F로 이 문서 안에서 찾습니다.
- 읽기 폭을 좁게·넓게·크게 고릅니다. 마크다운 원문을 보고 복사할 수 있고, 넓은 화면에서는 관련 항목이 본문 옆에 섭니다.
- 파일 탐색기에서 관련도순 이름 또는 내용으로 검색하고, 강조된 일치로 바로 이동합니다.
- 사용 한도 카드를 하나씩 접어도 사용률, 심각도, 리셋까지 남은 시간이 보입니다.

#### Changed
- Codex가 위키 변화를 스스로 따라갑니다. 읽는 중인 문서는 말없이 바뀌지 않습니다.
- Map에서 키보드 포커스가 보입니다. 나머지 패널이 물러나고 포커스 패널이 올라가며 brass 링이 한 번 돕니다. 설정 -> 겉모습에서 물러남을 정합니다.

#### Fixed
- 문서를 크게 열어도 목차가 첫 섹션에 얼어붙지 않습니다.
- 링크를 따라갔다가 돌아오거나 새로고침해도 읽던 자리가 유지됩니다.
- 설정 -> 겉모습의 콘솔 미리보기가 옆 테마 컨트롤과 같은 높이입니다.

## [1.75.0] - 2026-08-26

### fleet-console

#### Changed
- 라이트 테마에서도 리퀴드 글래스가 보여, 켜고 끄는 차이가 눈에 보입니다.
- 설정 화면이 커맨드 밴드 뒤로 스크롤되어 유리가 굴절할 내용을 갖습니다.
- 다크 테마 Maritime과 Carbon이 Cruise, Tactical, War Room에서 자기 Map 바닥을 갖습니다. Instrument는 그대로입니다.
- 설정이 하는 일 기준으로 묶이고, 이름이나 관련어로 검색합니다.
- 네 테마가 카드로 함께 보이고, 옆의 미리보기로 바로 확인합니다.
- 설정에서 켬/끔은 스위치, 택일은 분절 버튼입니다. 줄마다 적용 시점을 칩으로 밝힙니다.
- 활성 Theater를 바꾸지 않고도 사이드바의 모든 Theater 섹션을 접거나 펼칩니다.

#### Fixed
- 커맨드 밴드 환경 팝오버가 옆 메뉴와 같은 유리를 씁니다.
- 호스트가 제어권을 회수하거나 다른 기기가 접속한 뒤에는, 다시 연결할 때까지 원격 제어가 끊긴 채입니다.
- 표시 언어는 즉시 적용되고, Console 포트는 재시작을 기다립니다. 한 각주를 더 이상 공유하지 않습니다.
- 좁은 창에서도 설정 목록이 읽힙니다.
- 다크 유리에서 흐린 터미널 출력이 검은 덩어리로 찍히지 않습니다.

## [1.74.0] - 2026-08-24

### fleet-console

#### Changed
- 완료된 채팅 턴의 작업 요약이 최종 답에 이어집니다. 과정은 그 자리에서 펼쳐 볼 수 있습니다.
- Codex 읽기가 페인 폭에 맞춰 글자를 조절하고, 한 줄 개요로 시작해, 분할 페인에서는 본문이 먼저 보입니다.
- Codex AI 컴포저가 읽는 문단을 가리지 않고 읽기 프레임 경계에 앉습니다.
- Codex 카탈로그 행이 더 촘촘합니다. 예외 상태는 배지로, 태그는 한 줄로 보입니다.
- Codex 헤더 태그 칩이 카탈로그를 거릅니다. 시각은 카탈로그와 같은 상대시간입니다.
- Console 크롬, 설정, 내장 파일·저장소 도구의 hover, 포커스, 선택, 폼 피드백을 통일합니다.
- Glass 캡션이 더 잘 읽힙니다. 라우트나 사이드바가 바뀌어도 Command Band가 한 면으로 이어집니다.
- Cmd+K Operation 행이 사이드바와 같은 활동 마크를 쓰며, 미확인 완료 신호도 같습니다.

#### Fixed
- 리퀴드 글래스를 켜도 다크 테마 터미널이 캔버스로 가라앉지 않습니다.

## [1.73.1] - 2026-08-24

### fleet-console

#### Fixed
- 라이트 테마에서 터미널과 에이전트 패널 글자가 다시 또렷합니다.

## [1.73.0] - 2026-08-23

### fleet-cli

#### Changed
- Claude Code 패스스루 세션이 Fleet 플러그인 트리와 Harness 버전을 하나로 공유합니다.
- 예전의 공유 `marketplace/` 플러그인 트리에 더 이상 쓰지 않으며, 일주일 쓰이지 않으면 걷어냅니다.

### fleet-console

#### Added
- 채팅 컴포저도 Quick Launch처럼 `ultracode` 단어를 알아보면 무장합니다.
- 위키 문서를 확대하면 Codex 리딩 덱이 캔버스에 정박하고, 아웃라인, 읽기 폭 본문, 레일 카탈로그가 함께 있습니다.
- Codex 드라이독 검토가 렌더 diff와 변경만 토글을 보여 줍니다. 대기열 행에 제안자와 줄 diffstat이 있습니다.
- Codex 드라이독 대기열이 승인·반려된 패치를 결정 이력에서 다시 볼 수 있게 합니다.
- Codex 위키 링크에 호버 미리보기가 붙고, 각 문서가 자신을 참조하는 항목을 보여 줍니다.
- 떠 있는 메뉴, 팝업, 콘솔 크롬이 모든 테마에서 리퀴드 글래스 질감을 씁니다.
- 설정 - 테마에 「리퀴드 글래스」 체크박스가 기본으로 켜져 있습니다. 해제하면 불투명한 모습으로 돌아갑니다.
- 릴리스 노트를 닫으면 리퀴드 글래스를 소개하는 환영 카드가 한 번 뜨고, 끄는 방법도 안내합니다.

#### Changed
- Session Analyst가 채팅 원장과 같은 스트리밍 문법을 씁니다. 끝난 턴은 결말 한 문장으로 접힙니다.
- Chat Mode에서 첫 턴 컴포저로 시작하고, Esc로 멈추고, 예약한 지시를 취소하며, 읽는 동안 새 턴 수를 봅니다.
- Chat Mode 턴이 도는 동안 도구 호출은 진행 한 줄로 접히고, 백그라운드 작업은 앵커로 남습니다. 컴포저 글리프가 작업 면을 엽니다.
- 스트리밍 채팅 원장의 문장이 읽는 밝기를 유지하고, 집계 절 앞에 도구 계열 표식이 섭니다.
- Codex 패치 승인이 검토 상단의 고정 결정 독에 있어 근거와 결정이 한 화면에 남습니다.
- Codex 내비게이터 상태가 상시 칩과 충돌 수로 보입니다. 목록은 신선도로 묶입니다.
- Codex 충돌 상세가 전문 두 벌 대신 현행·제안 블록을 비교합니다.
- 파일 탐색기 폴더에 회전 펼침 chevron과 인덴트 가이드가 있고, 선택 행에 brass 스파인이 있습니다.
- 파일 트리 정렬이 전체 순서를 메뉴로 보여 줍니다. 헤더 버튼이 커지고, / 가 필터에 포커스를 줍니다.
- 파일 뷰어 헤더가 브레드크럼입니다. 조각을 클릭하면 그 경로를 복사하고, 폴더를 더블클릭하면 트리에서 펼칩니다.
- 이미지 뷰어에 맞춤/100% 배율과 치수·용량·표시 배율 메타 바가 있습니다.
- 새 Operation이 자기 id로 Claude 세션을 열어, 재개가 생성 시점에 확정됩니다.
- Fleet 플러그인을 싣지 못한 Chat 세션은 스킬 없이 돌지 않고 그 턴을 거부합니다.
- Claude 세션이 Fleet 플러그인 트리와 Harness 버전을 하나로 공유합니다.
- Claude Code 실행과 재개가 하나의 영속 세션 정체성을 쓰며, 자동 마이그레이션이 있습니다.
- Repository에서 변경 뷰로 가면 커밋 검사기가 한 줄로 접힙니다. 한 번 누르면 보던 커밋으로 돌아갑니다.
- 스태시를 열면 untracked를 포함한 파일과 적용, 적용 후 제거, 삭제가 보입니다. Stash는 저장 전에 메시지를 묻습니다.
- 스테이징 목록에 목록/트리 토글이 있습니다. 행 동작은 hover 시 스테이지, 내리기, 버리기, 삭제로 적힙니다.

#### Fixed
- 공급자 마크가 xAI, OpenCode, Kimi를 포함해 고유 색을 유지합니다.
- 뷰가 중간에 붙어도 Chat Mode 턴이 스트리밍 동안 작업 중으로 보이며, 답이 오는데 끝난 것처럼 보이지 않습니다.
- Codex 검색 발췌가 단어 중간에서 시작하지 않고, 줄바꿈을 한 칸으로 접지 않습니다.
- 라이트 테마 사용량 막대가 심각도 순으로 읽혀, 소진된 막대가 평상시보다 가볍게 보이지 않습니다.
- 라이트 테마의 추론 강도 손잡이와 트랙이 화면의 나머지를 누르지 않습니다.
- 라이트 테마에서 ULTRACODE와 MAX 단의 강도 손잡이 링이 다시 보입니다.
- 라이트 테마에서 게이트 뒤 ULTRACODE·MAX 트랙을 차분하게 합니다.
- Chat Mode와 터미널이 같은 세션 정의를 받아, 어느 표면으로 열어도 스킬과 설정이 같습니다.
- 툴바에서 스태시나 Pull을 하면 스테이징 목록이 바로 갱신됩니다.

#### Removed
- Chat Mode의 「이전 턴 재생됨」 안내를 없앴습니다. 복원된 대화는 그대로 보입니다.

## [1.72.0] - 2026-08-22

### fleet-cli

#### Added
- `agy` CLI 로그인으로 Antigravity 구독을 Claude Code 턴에 쓸 수 있습니다. AI Gateway 설정에서 켜면 Gemini 3.7 Flash와 Gemini 3.1 Pro를 고릅니다.
- `fleet gateway`로 터미널에서 AI Gateway를 구성하고, `fleet gateway status`, `models`, `set`으로 조회하거나 바꿉니다.
- `fleet gateway serve`로 Anthropic API 클라이언트가 `ANTHROPIC_BASE_URL`로 구독을 씁니다. 루프백에만 열리며 인증은 없습니다.

#### Changed
- `fleet --help`가 런타임, 명령, 설정, 유지보수로 나뉩니다. 자세한 내용은 `fleet <runtime> --help`에 있습니다.
- 공급자 인증이 `fleet gateway auth`로 옮겼습니다. 기존 `fleet auth`도 동작하며 어디로 갔는지 알려 줍니다.
- 비Anthropic AI Gateway 공급자에게 Claude Code 정체성과 Anthropic 빌링 헤더를 보내지 않습니다. Anthropic이 직접 처리하는 턴은 그대로입니다.

### fleet-console

#### Added
- `agy` CLI 로그인으로 Antigravity 구독을 Claude Code 턴에 쓸 수 있습니다. AI Gateway 설정에서 켜면 Gemini 3.7 Flash와 Gemini 3.1 Pro를 고릅니다.
- Quota 패널에 Antigravity 사용량과 Gemini 5시간·주간 한도를 보여 줍니다.

#### Changed
- 비Anthropic AI Gateway 공급자에게 Claude Code 정체성과 Anthropic 빌링 헤더를 보내지 않습니다. Anthropic이 직접 처리하는 턴은 그대로입니다.

## [1.71.1] - 2026-08-22

### fleet-cli

#### Fixed
- 위임 후보를 고를 때 Cursor Fast 변형을 기반 모델 등급으로 읽어, 플래그십 우선 티어가 경량으로 밀리지 않습니다.

### fleet-console

#### Added
- 소스 트리에 원격을 건드리지 않고 로컬 저장소 상태를 다시 읽는 새로고침이 있습니다.
- 잘린 읽기가 잘렸다고 말해, 완전한 것처럼 보이지 않습니다.

#### Changed
- 저장소의 원격 동사가 Fetch입니다. 결과 문구가 가져온 원격을 말하고, 툴팁에서 git 플래그가 빠집니다.
- 파괴적 동사가 무엇을 없애는지 말합니다. 추적되지 않는 파일 삭제와 추적 파일 변경 버리기의 이름이 갈립니다.

#### Fixed
- Cursor Fast 변형이 LIGHT가 아니라 기반 모델 등급을 달고, Composer 2.5는 Cursor와 xAI 모두 STANDARD입니다.
- 저장소 읽기 실패가 오류 코드 대신 문장과 다음 행동을 보여 줍니다.
- 상태를 읽지 못한 저장소가 깨끗한 것처럼 보이지 않습니다. 쓰기 동사는 잠긴 채 이유를 말합니다.
- 변경 뷰가 좁은 레일에서도 쓸 수 있습니다. 파일 목록과 diff가 쌓여 파일 이름이 눌리지 않습니다.
- diff의 추가·삭제 줄이 모든 테마에서 신호색을 지킵니다.

## [1.71.0] - 2026-08-21

### fleet-cli

#### Added
- Fleet 시스템 프롬프트 없이 온디맨드 Professional Pushback과 오케스트레이션 스킬을 추가합니다.
- OpenCode Go 모델 Ox-Alpha-Free와 Muse-Spark-1.2-Contributor를, 각 백엔드가 받는 추론 강도와 함께 제공합니다.

#### Fixed
- 오래 걸리는 턴이 모델이 아직 작업 중일 때 끊기지 않고 끝납니다.
- 공급자가 끊거나 거절한 게이트웨이 턴을 첫 시도에서 끝내지 않고 복구합니다.
- 공급자당 연결 수에 상한을 두어, 넓은 병렬 실행이 스트림을 잃지 않고 대기합니다.
- 실패한 게이트웨이 턴을 지속 로그에 기록합니다.

#### Breaking Changes
- 모든 `Workflow` 스테이지가 다시 게이트웨이 모델을 지정해야 합니다. 지정하지 않으면 실행 전에 거부되고, 스크립트의 `agentType`도 거부됩니다. `fleet:*` 정체성만 적어도 디스패치됩니다.

### fleet-console

#### Added
- Fleet 시스템 프롬프트 없이 게이트웨이 Operation에 온디맨드 Professional Pushback과 오케스트레이션 스킬을 추가합니다.
- OpenCode Go 모델 Ox-Alpha-Free와 Muse-Spark-1.2-Contributor를, 각 백엔드가 받는 추론 강도와 함께 제공합니다.

#### Changed
- 위임은 한 단입니다. 위임받은 에이전트가 다시 에이전트를 띄우지 못합니다.
- 파일 패널 검색이 의존성·빌드 폴더를 건너뜁니다. `deep/needle` 같은 경로 조각도 맞으며, Escape는 문서가 아니라 필터를 지웁니다.
- 열어 둔 문서가 디스크에서 바뀌면 표식이 서고, 클릭하면 다시 읽습니다.
- 큰 파일은 화면에 보이는 줄만 그려 바로 보입니다. 긴 줄은 옆으로 흐르지 않고 줄바꿈할 수 있습니다.
- 미커밋 변경이 상위 폴더까지 접혀, 루트에서 작업 세트가 보입니다.
- 문서를 열 때 패널 폭이 창에서 정해지고, 숨긴 열린 파일 수가 표시됩니다.
- 파일 트리에서 이름 첫 글자로 건너뛰고, PageUp/PageDown을 쓰며, Shift+F10 또는 Context Menu 키로 행 메뉴를 엽니다.
- Ledger가 지출을 백엔드별로 묶어, 모델 행을 열기 전에 어느 공급자로 갔는지 보여 줍니다.
- Shell Operation이 실행·대기 마크 대신 Shell 종류 글리프로 보입니다.
- 설치된 스킬 카드가 그 스킬이 하는 일을 보여 줍니다. 같은 이름 전역 스킬을 가리는 프로젝트 스킬은 표시됩니다.
- 스코프 전체 스킬 업데이트가 목록 위 한 동작으로, 몇 개를 건드리는지 적힙니다. 제거는 글리프가 아니라 낱말입니다.
- 설치된 스킬 필터가 설명까지 읽어, 설명에만 있는 낱말로도 찾습니다.

#### Fixed
- 오래 걸리는 턴이 모델이 아직 작업 중일 때 끊기지 않고 끝납니다.
- 공급자가 끊거나 거절한 게이트웨이 턴을 첫 시도에서 끝내지 않고 복구합니다.
- 공급자당 연결 수에 상한을 두어, 넓은 병렬 실행이 스트림을 잃지 않고 대기합니다.
- 실패한 게이트웨이 턴을 지속 로그에 기록합니다.
- 새로고침 뒤에도 열어 둔 폴더가 실제로 열린 채 복원됩니다.
- 긴 목록이 안내문이 말하는 이름순 경계에서 잘립니다.
- 열기에 실패한 폴더는 조용히 접히지 않고 이유와 다시 시도를 단 채 열린 채입니다.
- 턴이 끝나야 사용량을 주는 게이트웨이 모델도 실행 중 토큰 수를 보여 줍니다.
- `.cmd` shim으로 에이전트 CLI를 실행하는 Windows에서 여러 줄 Quick Launch 프롬프트가 전문 그대로 전달됩니다.
- Ledger 일별 막대가 Today뿐 아니라 모든 기간에서 그날의 모델을 엽니다. 보여줄 것이 없는 날은 죽은 버튼이 아닙니다.
- 일별 차트에 놓지 못한 지출을 Ledger가 금액으로 밝혀, 합계와 차트가 말없이 어긋나지 않습니다.
- 설치된 스킬이 어느 레지스트리에서 왔는지 다시 표시됩니다.

## [1.70.0] - 2026-08-20

### fleet-cli

#### Added
- 도구 왕복 뒤 Grok이 앞서 한 추론을 되돌려 보내, 이미 한 생각을 다시 하지 않게 합니다.

#### Changed
- 워크플로 스테이지가 게이트웨이 모델을 반드시 지정하지 않아도 됩니다. 지정하지 않으면 세션 모델로 돌고, `agentType`으로 정체성을 다시 핀할 수 있습니다. `Agent` 위임은 게이트웨이 정체성이 필요합니다.
- Grok 턴이 공식 Grok CLI가 쓰는 엔드포인트로 가며, xAI 구독이 기대는 경로입니다.
- 그 엔드포인트에 이 기기에 설치된 Grok CLI 버전을 알립니다.

#### Fixed
- Codex 턴이 대화 전체를 다시 계산하지 않고 프롬프트 캐시를 재사용합니다.
- xAI가 용량 부족으로 거절한 Grok 턴을 재시도하고, 재시도까지 거절되면 과부하로 알립니다.

### fleet-console

#### Added
- Settings > AI Gateway에서 Grok 엔드포인트를 고릅니다. 기본은 Chat Proxy이고, Direct는 xAI 자체 API입니다. 둘 다 같은 구독을 씁니다.
- 도구 왕복 뒤 Grok이 앞서 한 추론을 되돌려 보내, 이미 한 생각을 다시 하지 않게 합니다.

#### Changed
- Session Analyst 모델 선택 목록의 모델명이 캔버스 우클릭 메뉴와 같은 서체를 씁니다.
- Pull·Push·Stash 결과가 저장소 패널을 밀어내던 배너 대신 버튼 자리에서 보입니다.
- 우측 레일에서 Theater당 Shell Operation 하나를 만듭니다. 이미 있으면 다시 눌러 그 패널에 포커스합니다.
- Grok 턴이 기본적으로 공식 Grok CLI 엔드포인트로 가고, 이 기기의 Grok CLI 버전을 알립니다.

#### Fixed
- Codex 턴이 대화 전체를 다시 계산하지 않고 프롬프트 캐시를 재사용합니다.
- xAI가 용량 부족으로 거절한 Grok 턴을 재시도하고, 재시도까지 거절되면 과부하로 알립니다.

#### Removed
- 우측 레일에서 알림 패널을 뺍니다. Operation 주의는 왼쪽 목록과 모바일 알림 탭에 남습니다.

## [1.69.0] - 2026-08-20

### fleet-cli

#### Added
- Cursor의 GPT-5.6 Sol·Terra·Luna, Gemini 3.7 Flash, Kimi K3를 게이트웨이 모델로 제공하며, Cursor가 공개한 추론 단계를 따릅니다.

### fleet-console

#### Added
- Cursor의 GPT-5.6 Sol·Terra·Luna, Gemini 3.7 Flash, Kimi K3를 게이트웨이 모델로 제공하며, Cursor가 공개한 추론 단계를 따릅니다.

## [1.68.0] - 2026-08-19

### fleet-cli

#### Changed
- 게이트웨이 세션이 Fleet 시스템 프롬프트와 Fleet 스킬을 싣지 않습니다. 게이트웨이 정체성 없는 위임은 안내와 함께 거절됩니다.

### fleet-console

#### Added
- 파일 탐색기 뷰어가 열린 파일을 칩 스트립으로 유지하고, 뒤로/앞으로, Escape 닫기, 마크다운 미리보기/소스, 크기 메타 바를 제공합니다.
- 파일 트리에 이름/수정시각/크기 정렬이 생깁니다. 작업 트리에서 삭제된 파일은 취소선 고스트 행입니다.
- 열린 파일과 펼친 폴더를 Theater별로 기억해 리로드 후 복원합니다.
- 저장소 패널이 Git 클라이언트처럼 동작합니다. 미스테이지/스테이지, 개별·일괄 스테이지와 해제, 2단계 버리기, amend 커밋입니다.
- 저장소 툴바에 Sync와 함께 Pull, Push, Stash가 생기고 ahead/behind가 보입니다. Pull은 fast-forward만 합니다.
- 체크아웃 탭으로 루트와 워크트리를 오갑니다. 커밋 검사기 File Tree가 그 커밋의 전체 트리를 탐색합니다.
- index가 잠겼거나 merge/rebase/cherry-pick 중이면 쓰기 동작이 잠기고, 같은 체크아웃에 Operation이 있으면 경고합니다.
- 설정에 Claude Code 시스템 프롬프트 스위치가 있습니다. On은 자체 프롬프트, Off는 빈 프롬프트입니다. 새 세션에만 적용됩니다.
- AI Gateway 모델 목록에 xAI Grok Composer 2.5 Fast를 넣습니다.

#### Changed
- Grok 턴이 Grok CLI 프록시 대신 xAI Responses 엔드포인트로 갑니다. 직결을 거부당한 계정은 프록시로 되돌아갑니다.
- WORKING > 변경이 스테이징 워크벤치가 되어, 통합 목록 대신 미스테이지/스테이지로 나뉩니다.
- 저장소 워크스페이스 필터가 브랜치, 태그, 스태시, 워크트리까지 거릅니다.
- 스태시 행 메뉴에서 적용, 적용 후 제거, 삭제를 실행합니다. 삭제는 2단계 무장입니다.
- 사이드바가 Theater 목록 위에 그룹 | 상태 스위치 하나를 둡니다. Alt+S로 바꾸고, 세션마다 그룹에서 시작합니다.
- Theater의 대기 표식이 그 Theater 이니셜 위에 있어, 어느 Theater가 기다리는지 말합니다.
- 게이트웨이 정체성을 핀하지 않은 Agent·Workflow 실행은 gateway_models를 읽고 지정하라는 안내와 함께 거절됩니다.
- 업데이트 후 같은 주소로 돌아오고 열린 탭이 다시 연결됩니다. 옛 주소를 못 되찾을 때만 새 창이 열립니다.
- 업데이트 중에는 연결 오류 대신 진행 상태가 보입니다.
- 업데이트 표식이 도움말 버튼으로 옮겼고, 메뉴가 설치할 버전을 표시합니다.
- 원격 세션에서의 업데이트는 먼저 확인합니다. 다른 기계의 Console을 다시 시작합니다.

#### Fixed
- 캡션 버튼 이름표가 포인터가 떠나면 바로 사라집니다. 키보드 포커스에서는 이름이 남습니다.
- 토리·보리·도리의 제자리에 두기가 Console 재시작이나 업데이트 뒤에도 유지됩니다.
- 설치 중인데 완료했다고 말하지 않습니다. 실패한 업데이트는 사유와 함께 실패를 알립니다.
- Console이 다시 시작된 뒤 원격 화면이 스스로 복구되며, 만료된 세션으로 영원히 재시도하지 않습니다.
- 원격 접속을 켠 Console이 종료 요청에 응답합니다.
- 스트리밍 도중 업스트림이 조용해진 Grok 턴을 끝냅니다. 무한정 기다리지 않습니다.

#### Removed
- Fleet 시스템 프롬프트 모드 설정이 사라졌습니다. 게이트웨이 세션과 Chat Mode가 Fleet 시스템 프롬프트를 더 이상 받지 않습니다.
- War Room 덱 카드의 hover 확대를 없앱니다. 클릭하면 무대에 오르고, 지도 마커 hover로 패널을 띄우는 동작은 그대로입니다.

### fleet-desktop

#### Changed
- Console 안에서의 업데이트가 Desktop에서도 동작합니다. 셸이 자기 재시작으로 수행합니다.

#### Fixed
- 원격 세션이 끝난 창에는 필요 없는 액세스 링크 대신 해당 호스트를 다시 열라고 안내합니다.

## [1.67.0] - 2026-08-18

### fleet-cli

#### Changed
- 에이전트가 셸 명령만이 아니라 Grep, Glob 도구로 검색합니다.

#### Fixed
- Grok 답변 끝에 모델이 텍스트로 흘리는 `<|eos|>` 마커가 붙지 않습니다.

### fleet-console

#### Added
- Chat Mode 패널에 읽기 폭에 맞춘 전용 컴포저가 생깁니다. Enter 전송, 패널별 초안, 이미지 첨부, 턴 중 중지입니다.
- Operation 채팅에 「채팅 읽기 폭」이 생겼습니다. 기본·넓게·전체를 패널 캡션이나 Terminal 설정에서 바꿉니다.

#### Changed
- 접힌 채팅 턴 요약이 결말만 말합니다. 개별 실패는 과정을 펼치면 보입니다.
- 에이전트가 셸 명령만이 아니라 Grep, Glob 도구로 검색합니다.
- Operation 패널 컨트롤이 캡션에 있습니다. Session Analyst, 채팅/터미널, 읽기 폭입니다. 채팅 로그는 패널 맨 위에서 시작합니다.
- 채팅 문맥 미터가 컴포저 행, 전송 바로 왼쪽에 앉습니다.
- 캡션 레일 움직임이 상태마다 다릅니다. 백그라운드는 흐름, 기다림은 맥동, 끝나 미확인은 느린 숨입니다. 모션 최소화 때 백그라운드는 점선 레일입니다.
- War Room 카드가 포인터에 바로 brass 실선과 밝아진 캡션으로 답합니다. 키보드로 닿은 카드도 같은 표시입니다.

#### Fixed
- Alt+F, Alt+T 같은 화면 전환이 텍스트 입력 중에도 동작합니다. Alt+화살표는 그대로 단어 단위 커서 이동입니다.
- 채팅 Operation 턴이 끝난 뒤에도 서브에이전트나 워크플로가 돌면 유휴가 아니라 백그라운드로 보입니다.
- Fleet이 에이전트 작업으로 보지 않는 백그라운드는 Operation 백그라운드 상태에 세지 않습니다.
- 에이전트 CLI 내부 줄이 사용자가 보낸 말풍선으로 재생되지 않습니다.
- Cruise Map이 키보드 포커스를 받지 않아, 채팅에서 Enter를 눌러도 지도에 금색 띠가 생기지 않습니다.
- Claude Code 한도 임박 마무리가 게이트웨이 요청에 실리지 않아, 다른 공급자 작업이 줄어들라고 듣지 않습니다.
- Grok 답변 끝에 모델이 텍스트로 흘리는 `<|eos|>` 마커가 붙지 않습니다.
- 눌린 캡션 컨트롤이 모든 테마에서 brass로 그려지며, 초록이나 자홍에 착지하지 않습니다.
- War Room 카드 상태 링이 캡션을 포함해 카드 전체를 감쌉니다.
- War Room에 들어오기 전부터 포커스한 패널도, 선별 중 끝나면 무대에 오릅니다.
- 도구 호출은 도착했는데 스트림이 닫히기 전에 멈춘 Grok 턴을 오류로 끝내지 않고 살립니다.

#### Removed
- 채팅 로그가 시작 좌표 줄로 열리지 않습니다. 컴포저 배지가 이미 그 사실을 집니다.
- 채팅에서 Quick Launch를 열던 말풍선과 캡션 좌표 배지, 떠 있던 중지가 퇴역했습니다. 인패널 컴포저가 대신합니다.
- 좌측 사이드바 Operation 행에서 CLI/CHAT 표면 칩을 제거합니다.

### fleet-desktop

#### Fixed
- Windows에서 배율이 다른 모니터로 옮기거나 줌을 바꿔도 창 제어 버튼이 상단 밴드에 맞습니다.

## [1.66.0] - 2026-08-17

### fleet-cli

#### Added
- Codex Sol, Luna, Terra 선택기에 Fast를 포함한 명시적 524K 변형을 기존 272K 및 1M과 함께 복원합니다.

### fleet-console

#### Added
- Codex Sol, Luna, Terra 선택기에 Fast를 포함한 명시적 524K 변형을 기존 272K 및 1M과 함께 복원합니다.
- 채팅 패널이 세션의 모델과 강도를 칩 줄과 로그 첫 줄에서 밝히고, ultracode 세션은 apex 티어로 표시합니다.

#### Changed
- Session Analyst 모델 메뉴가 캔버스 실행 메뉴처럼 공급자를 묶고, 창 안에 머뭅니다.
- Session Analyst의 모델·강도를 Quick Launch와 같은 칩과 3단 트랙으로 고르며, 강도는 low·medium·high만입니다.
- 왼쪽 사이드바에서 백그라운드 그룹을 없앱니다. 남은 작업은 실행 중에 앉고, 백그라운드라는 사실은 행 마크가 말합니다.
- 끝나 아직 열지 않은 사이드바 행을 활동 마크 하나로만 말합니다. 별도 점, 틴트, 헤더 카운트로 되풀이하지 않습니다.
- 활동 마크가 답을 기다리는 Operation과 열지 않은 채 끝난 Operation을 가릅니다. 둘 다 상태 목록 위에 모입니다.
- 사이드바와 War Room의 상태 그룹이 선반 캡션 한 줄로 접힙니다. 복원 동사는 최소화·종료됨에만 남습니다.

#### Fixed
- 채팅 뷰가 Claude Code의 200k 좌표가 아니라 게이트웨이 모델의 실제 컨텍스트 창을 말합니다.
- 채팅 컨텍스트 미터가 턴이 도는 동안 움직여, 모델이 방금 읽은 파일이 작업 중에 드러납니다.
- Cruise에서 우클릭으로 만든 패널이 기본 계단이 아니라 클릭한 자리에 놓입니다.
- 그룹 축과 최소화 선반을 포함해 모든 사이드바 목록이 같은 활동을 읽습니다.

### fleet-mobile

#### Added
- iPhone과 iPad용 Fleet Console 앱을 추가합니다. 같은 네트워크 Console에 안전하게 페어링하고, 테스터 빌드는 TestFlight로 배포합니다.

## [1.65.0] - 2026-08-17

### fleet-cli

#### Changed
- Codex Sol, Luna, Terra 선택기에서 512K 변형을 제거하고 기존 272K와 명시적 1M만 유지합니다.

### fleet-console

#### Changed
- Codex Sol, Luna, Terra 선택기에서 512K 변형을 제거하고 기존 272K와 명시적 1M만 유지합니다.
- Chat Mode가 실패한 스텝과 Theater 밖 쓰기도 문장별 집계에 일반 도구와 같이 접습니다.

## [1.64.0] - 2026-08-17

### fleet-cli

#### Changed
- Codex Sol, Luna, Terra는 기존 272K를 유지한 채 Fast를 포함한 명시적 512K와 1M을 추가합니다.

### fleet-console

#### Added
- 도는 백그라운드 작업을 작업 상세에서 하나씩 중단할 수 있습니다.
- 빈 캔버스의 「모두 Tactical로 열기」가 꺼둔 Operation 전부를 자동 정렬 그리드로 재개합니다. 8개를 넘으면 버튼이 한 번 더 묻습니다.

#### Changed
- Codex Sol, Luna, Terra는 기존 272K를 유지한 채 Fast를 포함한 명시적 512K와 1M을 추가합니다.
- Chat Mode가 Operation이 열린 동안 에이전트 세션을 살려, 백그라운드 셸·서브에이전트·워크플로가 시작한 턴보다 오래 삽니다.
- 채팅 턴 중지가 세션을 끝내지 않고 에이전트를 중단시켜, 앞서 시작된 백그라운드 작업이 계속 돕니다.
- 빈 캔버스 대기 목록이 꺼둔 Operation 전부와 최근 활동 시각을 보여 줍니다. 한국어 표기는 「꺼둔」입니다.
- 상태별 정렬과 War Room 그룹 헤더에서 둥근 상태 마크를 뺍니다. 각 패널이 이미 그 마크를 보여 줍니다.

#### Fixed
- Chat Mode 호스트 원장에서 중첩 서브에이전트 프레임을 빼, 도구와 보고가 작업 면에만 남게 합니다.
- Quick Launch에서 ULTRACODE를 고른 채 채팅으로 열면 max 강도로 접히지 않고 다이나믹 워크플로우를 유지합니다.

## [1.63.0] - 2026-08-16

### fleet-cli

#### Changed
- `fleet --version`, `-v`, `version`이 Fleet 패키지 버전과 채널을 출력합니다. Claude Code 버전은 그대로 `fleet cli --version`입니다.
- `fleet auth login`과 `logout`이 알 수 없는 공급자 이름을 픽커로 넘기지 않고 거절합니다.
- `fleet doctor`가 설치본, PATH의 Claude Code, 게이트웨이 인증, Console 상태를 바꾸지 않고 보고합니다. `fleet status`는 `fleet console status`와 같습니다.
- `fleet update --check`가 설치하거나 Console을 내리지 않고 새 패키지 여부만 보고합니다.

### fleet-console

#### Added
- 채팅 세션이 컨텍스트 창을 얼마나 쓰는지 보여 줍니다. 미터 칩이 내역을 열고, 접힌 턴 줄마다 그 턴이 더한 토큰이 붙습니다.

#### Changed
- 모든 AI Gateway 모델이 두 런치 강도 컨트롤에서 ULTRACODE를 제공합니다. MAX가 없는 모델은 그 단을 건너뜁니다.
- 우현 레일 패널이 본문 활자 크기를 공유해, 도구를 오가도 글자 크기가 바뀌지 않습니다.
- 커맨드 밴드 컨트롤이 한 높이에 서서 로컬 칩, 브레드크럼, 모드 스위치가 같은 기준선에 맞습니다.
- 파일 미리보기 코드 하이라이팅이 상태색이 아니라 자기 색을 써서, 문자열이 완료로 읽히지 않습니다.
- 사용 한도 리셋이 남은 시간 옆에 날짜와 시로 보입니다. 하루 안이면 24시간제 시각입니다.
- 모든 Operation이 사이드바, War Room, 커맨드 밴드에서 같은 활동 마크를 씁니다. 칩의 알림 개수는 사라졌습니다.
- 패널 캡션이 이름과 창 컨트롤만 남습니다. More가 사이드바 칩 우클릭과 같은 Operation 메뉴를 엽니다.
- War Room 카드에서 중지와 회신을 숨깁니다. 무대에 오른 패널에서 다시 보입니다.

#### Fixed
- Chat Mode 패널을 여러 개 열어도 Console API가 멈추지 않습니다.
- Chat Mode 단계 코멘터리를 이탤릭으로 강제하지 않고 마크다운으로 그립니다.
- 채팅 워크플로 단계 표가 패널 폭과 무관하게 같은 열을 유지하고, 넘치는 값은 말줄임합니다.
- 채팅 워크플로 단계 표에서 게이트웨이 모델을 반복 라우팅 별칭 없이 자기 이름으로 보여 줍니다.
- 빈 원장 구간을 그리지 않아, 긴 에이전트 턴이 빈 여백으로 늘어나지 않습니다.
- 접힌 도구 줄이 아래 빈 줄 없이 자기 문장에 붙어 읽힙니다.
- 채팅 회신 말풍선으로 Quick Launch를 열면 그 Operation을 멘션하고, 쓰다 닫은 초안은 버립니다.
- Quick Launch 프롬프트에 `ultracode`가 있는 동안 apex 외곽선이 계속 돕니다.
- `fleet-console --help`가 항상 `local`이라고 하지 않고 설치된 버전과 채널을 보여 줍니다.

## [1.62.0] - 2026-08-16

### fleet-cli

#### Fixed
- 호출자에게 출력이 보이기 전 발생한 Grok 서버 및 소켓 일시 오류를 재시도합니다.

### fleet-console

#### Added
- 채팅 뷰 안에서 에이전트에게 답합니다. 선택하거나 직접 쓰고, 검토를 요청한 계획도 그 자리에서 받습니다.
- 답을 기다리는 Operation이 사이드바, War Room, 맵에서 입력 대기로 보입니다. 세션은 답하거나 건너뛸 때까지 기다리며 스스로 닫히지 않습니다.
- Chat Mode가 백그라운드 작업을 자기 시계로 추적합니다. 회신 위 줄이 아직 도는 수를 세고, 대화 옆에 작업 면을 엽니다.
- Chat Mode에서 엉뚱한 턴을 중지할 수 있습니다. 닫힌 턴은 실패가 아니라 중지이며, 이미 시작된 백그라운드는 계속 돕니다.
- Chat Mode가 터미널 Operation과 같은 Fleet 지침, 스킬, 게이트웨이 도구 아래에서 돕니다.
- 스트리밍 중에도 읽던 채팅 자리를 지키고, Follow 칩으로 실시간 꼬리로 돌아갑니다.
- 사용 한도에 OpenCode Go가 다시 표시되며, 계정 단위 세션·주간·월간 사용률을 읽습니다.

#### Changed
- Session Analyst 패널에 정체, 상태, 초기화, 대화/아티팩트 전환이 있는 캡션바가 생겨 대화 첫 줄을 가리지 않습니다.
- Session Analyst 컴포저가 모델, 강도, 슬래시 명령 컨트롤을 입력 상자 안에 둡니다.
- 터미널과 Chat Mode를 오갈 때 대화 파일 하나를 이어 써서, 한쪽이 다른 쪽을 덮어쓰지 않습니다.
- Ledger가 Claude Code 모델 사용량을 Anthropic·Gateway 공급자별로 보여 주고, OpenRouter 비용은 정적 추정치이며 Fast 변형은 합치고, 모델 상세는 Today에만 둡니다.
- Codex 리셋 크레딧을 간결한 칩으로 보여주고, 없으면 줄을 감춥니다.
- War Room 카드에서 Analyst·Chat 칩을 숨겨 채팅 카드가 그 자리를 쓰게 합니다. 무대 패널에서는 칩이 돌아옵니다.

#### Fixed
- Settings에서 추가한 게이트웨이 모델이 Session Analyst 목록에 곧바로 나타납니다.
- 아직 도는 작업이 있는 턴에 완료 표시가 찍히지 않습니다. 접힘 줄이 남은 작업 수를 말하고, 중간에 거둔 작업은 완료가 아니라고 합니다.
- 접힌 도구 호출 줄이 마우스를 올리기 전에도 열 수 있어 보입니다.
- 백그라운드 작업이 끝난 뒤의 답은 이전 턴을 덮지 않고 새 턴으로 섭니다.
- 채팅 모드가 주차·최소화·숨김 패널을 스트리밍하지 않아 닫기와 Resume이 막히지 않습니다. War Room 덱 타일은 라이브 보기를 유지합니다.
- 패널이 끝나면 보리 부관이 만세 뒤 유휴로 돌아가며 중간에 멈추지 않습니다.
- 휴대폰 관성 스크롤이 마우스 좌표를 터미널 프롬프트에 넣지 않습니다.
- 잠깐 난 Grok 서버·소켓 오류를 재시도해 턴이 불완전 응답 API 오류로 끝나지 않게 합니다.
- War Room에서 포커스된 덱 패널이 대기로 들어가면 지목 없이 무대로 올라옵니다.
- War Room 빈곳을 누르면 무대 밖 포커스 패널이 풀립니다. Cruise 맵 빈곳과 같습니다.

#### Removed
- 공급자가 하나일 때는 Analyst 공급자 컨트롤을 두지 않습니다. 모델 메뉴가 기본·게이트웨이 모델을 이미 모두 담습니다.

## [1.61.0] - 2026-08-16

### fleet-cli

#### Fixed
- Console 서버가 시작되지 않으면 원인을 말합니다. 브라우저가 안 열리면 성공이 아니라 주소를 건넵니다.

### fleet-console

#### Changed
- 채팅 뷰가 에이전트 작업을 실시간으로 보여 줍니다. 각 스텝에 동사·대상·결말이 있고, 바꾼 파일이 위에 섭니다.
- 한 턴이 의도 다음에 작업으로 읽힙니다. 평범한 스텝은 펼칠 수 있는 집계로 접히고, 실패와 Theater 밖 쓰기는 접히지 않습니다.
- 끝난 턴은 작업 시간 한 줄로 접힙니다. 실패한 스텝은 체크 뒤에 숨지 않고 그 줄에 나옵니다.
- Theater 폴더 밖 쓰기는 원장에서 표식으로 구별됩니다.
- 새로 설치한 Console은 What's New를 띄우지 않습니다. 다음 릴리스부터 알립니다.
- Console을 다시 연 뒤 복원 세션은 유휴가 아니라 같은 Ended 신호와 다시 시작 경로를 씁니다.
- 거절된 이름·액센트·그룹·순서 변경은 저장된 것처럼 보이지 않고 되돌린 뒤 Try again을 줍니다.

#### Fixed
- 실패가 무슨 일인지, 왜인지, 무엇을 하면 되는지 말하며 상태 코드나 기계 이름을 보여 주지 않습니다.
- 한 설정을 저장하는 동안 다른 설정이 막히지 않습니다. 실패한 저장만 되돌아갑니다.
- 스킬 제거, 휴면 Shell 재기동, 원격 호스트 열기가 거절되면 그 사실을 알립니다.
- 터미널 패널과 Skills 목록이 콘솔 언어를 따릅니다. Skills 미리보기와 탭, Theater 행을 스크린 리더로 읽을 수 있습니다.
- 패널을 세우지 못한 플러그인이 그 사실을 알리며, 레일에 빈자리만 남기지 않습니다.
- 휴대폰에서 전체 화면 에이전트를 스크롤해도 CLI에 `NaN`이 입력되지 않습니다.
- Carbon·Maritime·Whites에서 퀘이커 부관 말풍선과 전문 카드를 불투명하게 유지합니다.
- 실행 기록이 없는 복원 Codex 등 Agent CLI 세션이 Claude 마크를 물려받지 않습니다.
- Station Keeping이 본문뿐 아니라 창 캡션도 이웃 패널과 겹치지 않게 합니다.
- 드래그가 끊겨도 Station Keeping이 정착해 캡션이 겹친 채 멈추지 않습니다.
- War Room에서 덱 밀도를 바꾸면 호버 확대를 바로 걷습니다.
- War Room에서 포인터가 패널을 떠나면 호버 확대를 바로 걷습니다.
- 밀도나 열이 바뀌어도 War Room 패널의 캡션과 본문이 자기 칸 안에 머뭅니다.
- War Room 호버 확대는 포인터가 그 패널로 옮겨 왔을 때만 열립니다. 멈춘 커서 밑으로 칸이 들어와도 커지지 않습니다.

### fleet-desktop

#### Fixed
- Desktop이 시작하지 못하면 원인과 로그 위치를 알리며, 창 없이 종료하지 않습니다.

## [1.60.0] - 2026-08-15

### fleet-cli

#### Added
- AI Gateway가 Settings의 Compact timing을 따릅니다. Auto, Early, Late, 또는 Custom 70-99%입니다.

### fleet-console

#### Added
- Settings > AI Gateway에 Compact timing(Auto / Early / Late / Custom)을 두어 게이트웨이 자동 압축 시점을 고릅니다.
- Map 밖 콘솔 크롬이나 Map의 빈 바다를 누르면 활성 패널이 풀립니다.
- Operation 그룹을 패널 캡션에 색 점과 이름으로 표시합니다.
- Quick Launch를 열 때 보고 있는 Operation을 멘션하도록, 고정 옆 옵트인으로 켤 수 있습니다.
- Quick Launch `@`에 근무 중 퀘이커 부관이 올라옵니다. 말풍선으로 답하고, Open the full answer는 대화 카드로 이어집니다.
- Quick Launch `/effort`가 고른 모델이 실제로 내놓는 게이트 단만 이름 붙이고, 없으면 그 행을 생략합니다.
- Quick Launch에서 `/view`로 Operation을 채팅 뷰로 시작할 수 있습니다. 선택은 기억되며 `/view`나 Use terminal view로 되돌립니다.
- War Room 카드 제목 왼쪽에 사이드바와 같은 Operation 마크가 보입니다.

#### Changed
- Session Analyst가 채팅 뷰 문법을 씁니다. 대화 배치, 응답마다 접히는 작업 과정, 인라인 증거 칩, 더 큰 답 글자입니다.
- Session Analyst가 Chat/Artifacts 전환이 있는 한 패널이 됩니다. 발행 브리프는 그 자리에서 열리고, Analyst 칩이 다른 뷰 칩 옆에 섭니다.
- Session Analyst [eN] 칩을 누르면 그 근거를 맥락과 함께 보여 달라는 질문이 채워집니다.
- 채팅 Operation은 말풍선 버튼으로 회신하며 Quick Launch가 그 세션 앞으로 열립니다. 하단 상태 줄은 사라집니다.
- 등록된 Theater가 없으면 우클릭으로 실행 메뉴가 열리지 않습니다.
- 파일 탐색기가 숨겨 오던 목록·필터·팔레트·git 상한을 표시합니다. 폴더 목록 500개 상한을 포함합니다.
- .git, .svn, .hg가 파일 탐색기에 나타나지 않습니다. 숨김 파일을 켠 때는 무엇을 감췄는지 흐린 행으로 남깁니다.
- Ledger가 로컬 사용량을 CLI 행으로 뭉개지 않고 공급자와 모델 이름을 보여 줍니다.
- 프롬프트에 `ultracode`가 있어도 Quick Launch 바에 ULTRACODE 칩을 두지 않습니다. 입력 위 안내는 남습니다.
- 퀘이커 마스코트는 부관입니다. Admiral은 계획하고 위임하는 호스트 에이전트만 가리킵니다.
- Quick Launch `/model`에서 공급자 밴드가 스크롤 중에도 위에 붙고, 모델은 그 아래 한 번씩만 나옵니다.
- Quick Launch `/effort`가 강도 트랙과 같은 톤을 쓰며 MAX와 ULTRACODE도 포함합니다.
- 추론 강도 사다리가 폭 전체를 써 이웃 단이 한눈에 갈립니다.
- 쿼터 카드가 Claude Max 5x/20x, Codex Pro 5x/20x, xAI SuperGrok 플랜 이름을 보여 줍니다.
- 가져올 것이 없는 저장소 동기화는 패널을 밀어내는 알림 대신 Sync 버튼에서 답합니다.
- War Room 덱 칸에 실제 패널이 서서, 셸이 축소 그림이 아니라 그 칸에 맞춰 다시 줄바꿈합니다.
- 보리는 경보 표시만 켜고, 뛰어오르거나 퍼덕이지 않습니다.

#### Fixed
- 채팅 Operation 캡션이 작업 중인지 보여 주고, 복원된 Operation은 유휴가 아니라 휴면으로 읽힙니다.
- 채팅에 포커스해도 로그 전체를 다시 칠하지 않습니다. 포커스 워시는 캡션만 받습니다.
- 채팅 뷰 Operation이 휴면으로 보이지 않습니다. 사이드바가 터미널과 같은 실행·대기·유휴를 쓰고, 칩에 CLI 또는 CHAT 표식이 붙습니다.
- 활동 신호에 닿지 못하면 Console이 배너로 알리고, 모든 Operation을 유휴로 보여 주지 않습니다.
- 패널 크기가 바뀌어도 채팅 로그가 자리를 지킵니다. War Room 무대로 올라갈 때도 그렇습니다.
- 하단 Quick Launch 바가 Ctrl+Space를 Mod+J 옆에 보여 줍니다.
- 트랜스크립트가 없는 채팅 Operation이 끝없이 기다리지 않고 그 사실을 말합니다. 옆 터미널 버튼으로 빠져나갑니다.
- Tactical Grid 빈칸이 캡션을 포함한 점유 창과 같은 크기로 맞습니다.
- War Room 덱이 캔버스 바닥까지 내려옵니다.

#### Removed
- 패널 왼쪽 액센트 띠를 걷습니다. 정체성은 캡션 마크만으로 말합니다.

## [1.59.0] - 2026-08-15

### fleet-cli

#### Added
- AI Gateway에서 Cursor Opus 5와 Fable 5를 제공합니다. Cursor API 풀로 청구되는 Max Mode 1M 변형을 포함합니다.

#### Fixed
- Cursor 모델이 실패한 기본 읽기 대신 호출자 Read 도구로 파일을 나눠 읽습니다.

### fleet-console

#### Added
- AI Gateway 모델 선택에 Cursor Opus 5와 Fable 5를 올립니다. Cursor API 풀로 청구되는 Max Mode 1M 변형을 포함합니다.
- Claude Gateway Operation을 같은 세션의 채팅 뷰로 전환할 수 있습니다. Claude Agent SDK로 이어지고, 회신은 Quick Launch로만 들어가며, 터미널은 기록을 유지한 채 다시 열립니다.
- Quick Launch 슬래시 명령으로 키보드에서 Theater·모델·추론 강도·하단 고정을 바꿉니다.
- macOS를 포함해 모든 플랫폼에서 Ctrl+Space로 Quick Launch를 토글합니다. Mod+J는 그대로입니다.
- Quick Launch에서 붙여넣기·드롭·첨부 버튼으로 이미지를 붙입니다. 새 실행과 `@` 멘션에 실리고, 에이전트가 메시지와 함께 읽습니다.
- Quick Launch가 프롬프트의 `ultracode`를 알아보고 파선 `ULTRACODE` 칩을 보여 줍니다. 표식을 숨겨도 프롬프트는 입력한 그대로 전달됩니다.

#### Changed
- 채팅 뷰가 터미널 패널과 같은 면을 씁니다. 터미널로 돌아가기는 우상단 칩이며 채팅 전환과 같습니다.
- Cursor 검색이 호출자 승인 셸 도구를 쓸 때 반복 명령·결과 분량을 줄입니다. 권한 검사는 유지됩니다.
- Operation 패널이 테마마다 한 면·한 색이 되어 겹친 시트처럼 보이지 않습니다.
- Operation 상태가 창 캡션에 읽히며 배경 작업도 드러납니다. 아웃라인은 중립 테두리로 남습니다.
- Operation 패널 본문 위에 창 캡션이 생깁니다. 캡션에서 드래그하고, 이름 변경은 더블클릭입니다.

#### Fixed
- 게이트웨이 Operation의 Cursor 모델이 실패한 기본 읽기 대신 Read로 파일을 나눠 읽습니다.
- Tactical에서 패널을 최대화해도 모드 경계가 그 위에 남지 않습니다.
- 레일 Shell 패널이 안의 터미널과 같은 면에 앉습니다.
- 무대에 올린 War Room 패널이 Tactical과 같은 인셋까지 캔버스를 채웁니다.
- 패널 크기가 바뀔 때 터미널이 빈 화면으로 번쩍이지 않습니다.
- 사이드바가 멈추는 즉시 터미널이 맞춰지며, 잘린 채로 남지 않습니다.
- 문자 격자가 그대로인 리사이즈에서 셸에 전체 화면을 다시 그리게 하지 않습니다.

#### Removed
- 사용 한도에 OpenCode Go 카드가 없습니다. OpenCode는 지원되는 쿼터 API가 없고, 모델 발사는 그대로입니다.

## [1.58.1] - 2026-08-14

### fleet-cli

#### Changed
- 도구 검색 루프에서 반복되는 Grok 4.6 도구 스키마 전송을 줄입니다. 선택된 도구와 이어서 참조된 도구는 유지됩니다.

### fleet-console

#### Changed
- 도구 검색을 쓰는 게이트웨이 Operation에서 반복되는 Grok 4.6 도구 스키마 전송을 줄입니다. 선택된 도구와 이어서 참조된 도구는 유지됩니다.

## [1.58.0] - 2026-08-13

### fleet-cli

#### Added
- 공식 Grok CLI 구독으로 Grok 4.6을 추가합니다. API 키나 Fleet 관리 OAuth 없이 로컬 로그인을 쓰고, 불완전한 도구 호출은 실행 중처럼 보이기 전에 거절합니다.

#### Changed
- Claude와 Kimi가 아닌 게이트웨이 모델에는 Claude Code의 Web Search 도구를 넘기지 않습니다.

### fleet-console

#### Added
- 실행·설정·할당량 화면에 Grok 4.6 게이트웨이 라우팅, 주간 구독 할당량, 공식 Grok 마크를 추가합니다.
- Ledger가 기간 비용을 Console 귀속 Operation과 다른 로컬 세션으로 나누고, 기기 전체 합계를 같은 블록에 보여 줍니다.
- 기간에 매칭된 사용량이 없는 Operation도 Ledger에 흐린 행으로 남고, 최근 활동/비용 높은 순 정렬이 있습니다.
- Ledger 일별 트렌드가 제곱근 스케일로 작은 날도 읽히게 하고, 막대 안에 Console 귀속 비용을 표시합니다.
- 모바일 세션 제목에서 데스크톱과 같은 두 번 탭 무장과 실행 취소로 Operation을 닫습니다.
- Quick Launch를 하단에 고정해 작업을 보면서 지시를 씁니다. 시선을 떼면 초안 한 줄로 물러나고, 포커스나 단축키로 돌아옵니다.
- 사용 한도 공급자 카드를 드래그로 재배치하며 화살표 키로도 움직입니다. 순서는 유지됩니다.

#### Changed
- 공급자 글리프를 Command Band Operation 이름 왼쪽으로 옮기고 모델 칩을 뗍니다. 실행 중 모델은 전환 목록에 남습니다.
- Codex 작업공간 에이전트 메뉴의 강도 트랙이 모델 행 오른쪽으로 먼저 열립니다.
- Claude와 Kimi가 아닌 게이트웨이 모델에는 Claude Code의 Web Search 도구를 넘기지 않습니다.
- Quick Launch 선택 메뉴가 표준 키를 따릅니다. 방향키, Home/End, 글자 점프, Enter로 선택, Escape로 칩에 복귀합니다.
- 사용 한도가 점 세 개 대신 사용량을 읽는 중이라고 말합니다.
- Windows에서 프롬프트에 cmd.exe 특수문자(" & < > ( ) @ ^ | %)가 있거나 명령줄이 넘치면(8,191 / 32,767자) Quick Launch가 임시 파일에 쓰고 세션에 그 파일을 읽으라고 합니다. Trust folder와 업데이트 대화상자는 그 전에 끝나고, Operation 이름은 원문 프롬프트에서 짓습니다.

#### Fixed
- Theater·모델 칩 포커스 링이 온전한 링으로 그려집니다.
- Escape로 Quick Launch를 닫아도 초안이 남고, 다시 열면 복원됩니다.

#### Removed
- War Room 하단 대기 레일을 제거합니다. 대기 순서는 사이드바가 이미 쥐고 있습니다. 치워두기와 뒤로 미루기는 그대로입니다.

## [1.57.2] - 2026-08-13

### fleet-cli

#### Added
- Grok 4.5를 유지한 채 Cursor Grok 4.6과 Grok 4.6 Fast를 Low, Medium, High, Extra High 추론 강도로 추가합니다.

### fleet-console

#### Added
- AI Gateway 모델 선택에 Cursor Grok 4.6과 Grok 4.6 Fast를 CursorBench 3.2 품질 근거와 함께 올립니다.

## [1.57.1] - 2026-08-13

### fleet-console

#### Fixed
- 휴대폰에서 Quick Launch가 화면 안에 남습니다. 긴 Theater 이름은 잘리고, Theater·모델·강도는 각자 줄로 내려갑니다.

## [1.57.0] - 2026-08-12

### fleet-console

#### Added
- 터치 터미널에 소프트 키보드에 없는 키 막대를 둡니다. Escape, Tab, 방향키, 걸어 두는 Ctrl·Alt와, 한 번 더 누르면 나오는 키들입니다.
- Quick Launch에서 @로 실행 중 Operation을 멘션하면 그 터미널로 프롬프트가 갑니다. 휴면이면 먼저 재개합니다.

#### Changed
- 커맨드 밴드와 Operation 메뉴가 실제로 도는 모델 이름을 답니다. GPT-5.6-Sol-Fast나 Opus처럼요.
- Cowork 에이전트 설정이 행마다 강도 손잡이가 있는 모델 메뉴 하나입니다. 문서 코워크에는 low/medium/high만 제공합니다.
- Cowork 모델은 opus[1m], sonnet, haiku입니다. 문서 코워크에 fable은 더 이상 없고, 지시 입력은 브라우저 자동완성을 띄우지 않습니다.
- 우측 레일 패널 헤더는 호버·포커스 때 오버레이로 나타나 높이를 본문에 돌려줍니다. 헤더에서 Escape로 패널을 닫습니다.
- 콘솔은 원격 접속을 하나만 유지하고 마지막에 들어온 기기에게 자리를 줍니다. 이전 기기는 다른 기기가 접속했다고 듣고 페어링은 유지되며, 다시 접속하면 자리를 되찾습니다.

#### Fixed
- 터미널에서 답하면 Agent Operation이 바로 실행 중으로 돌아옵니다. 턴이 끝날 때까지 대기로 남지 않습니다.
- 턴이 끝난 뒤 남은 서브에이전트·워크플로우 작업은 실행 중이 아니라 백그라운드로 표시됩니다.
- 폰에서 터미널이 화면 전체 높이로 표시됩니다.

## [1.56.0] - 2026-08-12

### fleet-console

#### Added
- 휴대폰 Theater 탭에서 Theater를 전환하며, Operation 수와 대기 건수를 보여 줍니다.
- 새 액세스 링크를 QR 코드로 띄우고, 남은 시간과 페어링 순간을 같은 창에 보여 줍니다.

#### Changed
- 휴대폰 Settings가 섹션 목록으로 열립니다. 행을 누르면 전체 화면이 되고, 각 행이 현재 값을 보여 줍니다.
- 휴대폰 Settings 컨트롤이 손가락 크기이고, 테마 선택지는 한 열입니다.
- 모바일 Operation 목록 위에 활성 Theater 이름을 표시합니다.

#### Fixed
- 모바일 레이아웃이 켜지는 폭에서는 데스크톱 Settings 행도 세로로 쌓입니다.

## [1.55.0] - 2026-08-11

### fleet-console

#### Added
- Remote access는 기본적으로 로컬 네트워크에 엽니다. 공개 호스트 이름과 NAT 경로는 명시적으로 켜고 확인해야 합니다.
- 원격 엔드포인트는 초안으로 고칩니다. Start listening, Save for later, Apply changes, Stop listening만 저장하며, 인터페이스를 고르는 것만으로 실행 중 리스너를 건드리지 않습니다.
- 적용 전에 대가를 알립니다. 리스너 재시작은 페어링을 유지하지만, 신뢰 주소가 바뀌면 세션이 끊기고 미사용 링크가 취소되며 모든 기기 페어링이 해제됩니다.
- 필요한 값이 모두 유효할 때만 연결 경로를 보여 줍니다.
- 라우터 칸을 외부 포트·내부 IP 주소·내부 포트로 제시하고, 두 포트가 다를 때 경고합니다.
- 세션 없이 원격 리스너가 응답하는 것은 페어링뿐입니다. 인증서 지문을 확인하는 Fleet Console 앱에서만 이루어지고, 브라우저 안내 페이지는 없습니다.
- 공개 엔드포인트 페어링에 실패 예산을 둡니다. 거절은 Settings에 보이고, 성공하면 그 출처 예산이 지워집니다.
- Remote access 설정은 소유자 기기에만 있습니다. 다른 기기에서 접속하면 보이지 않고, 닿을 수 없는 공개 호스트 이름은 거부됩니다.

#### Changed
- Claude 실행 그룹을 Claude built-in 대신 Claude로 표시합니다.
- MAX와 ULTRACODE가 각자 모션을 갖습니다. 감속 모션 환경에서는 정적입니다.
- 실행 메뉴가 기본 스크롤바 대신 방향 스트립을 씁니다. 휠·방향키는 그대로이고, 모션 축소에서는 클릭 이동입니다.

#### Fixed
- Backend API 설정이 Console Operations와 로드된 플러그인 HTTP, SSE, WebSocket, proxy 라우트를 Core와 플러그인별 그룹으로 묶습니다.
- apex 강도 게이트를 열어도 기존 스톱이 밀리지 않습니다.
- 휴면 Agent 패널은 재개 때 Claude Code 모델과 강도를 유지합니다. 저장 설정이 없는 기존 패널은 Opus 1M이 기본입니다.

## [1.54.1] - 2026-08-11

### fleet-cli

#### Changed
- 실제 창이 1M 이상인 게이트웨이 모델만 `[1m]`으로 표시합니다. 혼합 모델 세션은 각 모델 실제 창을 16K 남기고 압축합니다.

### fleet-console

#### Changed
- 실제 창이 1M 이상인 게이트웨이 모델만 `[1m]`으로 표시합니다. 혼합 모델 세션은 각 모델 실제 창을 16K 남기고 압축합니다.

## [1.54.0] - 2026-08-10

### fleet-cli

#### Removed
- 지원하지 않는 Cursor AI Gateway 모델을 디스커버리에서 뺍니다. Cursor는 Auto, Composer 2.5, Grok 4.5(fast 변형 포함)만 남기고, Claude·GPT·Kimi는 Cursor 아래에 없습니다.
- 안정 Console 릴리스에서 `@dotobokuri/fleet-cli` 이전 패키지 게시를 중단합니다. `@dotobokuri/fleet-console`을 설치하세요.

### fleet-console

#### Added
- 강도 트랙이 XHIGH에서 닫힙니다. MAX와 ULTRACODE는 모델이 실제로 내놓을 때만 apex 확장 뒤에 있습니다.
- ULTRACODE 실행은 Claude Code `--effort ultracode`를 전달합니다. xhigh 강도와 상시 멀티에이전트 오케스트레이션입니다.
- Kimi K3 1M 옆에 Cursor Max Mode 1M 변형 Opus 5·Fable 5를 제공합니다.

#### Changed
- Operation 실행 메뉴 모델 행을 간결히 하고 Shell을 마지막 Etc 그룹으로 묶습니다.
- 기존 Fable 표시 이름을 유지한 채 Claude Code 1M 컨텍스트 좌표로 Fable을 실행합니다.
- 각 Agent Operation에 실행한 공급자를 새겨, 사이드바·커맨드 밴드·팔레트가 획일 Claude 마크 대신 그 글리프를 보여 줍니다.
- 사용 한도 미터가 AI Gateway 로스터와 같은 위험 판정을 써서, 막대가 가득 차기를 기다리지 않습니다. 도움말이 채움·눈금·빗금을 설명합니다.
- 저장소 그래프 배지가 브랜치를 태그보다 앞에 두고 원격 추적을 브랜치 표식에 합치며, 패널을 줄여도 잘리지 않습니다.

#### Removed
- 설정과 실행 선택기에서 지원하지 않는 Cursor AI Gateway 모델을 뺍니다. Cursor는 Auto, Composer 2.5, Grok 4.5(fast 변형 포함)만 남깁니다.
- 세션 기본을 정하던 AI Gateway 모델 별표를 제거합니다. 새 게이트웨이 세션은 Claude Code 자체 모델 선택을 유지합니다.

## [1.53.0] - 2026-08-09

### fleet-cli

#### Fixed
- AI Gateway API 키 검증 시간을 늘려, 느린 OpenCode Go 응답이 타임아웃으로 키 저장을 막지 않습니다.

### fleet-console

#### Added
- 캔버스 실행에서 AUTO가 아닌 강도를 고르면 노브를 다시 누르라는 팁이 뜹니다. Help > Show the screen guide로 되돌립니다.
- Settings > Remote access가 최신 Fleet Desktop과 GitHub releases 링크를 안내합니다.

#### Changed
- 캔버스나 사이드바 빈 자리 우클릭이 모델 목록을 바로 엽니다. Shell이 첫 행이고 공급자 밴드가 같은 메뉴에 이어집니다.

#### Fixed
- AI Gateway API 키 검증 시간을 늘려, 느린 OpenCode Go 응답이 타임아웃으로 키 저장을 막지 않습니다.
- 이 컴퓨터의 다른 콘솔(WSL 포함)로 건너가도 돌아갈 길이 사라지지 않습니다. 호스트 박스가 지금 서 있는 콘솔 이름을 말합니다.

### fleet-desktop

#### Fixed
- Windows Desktop 앱에서 액세스 링크 복사가 되며, 클립보드 권한은 활성 Console origin으로 제한됩니다.

## [1.52.0] - 2026-08-09

### fleet-cli

#### Added
- 게이트웨이 카탈로그와 `gateway_models` 로스터에 CursorBench 점수를 기록합니다. 판단 작업은 측정 점수 우선입니다.
- 옵트인 `providerPriority`를 추가합니다. 실제 실패가 관측되기 전에는 지정한 공급자 할당량을 먼저 씁니다.

#### Changed
- Codex·OpenCode Go의 Claude Code 세션이 보이는 턴 뒤 짧은 제안에 전체 도구 카탈로그를 다시 보내지 않습니다. 일반 턴 도구는 그대로입니다.
- AI Gateway Cursor 세션이 도구 지침을 상시 Cursor 규칙으로 보내, 도구를 고르는 시점에 닿게 합니다.
- 게이트웨이 모델 정체성이 Fleet 플러그인 스코프 `fleet:<name>`을 쓰며 `gateway_models`와 같습니다.
- `@dotobokuri/fleet-console`에서 `fleet`를 게시하며 `fleet cli`, `fleet console`, Claude Code 패스스루를 지원합니다.
- `@dotobokuri/fleet-console`만 업데이트하며, 로컬 Console이 있으면 먼저 중지합니다.
- 기존 설치가 `@dotobokuri/fleet-console`과 맞춰지도록 같은 버전의 `@dotobokuri/fleet-cli` 이전 패키지를 게시합니다.

#### Fixed
- AI Gateway Cursor 세션이 도구 호출 뒤 3분씩 멈추거나 다음 턴에 대화 전체를 다시 보내지 않습니다. Cursor Auto, Composer, Grok도 같은 경로입니다.
- AI Gateway Cursor 세션이 Cursor 내장 읽기·검색·셸에서 멈추지 않습니다. 세션 도구로 넘기며 권한 검사를 유지하고, 표현할 수 없는 내장은 계속 거부합니다.
- Cursor 턴 실패 이유를 빈 응답 대신 보고합니다. 로그인 만료, 사용량 제한, Cursor 메시지가 그대로 전달됩니다.
- AI Gateway OpenCode Go 세션이 선언되지 않은 도구 인자 때문에 실패하지 않습니다. 여분 키는 제거됩니다. 백엔드는 `strict: true`를 받고도 무시합니다.
- 활성 모델이 많아도 Windows 실행이 깨지지 않습니다. 정체성 정의가 명령줄에서 플러그인 파일로 옮겨졌습니다.
- AI Gateway 선별에서 모델을 끄면 서빙과 청구도 멈춥니다.
- Windows에서 인자가 Claude Code 명령줄에 안 들어가면 크기와 상한을 밝히며 거부합니다.

#### Removed
- OpenCode Go 구세대(MiniMax M2.x, Qwen 3.5~3.7, GLM 5·5.1, Kimi K2.5~K2.7)를 빼고 각 라인업의 현행 세대만 남깁니다.

#### Breaking Changes
- `FLEET_AGENT_CLI=claude`를 폐지된 Classic 대신 게이트웨이 실행으로 해석해, 옛 환경도 기동합니다.
- `@dotobokuri/fleet-cli`는 더 이상 `fleet` 명령을 제공하지 않습니다. 기존 설치에서는 `fleet update`를 쓰거나 `@dotobokuri/fleet-console`을 설치하세요. 그 패키지를 npm으로 직접 설치하면 PATH에 `fleet`가 없습니다.

### fleet-console

#### Added
- 게이트웨이 카탈로그에 CursorBench 점수를 기록합니다. Console에서 띄운 세션은 판단 작업을 측정 점수 우선으로 배정합니다.
- AI Gateway 설정에서 옵트인 공급자 소진 순서를 정합니다. 할당량에만 작용하고 모델 품질에는 영향이 없습니다.
- 저장소 기록이 커밋 작성자를 보여 주고, 본문이 있는 커밋을 표시하며, 계보순/시간순을 고를 수 있습니다. 기본은 계보순입니다.
- AI Gateway 설정에서 모델 이름 옆에 등급 `flagship`, `standard`, `light`를 읽습니다.
- 라우팅 별칭은 빈칸 대신 `unclassed`입니다. 호출마다 서빙 모델이 달라서입니다.
- AI Gateway 모델을 host-only로 표시할 수 있습니다. Claude Code `/model`과 실행 목록에는 남고, `gateway_models`와 위임 정체성에서는 빠집니다.
- 캔버스 메뉴에서 고른 Claude 별칭이나 켠 Gateway 모델로 Claude (Gateway) Operation을 시작하고, 실행 전에 추론 강도를 정합니다.
- 추론 강도는 목록이 아니라 트랙입니다. 없는 단도 자리를 지키고, 맨 앞은 모델 기본값이라 최저 단처럼 보이지 않습니다.
- 캔버스 실행 메뉴에서 트랙은 강도만 정하고 실행은 모델 행이 맡습니다. 트랙은 행 오른쪽 손잡이에서 엽니다.
- Opus를 고르면 Claude Code `opus[1m]`으로 실행됩니다. 표시는 `Opus`이고, 남은 `opus` 선택은 `opus[1m]`으로 바뀝니다.
- Terminal 설정에서 새 Claude AI Gateway 세션이 Fleet 규칙을 덧붙이거나 대체하거나, Fleet harness 도구는 유지한 채 Claude Code 자체 프롬프트를 쓰게 할 수 있습니다.
- 어디서든 Mod+J로 작업을 시작합니다. 할 일을 적고 Theater와 모델을 고른 뒤 Enter입니다. 터미널 포커스 중에도 단축키가 먹습니다.
- Quick Launch 컴포저 안에서 모델과 강도를 고릅니다. 기억된 강도를 못 내는 모델은 그 값을 비웁니다. 처음 Quick Launch는 Opus로 시작합니다.
- 화면의 Theater뿐 아니라 어느 Theater로든 실행합니다. 마지막 Theater·모델·강도는 실행하지 않고 닫아도 다시 쓰입니다.
- Windows에서 프롬프트가 에이전트 명령줄에 안 들어가면 줄일 글자 수와 함께 실행 전에 거부합니다. 컴포저는 그대로 남습니다.
- 원격 접속은 TLS로 Console을 열고 `fleet://join?code=...` 링크를 발급합니다. 링크는 주소·1회용 자격·지문·이름을 인코딩한 것이며 암호화가 아니므로 비밀로 다루세요. full 링크는 명령을 실행할 수 있다고 경고하고, 감시 전용은 그러지 않습니다. 원격 접속을 끄면 리스너가 닫히고 원격 세션이 끝납니다.
- 액세스 링크로 연 기기는 링크를 쓴 뒤에도 페어링이 남습니다. 연결 끊기는 페어링을 남기고, 삭제는 회수합니다. 새 인증서이거나 약 1년 비우면 새 링크가 필요합니다.
- Settings가 닿을 수 있는 콘솔을 보관하고, 커맨드 밴드 호스트 칩으로 전환합니다. 액세스 링크를 붙여넣으면 저장 전에 인증서를 확인합니다. Windows WSL을 포함한 로컬 콘솔은 링크가 필요 없습니다. 원격 콘솔은 다른 기계 주소를 받지 않습니다.
- 제어 기기는 한 번에 하나입니다. 다른 기기가 가져가면 안내가 이름을 밝히고 회수를 제안합니다. 터미널은 읽기 전용으로 남습니다. 접속 중 두 번째 full 링크는 거절되며 나중에 다시 쓸 수 있습니다.
- 원격 접속은 닿는 곳마다 실험으로 표시되고, 처음 쓸 때 스스로를 소개합니다. 링크와 페어링 동작은 릴리스마다 달라질 수 있습니다.
- Cruise에 옵트인 Station Keeping이 생깁니다. 겹친 패널을 펼친 뒤 실행·드래그·리사이즈·복원이 겹치지 않게 유지합니다.
- Settings > Terminal > General에서 선택하지 않은 터미널을 얼마나 자주 그릴지 Balanced, Instant, Saving 중 고릅니다. 선택한 패널은 항상 제 속도입니다.
- War Room Watch Deck 카드에 최소화·닫기가 늘 보입니다. 최소화는 Operation을 덱에서 내립니다. 닫기는 첫 누름이 무장입니다.
- 최소화한 Operation이 War Room에서 휴면 위 선반을 갖습니다. Alt+S 상태 보기도 최소화와 휴면을 같이 구분합니다.

#### Changed
- Codex·OpenCode Go의 Claude (Gateway) Operation이 보이는 턴 뒤 짧은 제안에 전체 도구 카탈로그를 다시 보내지 않습니다.
- Cursor 모델의 Claude (Gateway) Operation이 도구 지침을 상시 Cursor 규칙으로 보냅니다.
- Session Analyst가 탐지된 Claude Code CLI가 아니라 Console AI Gateway에서 돕니다. 기본은 `Opus [1M]`·`xhigh` 대신 `Sonnet`·`low`입니다. 파일·셸 도구는 없습니다.
- Scuttlebutt의 토리·보리·도리가 같은 AI Gateway로 답해, Claude Code CLI 설치 없이 대화합니다.
- 위키 Cowork가 Agent CLI를 묻지 않고 Console AI Gateway에서 돕니다. 기본은 `Sonnet`·`low`입니다. 끝나지 않고 멈춘 턴은 오류로 알립니다.
- 캔버스 컨텍스트 메뉴가 절반 가까이 낮아집니다. Claude 종류는 이름 옆에 짧은 대비를 두고, 한 줄 설명은 호버·포커스 때 엽니다.
- 새 소식이 런타임별로 묶입니다. Fleet CLI, Fleet Console, Fleet Desktop입니다. v1.51.0까지는 `fleet-plugin`·`fleet-core` 내부 항목을 더 이상 나열하지 않습니다.
- 저장소 커밋 그래프를 행 단위로 그려 본문이 실제 레인 옆에 붙습니다. 브랜치·태그·원격 뱃지가 더 잘 읽힙니다.
- 전체 화면에서 커맨드 밴드를 계속 보이게 하면 작업 위에 뜨지 않고 자기 자리를 갖습니다.
- 전체 화면 커맨드 밴드는 맨 위 몇 픽셀뿐 아니라 상단 근처에서 포인터가 위로 움직일 때도 내려옵니다.
- 전체 화면 커맨드 밴드 표시가 기억됩니다. 밴드가 숨어도 명령 팔레트에서 켜고 끌 수 있습니다.
- 게이트웨이 모델 정체성이 Fleet 플러그인 스코프를 씁니다. Agent Operation은 `fleet:<name>`으로 고르며 `gateway_models`와 같습니다.
- AI Gateway 설정에서 모델 추론 강도를 `effort` 배지에서 읽고 고칩니다.
- 한 패키지에서 `fleet`와 과도기 `fleet-console` bin을 모두 소유합니다.
- 도움말에서는 `fleet console`를 우선하고 과도기 `fleet-console`도 유지합니다.
- 로컬·원격을 오가거나 새로고침해도 Fleet Console이 캔버스 모드와 펼친 패널을 복원합니다. 새 탭은 여전히 Cruise로 시작합니다.
- 화면 안내 다시 보기가 온보딩 전체를 처음부터 다시 재생합니다.
- 라이트·다크 전환이 터미널마다 힌트를 띄우지 않고 닫을 수 있는 안내 하나를 보여 줍니다. CLI 재시작이나 `/theme` 제안은 남습니다.

#### Fixed
- Cursor 모델 Claude (Gateway) Operation이 도구 호출 뒤 3분씩 멈추거나 다음 턴에 대화 전체를 다시 보내지 않습니다. Cursor Auto, Composer, Grok도 같은 경로입니다.
- Cursor 모델 Claude (Gateway) Operation이 Cursor 내장 읽기·검색·셸에서 멈추지 않습니다. 표현할 수 없는 내장은 계속 거부합니다.
- Cursor 턴 실패 이유를 빈 응답 대신 보고합니다.
- OpenCode Go Claude (Gateway) Operation이 선언되지 않은 도구 인자 때문에 실패하지 않습니다. 여분 키는 제거됩니다. 백엔드는 `strict: true`를 받고도 무시합니다.
- 워크플로우를 백그라운드에 남긴 Claude Operation은 그 워크플로우가 끝날 때까지 백그라운드로 남고, 아무것도 안 돌면 `AWAITING`으로 돌아옵니다.
- 캔버스 컨텍스트 메뉴에서 방향키가 다시 동작하고, 스크린 리더에 메뉴로 알립니다.
- 캔버스 아래쪽에서 제어 메뉴를 열어도 판이 위로 밀리지 않습니다.
- Tactical에서 캔버스 우클릭이 실행 종류를 모두 회색으로 만들지 않고 그대로 제공합니다.
- 반복된 섹션 제목 때문에 빠지던 릴리스 노트를 보여 줍니다. v1.3.0 `Fixed` 중복을 포함합니다. 모르는 제목 아래 항목도 보존합니다.
- 설치된 Console에서 TypeScript 외부 플러그인을 건너뛰지 않고 불러옵니다.
- 커밋 메시지가 커 로그 버퍼를 채우는 저장소에서도 나머지 기록에 닿습니다.
- 커맨드 밴드 토글이 켜져 있으면 눌린 모습으로 보입니다.
- 게이트웨이 모델을 많이 켜 두어도 Windows에서 Claude (Gateway) Operation이 시작에 실패하지 않습니다.
- AI Gateway 로스터에서 모델을 끄면 서빙과 청구도 멈춥니다.
- Settings의 분할 컨트롤이 열 전체로 늘어나지 않습니다.
- Carbon·Maritime·Whites에서 화면 안내 카드를 불투명하게 유지합니다.
- 변경 파일을 열어도 저장소 파일 목록이 있던 자리에 남고, diff는 목록 오른쪽에 엽니다.
- 전체 화면 agent CLI가 돌아도 터미널에서 드래그 복사가 됩니다. 프로그램이 요청한 클립보드 쓰기는 반영하고, 읽기는 계속 거부합니다.
- Watch Deck 프리뷰가 카드를 채우고 카드와 함께 커집니다. 확대창은 축소판을 키우지 않고 읽을 수 있는 크기로 엽니다.

#### Removed
- AI Gateway 로스터에서 OpenCode Go 구세대를 빼고 각 라인업의 현행 세대만 남깁니다.

#### Breaking Changes
- Claude (Classic)과 Claude (Native)를 폐지합니다. 새 Operation은 Claude (Gateway)이고, 기존 Classic·Native는 첫 기동 때 이주합니다. 그 전에 `state.json.classic-backup`을 한 번 보관합니다.
- Classic이 데리고 있던 Carrier 표면을 제거합니다. Carrier Streams, Carrier 설정, Carrier 딥링크입니다. 세션 상태·주의·제목은 그대로입니다.
- Metaphor 프롬프트 설정을 제거합니다. Classic 어조에만 쓰였고, 다음 저장 때 보관 설정에서도 빠집니다.

### fleet-desktop

#### Added
- Fleet Desktop이 `fleet://` 액세스 링크를 실행 중 Console에 넘깁니다. 자체 대화상자는 없습니다.
- Fleet Desktop에서 원격 콘솔을 고르면 연결 전에 저장 지문을 확인합니다. 인증서가 다르면 열리지 않습니다. 기기를 삭제했거나 인증서가 바뀌었거나 약 1년이 지나기 전에는 새 링크 없이 다시 엽니다.

#### Removed
- SSH 원격 런타임을 제거합니다. 그 Console은 Settings에서 액세스 링크로 추가하세요. 관리형 포함 로컬 접속은 그대로입니다.
- Connect to Runtime 메뉴와 트레이 항목을 제거합니다. 어떤 Console을 열지는 Console 안에서 고릅니다.

## [1.51.0] - 2026-08-06

### fleet-cli

#### Added
- [fleet-cli] 얇은 런처가 Fleet Console과 같은 AI Gateway 선별(`~/.fleet/ai-gateway.json`)을 읽고, 실제 공급자 할당량과 함께 `gateway_models`를 제공합니다.

#### Breaking Changes
- [fleet-cli] `fleet`가 내장 두 칸 터미널 대신 AI Gateway 주입(`--mcp-config`)과 함께 Claude Code를 바로 실행합니다. 캐리어 표면과 `--disable-cursor-sync`는 제거됩니다. `fleet auth`와 `fleet update`는 남고, 모르는 인자는 Claude Code로 전달됩니다.

### fleet-console

#### Added
- [fleet-console] Claude (Classic)이 폐지된다는 1회 안내를 보여 줍니다. 기존 Classic Operation은 계속 동작하고, 새 것은 AI Gateway를 쓰라고 합니다.

#### Changed
- [fleet-console] War Room 라이브 프리뷰를 에이전트 CLI 입력 위까지 잘라 Watch Deck 카드에 스트리밍 출력이 더 보이게 합니다.
- [fleet-console] Claude Gateway를 정식으로 올립니다. 메뉴 표기가 `Claude (Gateway)`이고 투어에서 실험 표기가 빠집니다.

#### Fixed
- [fleet-console] 같은 프로세스에서 Console 서버를 다시 시작할 때 플러그인 라우트 번들을 재사용합니다.
- [fleet-console] War Room에서도 사이드바 폭을 조절합니다. 모든 모드가 같은 가장자리 핸들을 씁니다.
- [fleet-console] War Room 빈곳(사이드바, 덱, Theater 밴드 사이, 큐 레일)을 우클릭하면 지도와 같은 실행 메뉴가 열립니다.
- [fleet-console] Operations 밖에서도 커맨드 밴드에 사이드바와 Activity Rail 토글을 두고, 누르면 Operations로 돌아가 그 패널을 엽니다.
- [fleet-console] 두 패널이 없는 화면에서 사이드바·Activity Rail 단축키가 저장된 상태를 바꾸지 않습니다.
- [fleet-console] Console 데몬과 업데이트 워커가 OS 인증서 저장소를 기본 신뢰합니다(`NODE_USE_SYSTEM_CA=1`). `FLEET_CONSOLE_NO_SYSTEM_CA=1`로 끌 수 있습니다.
- [fleet-console] 사용 한도의 OpenCode Go 카드가 Fleet Desktop에서 기록 없음 대신 세션·주간·월간 미터를 보여 줍니다.

#### Removed
- [fleet-console] Settings > General에서 Reduce panel motion을 제거합니다. 패널 모션은 OS 동작 줄이기만 따릅니다.

### fleet-desktop

#### Fixed
- [fleet-console] Desktop이 관리하는 Node 프로세스가 OS 인증서 저장소를 기본 신뢰해, TLS 검사 프록시에서도 Codex 사용량과 게이트웨이 호출이 동작합니다. `FLEET_CONSOLE_NO_SYSTEM_CA=1`로 끕니다.

### fleet-plugin

#### Added
- [fleet-console] AI Gateway 설정에 기본 꺼진 와이어 로그 토글을 추가합니다. Console 데이터 디렉터리에 기록하고(16 MiB 순환, 백업 하나) `FLEET_GATEWAY_WIRE_LOG`보다 우선합니다.
- [fleet-console] 저장소 커밋·비교 검사기에서 파일 목록과 diff 사이 폭을 끌어 조절합니다. 비교는 목록/트리 전환이 되고, 좁으면 세로로 쌓입니다.

#### Changed
- [fleet-console] AI Gateway 모델 선별을 Fleet 단일 파일 `~/.fleet/ai-gateway.json`에 저장합니다. 기존 선별은 이어받고, 데이터 루트를 명시한 Console은 자기 것을 갖습니다.
- [fleet-console] 모델 디스커버리 캐시를 쓰지 못하면 Claude (Gateway) 실행을 거부하며, 낡은 목록으로 시작하지 않습니다.

#### Fixed
- [fleet-console] 실행 중 도구가 상태를 안 보내면 캐리어 활동 카드의 상태 줄을 감추고 `{status}`를 남기지 않습니다.
- [fleet-console] 쿼터 패널의 TLS 인증서 실패를 Certificate verification failed로 표시하고, AI 게이트웨이 502에 전송 원인을 남깁니다.
- [fleet-console] 저장소 기록과 검사기 헤더에서 긴 커밋 제목을 한 줄로 두고, 전문은 툴팁입니다.
- [fleet-console] 검사기 파일 목록이 diff 위로 넘치지 않습니다. 긴 경로는 목록 안에서 스크롤됩니다.

### fleet-core

#### Added
- [core-ai-gateway] AI Gateway에서 Cursor GPT-5.6 Sol을 제공합니다. Cursor API 풀로 청구되고, Cursor의 low~max 추론 사다리를 씁니다.
- [core-ai-gateway] `FLEET_GATEWAY_WIRE_LOG`보다 우선하는 프로세스 안 와이어 로그를 추가해, 재시작 없이 진단 로그를 켜고 끌 수 있습니다.
- [core-ai-gateway] 게이트웨이 모델 등급(`flagship`/`standard`/`light`)을 카탈로그와 `gateway_models`에 기록합니다. 라우팅 별칭은 무등급입니다.

#### Changed
- [fleet-admiral] 게이트웨이 워크플로 단계를 판단/기계 역할로 나눕니다. 판단 석은 도달 가능한 최상위 등급을 유지하고, 기계 팬은 할당량 분배를 유지합니다.
- [fleet-admiral] 게이트웨이 단계 스킬 이름을 `workflow-architecting`, `workflow-research`, `workflow-implementing`, `workflow-review`로 바꿉니다.
- [fleet-admiral] Claude Code 하네스를 되풀이하던 상시 게이트웨이 독트린을 줄입니다. 디렉터리 독트린은 CLAUDE.md 자동 표면에 맡깁니다.
- [core-ai-gateway][fleet-admiral] Anthropic 호환 AI Gateway 라우터, 쿼터 프로브, 실행 환경을 Fleet Console과 Fleet CLI가 공유하는 호스트 중립 패키지로 옮깁니다.

#### Fixed
- [core-ai-gateway] OpenCode Go DeepSeek V4의 텍스트 전용 Chat Completions 요청에서만 이미지 파트를 제거해 `image_url` 스키마 오류를 막습니다.

#### Removed
- [fleet-admiral] `gateway_models`에서 실측 roleFit 테이블을 제거합니다. 품질 신호는 카탈로그 등급이고, 할당량은 같은 등급 안에서만 가립니다.

## [1.50.1] - 2026-08-05

### fleet-core

#### Fixed
- [core-ai-gateway] Codex 세션이 `400 Unknown parameter: 'input[N].reasoning_content'`로 죽지 않습니다. assistant 추론은 Responses 요청 본문에 실리지 않습니다.

## [1.50.0] - 2026-08-05

### fleet-core

#### Added
- [core-ai-gateway] OpenCode Chat Completions와 Cursor 추론을 Claude Code thinking으로 보여 줍니다. DeepSeek V4 추론 기록은 도구 턴 사이에도 남습니다.

## [1.49.0] - 2026-08-05

### fleet-plugin

#### Fixed
- [fleet-console] 공급자 추론이 조용한 동안에도 AI Gateway 응답 스트림을 유지합니다.

### fleet-core

#### Added
- [core-ai-gateway] Claude Code에서 OpenAI Responses 추론을 Anthropic thinking 블록으로 스트리밍합니다.

#### Changed
- [fleet-admiral] 게이트웨이 세션은 내장 Claude API 레퍼런스 스킬을 싣지 않습니다. 일반 Claude 세션은 유지합니다.
- [fleet-admiral] 게이트웨이 호스트가 대량 병렬 작업을 공급자 할당량 기준으로 나눕니다. 여유 있다고 보고된 공급자는 사용률과 무관하게 쓰고, 읽을 수 없는 할당량은 고갈이 아니라 분배에서 뺍니다.

#### Fixed
- [core-ai-gateway] Cursor 대화가 모델 창을 채운 뒤에도 눈먼 채 계속되지 않습니다. 게이트웨이가 다음 턴을 413으로 거절해 Claude Code가 압축 후 이어가게 합니다.
- [core-ai-gateway] 게이트웨이 응답 스트림에 Anthropic 호환 SSE keepalive 주석을 보냅니다.

## [1.48.0] - 2026-08-05

### fleet-console

#### Added
- [fleet-console] 휴면 Operation을 War Room 대기열 아래 선반에 둡니다. 고르면 War Room을 떠나지 않고 제자리에서 재개합니다.

#### Changed
- [fleet-console] War Room의 모든 표면에서 우클릭이 동작합니다. Theater 밴드 안 빈곳은 그 Theater로 실행하고, 어느 Theater에도 속하지 않으면 아무것도 열리지 않습니다.

#### Fixed
- [fleet-console] War Room에서 한 Operation은 어디서나 같은 활동 색입니다. 유휴·대기·백그라운드가 카드·지도·사이드바에서 서로 다르게 읽히지 않습니다.

### fleet-plugin

#### Added
- [fleet-console] Settings에서 AI Gateway 모델을 펼쳐 제공할 추론 강도를 고릅니다. 고른 강도 하나가 위임 정체성 하나가 됩니다.
- [fleet-console] 저장소 기록 안에서 커밋 두 개를 비교합니다. 기준을 고정하거나 Shift-클릭 쌍, 또는 검사기 동작입니다.
- [fleet-console] 스태시 행이 검사기에서 스태시 커밋을 엽니다.

#### Changed
- [fleet-console] 모델 추론 강도를 좁혀도 위임 정체성만 바뀝니다. `/model`과 세션 `/effort`는 전체 사다리를 그대로 봅니다.
- [fleet-console] 별도 비교 뷰를 은퇴합니다. 브랜치·태그 비교는 기록 안 결과 독으로 갑니다.
- [fleet-console] 기록은 체크아웃과 무관한 저장소 기록입니다. 흐림과 미커밋 행만 활성 체크아웃을 따릅니다.
- [fleet-console] 수동 저장소 동기화가 성공·실패를 알립니다. 자동 동기화와 스로틀 스킵은 조용합니다.

#### Fixed
- [fleet-console] AI Gateway 설정이 Anthropic 와이어로 전달할 수 없는 추론 강도를 제공하지 않습니다.
- [fleet-console] 기록 페인이 좁을 때 커밋 상세 독이 폭 0으로 접히지 않고 세로로 쌓입니다.

### fleet-core

#### Added
- [core-ai-gateway] 게이트웨이 모델 창이 감당 못 하는 스킬 본문을 보류하고 크기 스텁으로 바꾸며, 목록에서도 빼 모델이 적재를 시도하지 않게 합니다. 1M 이상 창은 감당 가능한 본문을 유지합니다.
- [core-ai-gateway] `FLEET_GATEWAY_WIRE_LOG`가 Cursor 턴이 실제로 싣는 내용과 replay 크기를 기록합니다. Cursor는 매 턴 replay 전체를 다시 올립니다.

#### Changed
- [fleet-admiral] 게이트웨이 독트린이 호스트를 떠나는 실행마다 모델을 명시하고, 세션 자신의 할당량은 맨 나중에 씁니다. `workflow` 스킬이 표면 선택과 모델 배정을 맡습니다.
- [fleet-admiral] 실행을 핀하지 않는 것은 기록되는 결정입니다. 세션 자신의 모델을 쓸 수 있는 예외는 `E1`, `E2`, `E3`뿐입니다. 읽을 수 없는 할당량은 고갈이 아닙니다.
- [fleet-admiral] `gateway_models` 지침이 계열(`homolineage`)과 청구 구독을 분리합니다.
- [fleet-admiral] gateway_models 로스터가 이 세션이 실제로 등록한 강도를 보고합니다.

#### Fixed
- [fleet-wiki] 위키 검색이 비라틴 토픽을 토큰화해, 여러 단어 한국어 질의가 `wiki_briefing`, `wiki_query`, `wiki_resolve`에 닿습니다.
- [core-ai-gateway] 모델 실제 창을 넘긴 게이트웨이 턴을 HTTP 413으로 거절합니다. Claude Code가 압축 후 이어가는 형태입니다.
- [core-ai-gateway] Cursor 대화가 컨텍스트 창 절반 근처에서 거절되지 않습니다. 천장은 모델 창뿐이고, 압축을 시작시키는 413으로 응답합니다.

## [1.47.0] - 2026-08-04

### fleet-console

#### Added
- [fleet-console] Watch Deck 카드가 호버 뒤 읽을 수 있는 확대창으로 커집니다. 키보드 포커스는 바로 열고, 클릭은 기존처럼 무대로 올립니다.
- [fleet-console] 선별 처리에서 우클릭하면 캔버스 제어 메뉴가 열리고, 실행 항목은 비활성입니다.
- [fleet-console] 선별 처리에 저장되는 SPOTLIGHT 토글이 생깁니다(기본 켬). 끄면 완료 작업이 자동 등단하지 않고, 검토 전까지 도착 신호가 남습니다.
- [fleet-console] 선별 Watch Deck를 휠·핀치·Ctrl/Cmd+휠로 확대/축소합니다. 판독 아래로 줄이면 작전지도가 됩니다.
- [fleet-console] 선별 작전지도가 Theater 구역 안에서 움직입니다. 휠 줌, 모든 마커 이름, 대기 마커 맥동, 실행 마커는 유영하다 호버에서 멈춥니다.
- [fleet-console] War Room이 어느 밀도에서나 같이 읽힙니다. 대기 섹션이 각자 건수를 말하고, 작전지도 구역은 자리를 지키며, 마커를 끌어 패널을 옮길 수 있습니다.
- [fleet-console] War Room 스포트라이트를 끄면 자동 등단이 모두 멈춥니다. 무대에 서는 것은 직접 고른 패널뿐입니다.
- [fleet-console] Help의 화면 안내 다시 보기가 지금 화면의 안내만 재생하며, 더 넓은 소개부터 다시 시작하지 않습니다.

#### Changed
- [fleet-console] 커맨드 밴드 Settings 버튼으로 설정을 토글합니다. 다시 누르면 이전 페이지 또는 Operations로 돌아갑니다.
- [fleet-console] 선별 처리가 모든 Theater를 아우르는 전역 모드입니다. 다른 Theater Operation을 무대에 올려도 Theater를 바꾸지 않습니다.
- [fleet-console] 맵 컨트롤 여섯을 Cruise / Tactical / War Room 스위치와 그 모드 도구 트레이로 바꿉니다. 세 모드 이름은 모든 로케일에서 영어입니다.
- [fleet-console] 선별 스포트라이트와 덱 밀도를 War Room 트레이로 옮겨, 모든 모드가 밴드의 같은 자리에 도구를 둡니다.
- [fleet-console] War Room 안내 하나를 화면별 안내로 나눕니다. 한 방문에는 안내 하나만 재생됩니다.

#### Fixed
- [fleet-console] Watch Deck 카드가 사이드바와 같은 상태 이름·순서를 써서, 유휴가 대기로 읽히지 않습니다.
- [fleet-console] 맵을 좌클릭하면 어디서 열었든 캔버스 제어 메뉴가 닫힙니다.
- [fleet-console] 레이아웃 버튼은 Tactical에만 있어, 눌러서 War Room이 조용히 끝나지 않습니다.
- [fleet-console] 눌러도 변화가 없던 War Room에서 캔버스 보기 초기화를 제공하지 않습니다.
- [fleet-console] 기다리는 작업이 없어도 War Room 안내가 뜹니다. 대기 레일에서 시작하고, 무대 스텝은 무언가 설 때까지 건너뜁니다.

### fleet-core

#### Changed
- [fleet-admiral] 게이트웨이 로스터가 각 모델을 실행하는 에이전트 이름을 추론 강도마다 실어, 거부되는 모델 id 대신 그 이름으로 단계를 배정합니다.

#### Fixed
- [core-ai-gateway] Kimi와 OpenCode Anthropic 계열 게이트웨이 응답이 세션이 요청한 모델 id를 실어, 재개 때 모델이 복원됩니다.

## [1.46.0] - 2026-08-03

### fleet-cli

#### Added
- [fleet-cli] `fleet auth login`과 `fleet auth logout`이 `kimi|opencode`를 받고, 인증 패널에 두 공급자가 보입니다.

### fleet-console

#### Added
- [fleet-console] 선별 큐가 비면 라이브 Watch Deck가 휴면이 아닌 Operation을 카드로 보여 줍니다. 카드를 누르면 무대로 올라갑니다.
- [fleet-console] 턴이 끝난 뒤에도 서브에이전트나 워크플로우가 남은 Operation을 Background 상태로 표시합니다.
- [fleet-console] Codex 검색이 본문까지 찾고 하이라이트 조각을 보여 줍니다.
- [fleet-console] Codex 리더에 뒤/앞 히스토리가 생기고, Theater별로 마지막 항목과 스크롤이 새로고침 후 복원됩니다.
- [fleet-console] Codex 분할 뷰에 현재 섹션을 따라가는 접을 수 있는 개요가 생깁니다.
- [fleet-console] Codex 항목 카드가 상대 갱신 시간을 보여 주고, 최신순/이름순과 태그 칩 필터가 생깁니다.
- [fleet-console] 커맨드 팔레트에서 Codex 위키 항목을 열어 어디서든 문서를 엽니다.
- [fleet-console] Codex 내비게이터가 마지막 drydock·충돌·검토 대기를 위키 상태 줄로 보여 줍니다.

#### Changed
- [fleet-console] 시스템 컨트롤을 커맨드 밴드로 옮깁니다. 기어는 Settings, ? 메뉴는 What's New·단축키·업데이트·GitHub입니다. 사이드바 System Menu는 제거됩니다.

#### Fixed
- [fleet-console] 사이드바를 접으면 커맨드 밴드 맵 컨트롤이 옛 폭에 뜨지 않고 왼쪽 컨트롤군에 붙습니다.
- [fleet-console] Codex 충돌 목록 행에서 상세가 열리고, drydock·schema 문서의 코드 복사가 동작합니다.
- [fleet-console] Codex 리더가 빠른 탐색 중 늦은 응답을 무시하고, 패치 메타데이터를 그리기 전에 이스케이프합니다.

### fleet-plugin

#### Added
- [fleet-console] 저장소 비교가 base를 기본 브랜치, head를 현재 브랜치로 채운 뒤 진입 시 실행합니다. 교환 버튼으로 맞바꿉니다.
- [fleet-console] 브랜치 행에 비교 동작이 생겨, 채워진 비교가 바로 실행됩니다.
- [fleet-console] 쿼터 창이 리셋 주기와 출처를 밝히고, Cursor 총합을 풀 합산으로 표시하며 Kimi 절대 사용량을 실습니다.
- [fleet-console] 터미널 세션이 정화한 최신 트랜스크립트 줄을 보고해, 라이브 프리뷰가 없는 Watch Deck 카드가 그 줄을 씁니다.
- [fleet-console] 백그라운드 서브에이전트 작업을 잔여가 소진되거나 30분이 지나거나 세션이 라이브를 벗어날 때까지 추적합니다.
- [fleet-console] Terminal 모델 인증을 AI Gateway용 API 키로 일반화하고, Kimi 옆에 OpenCode Go 로그인을 둡니다.
- [fleet-console] 사용 한도에 OpenCode Go 카드를 올립니다. 로컬 OpenCode CLI 기록과 공개 Go 캡 대조이며, 로컬 관측 분량임을 명시합니다.

#### Changed
- [fleet-console] Settings의 Carriers 섹션 이름을 두 로케일 모두 AI Classic으로 바꿉니다.

#### Fixed
- [fleet-console] Shell 패널을 다시 열어 스크롤백이 재생될 때 장치 질의 응답이 셸 프롬프트에 입력되지 않습니다.
- [fleet-console] 기본 레일 폭에서 비교 실행 버튼이 화면 밖으로 잘리지 않습니다.
- [fleet-console] 저장소 hunk 엔드포인트가 `--no-ext-diff`와 `--no-textconv`를 써서, 브라우저가 유발한 diff로 저장소 로컬 diff 드라이버가 실행되지 않습니다.

#### Removed
- [fleet-console] Terminal 플러그인 분석가·실행 카탈로그와 CLI 감지에서 Cursor Agent를 제거합니다. Cursor는 AI Gateway로 계속 쓸 수 있습니다.

### fleet-core

#### Added
- [fleet-admiral] `gateway_models`가 창마다 압력 판정을 서버에서 만듭니다. 워크플로는 리셋 주기가 다른 창의 원시 퍼센트 대신 그 판정으로 할당량을 서열화합니다.
- [core-ai-gateway] OpenCode Go를 AI Gateway 공급자로 추가합니다. 구독 모델 22종을 네이티브 경로로 서비스하며 `/zen/go/v1/messages`와 `/zen/go/v1/responses`를 포함합니다.
- [fleet-admiral] 기존 Kimi 검증과 같이, OpenCode Go API 키를 저장 전에 Anthropic 호환 실검증으로 확인합니다.

#### Fixed
- [core-ai-gateway] OpenAI strict 도구 스키마에서 JSON Schema `format` 힌트를 제거해, 허용되지 않는 format이 Codex 요청 전체를 400으로 실패시키지 않습니다.

#### Removed
- [core-unified-agent] 통합 Agent CLI SDK에서 Cursor ACP 공급자를 제거합니다. Cursor는 독립 AI Gateway 어댑터로 계속 제공됩니다.

## [1.45.0] - 2026-08-03

### fleet-cli

#### Removed
- [fleet-cli] Fleet 실행 카탈로그에서 Codex CLI를 제거합니다.

### fleet-console

#### Removed
- [fleet-console] `@fleet-console/sdk/launch` 서브패스를 제거합니다. `LaunchContext`는 `@fleet-console/sdk/plugin`에서 제공합니다.
- [fleet-console] 기존 세션 데이터와 Fleet Wiki Codex는 보존하면서 Codex Agent Operation을 제거합니다.

### fleet-plugin

#### Fixed
- [fleet-console] File Explorer git 배지가 Repository 패널과 같은 환경 차단으로 git을 실행해, 상속된 askpass나 알 수 없는 helper가 배지 갱신에서 실행되지 않습니다.
- [fleet-console] Session Analyst 라우트가 `Content-Type`에 charset이 붙거나 미디어 타입이 대문자인 JSON도 받습니다.
- [fleet-console] AI Gateway가 macOS뿐 아니라 Linux와 Windows에서도 Cursor 구독 토큰을 찾습니다. `%APPDATA%/Cursor/auth.json`, 또는 `$XDG_CONFIG_HOME`이나 `~/.config` 아래 `cursor/auth.json`입니다.
- [fleet-console] AI Gateway가 ChatGPT 구독 토큰을 찾을 때 `CODEX_HOME`을 존중합니다. Codex 홈을 옮겨도 Usage limits만 로그인을 찾고 모델 호출이 401이던 문제가 없습니다.

#### Removed
- [fleet-console] Terminal 플러그인에서 Codex Agent Operation의 실행·재개·캡처·활동 연동을 제거합니다.

### fleet-core

#### Added
- [core-ai-gateway] Cursor와 Codex 자격 조회를 공유 API로 두어, 모든 호출자가 플랫폼을 아는 한 구현으로 구독 토큰을 찾습니다.

#### Fixed
- [fleet-wiki] 대기 중인 패치가 개행·따옴표·역슬래시가 든 태그를 그대로 보존합니다.

#### Removed
- [fleet-admiral] Admiral에서 Codex CLI 프로필, 주입, 플러그인 등록을 제거합니다.

## [1.44.1] - 2026-08-02

Release v1.44.1

## [1.44.0] - 2026-08-02

Release v1.44.0

## [1.43.0] - 2026-08-02

### fleet-core

#### Changed
- [fleet-admiral] 게이트웨이 세션에서 Claude Code 내장 에이전트를 끄지 않습니다. 게이트웨이 모델 에이전트가 함께 놓여, 세션 자신의 모델로 위임할 수 있습니다.
- [core-unified-agent] 기본 테스트 실행에서 실제 Claude/Codex e2e를 뺍니다. 같은 계약은 mock 단위 테스트로 유지합니다.

## [1.42.0] - 2026-08-02

### fleet-console

#### Added
- [fleet-console] 플러그인이 특정 Operation에서 동반 패널을 사용 불가로 표시하면, 패널과 단축키, 단축키 도움말이 함께 사라집니다.
- [fleet-console] 캔버스 제어의 Claude 실행 종류마다 한 줄 설명을 붙입니다.

#### Changed
- [fleet-console] 캔버스 제어를 처음 열면 Claude 세 종류를 메뉴 순서로 안내해 Native와 Classic도 Gateway와 함께 소개합니다.

#### Fixed
- [fleet-console] 기능 안내에서 카드가 넘어가기 전에 Next를 연달아 눌러도 진행 수가 전체를 넘지 않습니다.

### fleet-plugin

#### Added
- [fleet-console] wiki 스킬·Console 훅·wiki MCP만 싣는 Claude (Native) 실행을 추가합니다. Carrier Streams 표면은 없습니다.

#### Changed
- [fleet-console] 캐리어 도구를 받지 않는 Claude (Gateway) Operation에서 Carrier Streams, STREAMS 핸들, 출격 리본, Alt+C를 제거합니다.

#### Fixed
- [fleet-console] 대비할 Native·Gateway가 없는 Codex 실행 항목에서 (Classic) 접미를 제거합니다.

### fleet-core

#### Added
- [fleet-admiral] Admiral 시스템 프롬프트와 캐리어/게이트웨이 도구를 생략하는 `native` doctrine의 Console 전용 `claude-native` Agent CLI를 도입합니다.
- [core-ai-gateway] `FLEET_GATEWAY_WIRE_LOG`로 켜는 게이트웨이 통신 로그를 추가합니다. 경로를 지정하지 않으면 동작하지 않습니다.

#### Changed
- [fleet-admiral] 게이트웨이 독트린에서 작업은 기본적으로 Agent에 맡기고, staged 워크플로는 요청했을 때만 씁니다.
- [fleet-admiral] 위임 실행마다 게이트웨이 로스터를 읽고, 세션 모델을 재사용하지 않고 적합도와 할당량으로 모델을 고릅니다.
- [fleet-admiral] 게이트웨이 실행을 결과가 돌아오는 run으로 기술하고, 백그라운드 작업 완료를 설명하던 `<system-reminder>` 서문을 제거합니다.
- [fleet-admiral] 게이트웨이 run 실패가 빈 반환이나 누락으로 온다고 보고, 두 번 실패한 run을 보고 없이 호스트 세션이 대신하지 않습니다.

#### Fixed
- [fleet-admiral] 기본 Claude Code 에이전트를 구형·현재 선택자로 막고, 게이트웨이 커스텀 에이전트 이름을 간소화합니다.
- [core-ai-gateway] 게이트웨이 모델이 요청받지 않은 선택 도구 인자를 채우지 않습니다. 고정한 서브에이전트가 Claude로 바뀌거나, 없는 인자 때문에 파일 읽기가 실패하지 않습니다.

## [1.41.0] - 2026-08-02

### fleet-cli

#### Removed
- [fleet-cli] `fleet console`와 로컬 전용 `fleet desktop`을 제거합니다. 독립 `fleet-console` 바이너리를 쓰세요. `fleet update`는 PATH의 `fleet-console`을 찾아 실행 중 Console을 먼저 멈춥니다.

### fleet-console

#### Added
- [fleet-console] 캔버스 제어에서 Claude Gateway 첫 실행을 안내합니다. 선별 안내는 그 모드에 들어갈 때만 뜹니다.

#### Changed
- [fleet-console] agent CLI 세션 캡처를 별도 captures 디렉터리 대신 Console 상태 파일에만 보관합니다.
- [fleet-console] 기존 캡처 파일을 시작 시 Console 상태 파일로 옮긴 뒤 남은 captures 디렉터리를 지웁니다.

#### Fixed
- [fleet-console] npm/pnpm 글로벌 bin 심링크와 macOS `/var`·`/private/var` 별칭을 풀어, 설치된 `fleet-console`이 조용히 종료되지 않고 시작하게 합니다.

### fleet-plugin

#### Added
- [fleet-console] 이틀 이상 기간에서 Ledger가 CLI별 합계 위에 기기 전체 일별 비용을 그립니다. 사용량 없는 날도 0으로 읽힙니다.
- [fleet-console] Ledger 일별 차트가 각 세션 비용을 마지막 활동일에 합산한다고 밝힙니다.
- [fleet-console] Ledger가 4자리 연도 로컬 날짜를 만들 수 없는 사용 기록을 건너뛰어, 잘못된 데이터가 패널을 깨지 않습니다.
- [fleet-console] File Explorer 행에 git 상태 배지(M/U/D)를 표시합니다.
- [fleet-console] Session Analyst의 Escape가 슬래시 명령 목록을 닫고, 초안을 지우고, Analyst 동반 패널을 닫습니다.
- [fleet-console] Session Analyst 아티팩트에 테마 토큰 베이스 스타일을 넣어 활성 콘솔 테마를 따르게 합니다(`--fleet-canvas` 등).
- [fleet-console] File Explorer 우클릭 메뉴에 경로 복사, 상대 경로 복사, 파일 관리자에서 보기, 기본 앱으로 열기를 둡니다.
- [fleet-console] File Explorer 구분선을 방향키로 조절할 수 있게 합니다.
- [fleet-console] 저장소 동기화 동작을 추가합니다. `git fetch --prune --no-tags` 뒤 브랜치·기록·워크트리·변경을 갱신합니다.
- [fleet-console] 마지막 fetch가 5분보다 오래됐으면 저장소 패널을 열 때 자동 동기화합니다. 실패해도 로컬 데이터는 보입니다.
- [fleet-console] Claude Code를 로컬 AI Gateway로 구동하는 Claude (Gateway) 실행을 추가합니다. Codex·Cursor·Kimi 백엔드로 중계하고, 기본 Claude 모델은 그대로 통과합니다.
- [fleet-console] Terminal 설정의 Agent CLI를 AI Gateway 공급자별 모델 로드아웃으로 바꿉니다. 켠 모델만 `/model`에 오르고(opt-in), 별표 모델이 세션 기본입니다.
- [fleet-console] AI Gateway 설정에 기본 꺼진 진단 제어를 추가합니다. 새로 시작하는 추적의 payload 없는 Cursor 전송 이벤트를 기록합니다.
- [fleet-console] AI Gateway가 컨텍스트를 소진한 턴에 `Prompt is too long: N tokens > M maximum`으로 응답해 Claude Code가 압축 후 이어가게 합니다.
- [fleet-console] 응답 헤더 이후 게이트웨이 실패가 스트림을 조용히 자르지 않고, 사유를 담은 종단 SSE `error` 프레임으로 끝납니다.
- [fleet-console] 사용 한도에 Kimi를 네 번째 공급자로 올리고, Fleet에 등록된 Kimi API 키로 조회합니다.
- [fleet-console] Cursor Auto 한도와 API 한도를 별도 창으로 추적합니다.
- [fleet-console] Claude (Gateway) 세션에 gateway_models 도구를 줍니다. 배치할 모델, 창, 강도, 쿼터 풀, 현재 공급자 한도를 보고하며 Settings에서 켠 모델만 나열합니다.
- [fleet-console] Claude Gateway Operation은 시작 때 AI Gateway 모델·강도 Agent를 주입하고, 그 경로의 Claude Code 내장 Agent 로스터를 끕니다.

#### Changed
- [fleet-console] Scuttlebutt 말풍선에서 완료되거나 시작된 Operation을 바로 엽니다. 닫기는 별도 동작입니다.
- [fleet-console] 동시에 몰린 Scuttlebutt 알림을 Operation별 행으로 펼쳐 각각 포커스할 수 있습니다.
- [fleet-console] 카탈로그 선택기에 포커스가 가면 Session Analyst 슬래시 명령 목록을 닫아 두 추천이 겹치지 않습니다.
- [fleet-console] 기존 Claude·Codex 캔버스 Operation을 AI Gateway Operation과 구분해 표시합니다.

#### Removed
- [fleet-console] 직결 Kimi (Claude Code) 실행을 제거합니다. Kimi 세션은 AI Gateway로 돌고, 강도는 Claude Code의 `/effort`와 `/model`에서 제어합니다.

### fleet-core

#### Added
- [core-ai-gateway] Anthropic Messages 요청을 하나의 모델 카탈로그에서 Codex·Cursor·Kimi 업스트림 호출로 번역하는 core AI gateway 패키지를 추가합니다.
- [core-ai-gateway] Cursor 도구 이어가기 동안 유지되면서, 새로 시작하는 추적에는 현재 설정을 적용하는 추적별 진단 정책을 추가합니다.
- [core-ai-gateway] 게이트웨이가 창을 넘는 턴을 상류 호출 전에 거절하고, 어댑터가 실제로 보내는 도구만 계산합니다.
- [core-ai-gateway] 카탈로그에서 모델별 라우팅 제약을 도출합니다. 업스트림 식별자, 광고된 추론 레벨, Anthropic 계보, Cursor 쿼터 풀입니다.
- [fleet-admiral] Claude (Gateway) 호스트가 단계별 모델·강도를 스스로 판단하게 합니다. 세션 모델 상속이 기본이고, 다른 모델 고정에는 명시 근거가 필요합니다.
- [fleet-admiral] Claude (Gateway) 세션에 온디맨드 작전 스킬 네 종을 추가합니다. architecture-review, implementation-run, quality-review, codebase-research입니다.
- [core-ai-gateway] 스트리밍·비스트리밍에서 Anthropic 토큰 회계를 맞추면서 Codex 캐시와 추론 사용량 세부 정보를 보존합니다.
- [core-ai-gateway] Codex Responses로 Claude Code `Web Search`를 지원합니다. 도메인 필터, 출처 결과, 명시적 공급자 검색 오류를 전달합니다.

#### Changed
- [fleet-admiral] Claude Gateway 세션은 프로토콜 스킬·캐리어 로스터·carrier_dispatch 없이 Standing Order만으로 동작하고, 기존 Agent CLI 세션은 그것을 유지하도록 Admiral 독트린을 나눕니다.
- [fleet-admiral] Gateway Orchestration Policy는 세션 기본과 다른 모델·강도의 staged Agent를 고르기 전에 `gateway_models` MCP 도구를 쓰게 합니다.

## [1.40.0] - 2026-07-30

### fleet-cli

#### Removed
- [fleet-cli] 터미널 호스트에서 Fleet Plans 도구와 워크스페이스 바인딩을 제거합니다.
- [fleet-cli] opencode-go 캐리어 색상 팔레트 항목을 제거합니다.

### fleet-console

#### Added
- [fleet-console] Mod+P로 커맨드 모드 접두가 채워진 커맨드 팔레트를 바로 엽니다.

#### Changed
- [fleet-console] 레일 헤더의 Float over Map 텍스트 토글을 화면 속 화면 아이콘으로 바꾸고, 불투명도 프리셋을 40-100 연속 슬라이더로 바꿉니다.
- [fleet-console] Whites 라이트 테마를 푸른 흰색에서 따뜻한 오트밀 뉴트럴로 재조율합니다. brass·신호·carrier·정체성 색은 그대로입니다.
- [fleet-console] Settings 테마 피커를 Light|Dark 스위치로 재편합니다. Dark에서 Instrument·Maritime·Carbon이 열리고, 브라우저별 마지막 다크 테마를 복원합니다.

#### Removed
- [fleet-console] Plans Activity Rail 패널, 검색 연동, HTTP API를 제거합니다.
- [fleet-console] Daywatch·Drydock 라이트 테마를 퇴역합니다. 저장된 선택은 Whites로 폴백해 첫 화면이 라이트로 남습니다.
- [fleet-console] Agent CLI 탐지, 실행 경로, Session Analyst 공급자 선택에서 OpenCode Go를 제거하고 opencode 캐리어 테마 토큰을 삭제합니다.

### fleet-plugin

#### Added
- [fleet-console] Scuttlebutt가 작업 시작도 Started 말풍선으로 알립니다. 보고 있는 Operation의 시작은 조용합니다. 기본 켜진 출발 알림 설정으로 끌 수 있습니다.
- [fleet-console] Claude Code와 Codex 구독 rate-limit을 보여 주는 사용 한도 레일 패널을 추가합니다. 세션·주간·모델별 막대와 Codex 리셋 잔여가 있습니다.
- [fleet-console] Claude 사용량은 명시적 연결 뒤에만 로컬 CLI 로그인으로 조회합니다. 자격은 읽기 전용이고, 요청은 각 공급자에게만 갑니다.
- [fleet-console] 사용 한도에 Cursor를 추가합니다. 청구 주기의 포함·Auto·API 사용, 리셋까지 남은 시간, 현재 요금제입니다.

#### Changed
- [fleet-console] Whites 터미널의 종이·잉크·무채 ANSI를 오트밀 분위기에 맞춰 따뜻하게 합니다. 유채 ANSI는 의미색 그대로입니다.
- [fleet-console] 사용량 공급자를 각각 독립적으로 연결·해제합니다. 하나를 조작해도 다른 공급자가 갱신되지 않습니다.

#### Fixed
- [fleet-console] 패널 안 동반 핸들 칩의 한글 라벨을 선명하게 합니다.
- [fleet-console] 저장소 기록에서 merge 커밋 레인 선이 분기점 위로 튀거나, 로드된 페이지 밖 부모를 향해 끊긴 선이 그려지지 않습니다.
- [fleet-console] 커밋 행의 그래프 거터 폭을 통일해 merge 구간에서 제목이 옆으로 밀리지 않습니다.

#### Removed
- [fleet-console] Terminal Agent 세션에서 Plan 도구를 제거합니다.
- [fleet-console] Ledger의 과거 opencode 사용량 행을 기본 글리프와 원시 클라이언트 id로 표시합니다.

### fleet-core

#### Breaking Changes
- [core-agent][fleet-admiral][fleet-carriers] Fleet Plans 패키지와 Plan 전용 오케스트레이션 계약을 제거합니다.
- [core-unified-agent][fleet-analyst] opencode-go 프로바이더를 제거합니다. opencode는 더 이상 Fleet 백엔드로 탐지되거나 실행되지 않습니다.

## [1.39.0] - 2026-07-29

### fleet-console

#### Changed
- [fleet-console] 라이트 테마에서 터미널이 가장 밝은 표면이 되어 시선이 작업 영역에 앉습니다. 다크 테마는 변화가 없습니다.
- [fleet-console] 라이트 테마의 Operation 창 프레임과 타이틀바가 화면에서 가장 어두운 색 대신 커맨드 밴드와 같은 톤을 씁니다. 다크 테마는 변화가 없습니다.

#### Fixed
- [fleet-console] brass 기본 버튼 텍스트가 라이트 테마 3종 모두에서 읽힙니다.

### fleet-plugin

#### Changed
- [fleet-console] 라이트 터미널 배경이 각 테마의 색조를 유지한 채 가장 밝은 표면이 되고, bright-white ANSI 블록은 페이지보다 더 희게 유지됩니다.

## [1.38.0] - 2026-07-28

### fleet-console

#### Added
- [fleet-console] Plans 레일에서 두 단계 ARM 확인으로 오래된 plan 파일을 삭제합니다. 열려 있던 리더는 해당 plan이 삭제되면 닫힙니다.

#### Changed
- [fleet-console] 커맨드 팔레트 검색이 오타와 약어에도 맞으며, 정확 일치를 먼저 보여 줍니다.
- [fleet-console] 한글 UI 폴백으로 Pretendard Variable을 넣고, 라이트 테마 본문 굵기를 조금 올려 한글 획이 읽히게 합니다.

#### Fixed
- [fleet-console] Daywatch, Whites, Drydock의 신호색 배지와 라벨 대비를 복원합니다. 다크 테마는 변화가 없습니다.
- [fleet-console] 라이트 테마의 보조 텍스트를 어둡게 해 작은 라벨이 밝은 배경에서도 읽히게 합니다.
- [fleet-console] 레일을 바꾸거나 다시 열 때 Codex 검색어·열린 문서·읽기 위치를 유지하고, Theater가 바뀔 때만 초기화합니다.

### fleet-plugin

#### Added
- [fleet-console] Agent CLI 설정에서 PATH가 닿지 않는 CLI의 실행 파일을 지정하고 확인하며, 어떤 PATH를 찾았는지도 볼 수 있습니다.
- [fleet-console] 새 셸·agent 세션에 COLORFGBG 힌트를 전달하고, 콘솔 테마가 다크·라이트 사이를 바꾸면 라이브 터미널에 한 번만 안내 칩을 보여 줍니다.

#### Changed
- [fleet-console] 저장소 기록이 패널을 나갔다 돌아와도 불러온 페이지·스크롤·선택한 커밋·필터를 유지합니다.
- [fleet-console] 기록 툴바에 새로고침이 생겨, 커밋을 다시 불러오려고 레일 패널을 오갈 필요가 없습니다.
- [fleet-console] Agent CLI 감지와 실행이 한 순서를 공유합니다. 환경 변수 재정의, 지정한 경로, PATH 순이며, 설정한 소스가 실패하면 조용히 다른 바이너리로 넘어가지 않고 알립니다.

#### Fixed
- [fleet-console] 라이트 테마 터미널에서 다크용으로 조율된 agent CLI나 흰색 ANSI 텍스트가 읽히게 합니다.
- [fleet-console] Operation 목록이 넘치면 Ledger 레일이 스크롤되어 CLI별 기기 전체 섹션에 닿을 수 있습니다.
- [fleet-console] Operation 상세를 열 때 Ledger 스크롤을 맨 위로 옮기고, 돌아올 때 목록 위치를 복원합니다.
- [fleet-console] 저장소 변경 파일과 비교 목록이 패널에 돌아올 때 스크롤 위치를 복원합니다.
- [fleet-console] 워크스페이스 소스 트리가 브랜치·워크트리·변경 개수 로딩이 끝나도 스크롤 복원을 잃지 않습니다.
- [fleet-console] Scuttlebutt 완료 말풍선이 Operation 제목이 길어도 Finished 라벨이 가려지지 않게 합니다.

## [1.37.1] - 2026-07-28

### fleet-plugin

#### Fixed
- [fleet-console] Console 테마가 바뀌어도 Scuttlebutt 제독 마스코트 3종의 색을 그대로 둡니다.

## [1.37.0] - 2026-07-27

### fleet-console

#### Added
- [fleet-console] 라이트 테마 3종(Daywatch, Whites, Drydock)을 추가하고 설정 테마 선택기를 다크·라이트 계열로 나눕니다.
- [fleet-console] 저장된 테마를 초기 HTML에 반영해, 라이트 테마 사용자가 다크 첫 화면 섬광을 더 이상 보지 않습니다.
- [fleet-console] 마크다운 구문 강조가 활성 테마를 따르며, 구형 엔진에는 다크 폴백을 둡니다.

#### Fixed
- [fleet-console] 로컬 네트워크 환경에서 유휴 Operations 스트림 연결이 유지됩니다.

### fleet-plugin

#### Added
- [fleet-console] 라이트 테마 3종의 터미널 팔레트를 추가하고, Global Shell과 Session Analyst 아티팩트가 활성 테마를 따릅니다.

## [1.36.0] - 2026-07-26

### fleet-console

#### Added
- [fleet-console] 커맨드 팔레트 명령 모드가 세션 생명주기를 다룹니다. 8초 실행 취소 창이 살아 있는 동안 마지막 닫기 실행 취소, Theater 추가, Theater 잊기가 사이드바와 같은 실행 취소 창을 씁니다.
- [fleet-console] 명령 모드가 레일 패널 검색 결과도 보여, > 뒤에 질의를 입력하면 명령 모드를 떠나지 않고 Files, Repository 등 패널에 닿습니다.
- [fleet-console] 서버 연결이 끊기면 커맨드 밴드, 배너, 갱신이 멈춘 레일 패널에서 알리고 각각 다시 연결할 수 있습니다.
- [fleet-console] 다시 연결 덮개가 레일 패널을 가리는 동안 포커스가 다시 연결로 옮겨 가고, 연결이 살아나면 돌아옵니다.
- [fleet-console] 모든 패널 맞춤이 보이는 Operation을 화면에 담습니다(Shift+1, 커맨드 밴드 버튼, 또는 팔레트). 터미널의 ! 입력은 유지되며, Formation 보기와 선별 처리 모드에서는 숨깁니다.
- [fleet-console] 맵과 Formation 보기에서 Alt+Up, Alt+Down으로 활성 패널을 최대화하거나 최소화합니다.
- [fleet-console] 선별 처리 모드에서 Alt+Down으로 이번 차례를 치워 둡니다. 1.5초 안에 한 번 더 누르면 실행되고 Escape로 취소합니다.
- [fleet-console] Alt를 누르고 있으면 패널 위치 번호와 현재 모드에서 쓸 수 있는 키를 보여 줍니다.
- [fleet-console] 플러그인이 컴패니언 패널에 단축키를 지정할 수 있고, Console이 그 키를 처리하며 단축키 도움말에 실어 줍니다.
- [fleet-console] 창이 좁아지면 커맨드 밴드 경로가 맵 컨트롤과 겹치기 전에 접힙니다.
- [fleet-console] 플러그인이 패널 없이 콘솔 전역에 부유 위젯을 그릴 수 있고, 함대 상태는 집계 신호로만 읽습니다.

#### Changed
- [fleet-console] 단축키 도움말에서 동작하지 않던 Esc 항목 두 개를 뺐습니다.

#### Fixed
- [fleet-console] 커맨드 밴드에서 선별 처리 버튼이 Formation 레이아웃 아이콘과 겹치지 않습니다.
- [fleet-console] 대기 중인 Operation이 없으면 선별 처리 모드가 빈 채, 포커스 없이 열립니다.
- [fleet-console] 커맨드 밴드 경로가 패널 영역 기준으로 가운데 정렬되어, 사이드바가 중심을 밀지 않습니다.

### fleet-plugin

#### Added
- [fleet-console] Session Analyst 아티팩트에 내보내기 메뉴가 생깁니다. HTML 다운로드, 소스 복사, 테마가 적용된 렌더를 새 탭에서 열 수 있으며, 작업은 이 기기에 머뭅니다.
- [fleet-console] 활성 agent Operation에서 Alt+C로 Carrier Streams를, Alt+A로 Session Analyst를 여닫습니다.
- [fleet-console] Ledger 패널이 이 기기의 로컬 agent CLI 토큰 사용량과 달러 비용을 Operation별·CLI별로 보여 줍니다. tokscale이 로컬에서 수집하며, 업로드하지 않고 프롬프트 본문은 읽지 않습니다.
- [fleet-console] Theater나 Operation 없이 빠른 질문에 답하며 콘솔을 돌아다니는 퀘이커 제독 셋, Scuttlebutt를 추가합니다.
- [fleet-console] 토리·보리·도리에게 각자의 대화 세션과 말투를 주고, 해당 제독을 눌러 엽니다.
- [fleet-console] 제독들이 웹을 검색하고 공개 자료를 읽되, 파일과 셸 작업은 Theater의 Operation에 남겨 둡니다.
- [fleet-console] 함대 상태를 자세로 알립니다. 작업 중에는 골똘히, 승인 대기나 연결 끊김에는 경보를, 작업이 끝나면 만세를 합니다.
- [fleet-console] 끝난 작업을 말풍선으로 알리고, 말풍선은 전하는 제독을 따라다니다 스스로 사라집니다.
- [fleet-console] 설정에서 제독을 개별로 물릴 수 있고, 대화 머리에서 한 마리를 제자리에 고정하되 동작은 계속합니다.
- [fleet-console] 제독단을 꺼진 채로 출하하고 설정에 실험 기능임을 표시하여, 요청하지 않은 마스코트가 떠 있지 않게 합니다.
- [fleet-console] 콘솔이나 시스템이 모션 감소를 요구하면 편대를 정지 대형으로 세웁니다.

#### Changed
- [fleet-console] 저장소 기록이 첫 200개 커밋에서 멈추지 않고 더 보기로 이어지며, 첫 커밋에 도달하면 알립니다.
- [fleet-console] 저장소 기록이 화면에 보이는 행만 그려, 목록이 길어져도 패널이 무거워지지 않습니다.
- [fleet-console] 저장소 기록 그래프의 레인이 필터를 걸어도 실제 조상 관계를 따릅니다.
- [fleet-console] 유휴 에이전트 세션 설정과 에이전트 알림을 영문 고정 대신 콘솔 표시 언어로 보여 줍니다.
- [fleet-console] 캐리어의 직함과 임무를 콘솔 표시 언어로 보여 주고, 언어 설정을 바꾸면 다시 불러오지 않고 전환합니다.

### fleet-core

#### Added
- [core-unified-agent] 시스템 지침이 CLI 기본값 앞에 붙는 대신 통째로 대체할 수 있어, 호출자가 에이전트 정체성 전체를 정의할 수 있습니다.
- [fleet-carriers] 캐리어 번역을 표시 전용으로 두어, 호스트 에이전트에 전달되는 라우팅 로스터가 언어와 무관하게 유지됩니다.

## [1.35.0] - 2026-07-26

### fleet-console

#### Added
- [fleet-console] 커맨드 밴드의 보기 모드로 자동·모바일·데스크톱을 전환합니다. 자동은 창 너비를 따라가 좁은 창에서는 모바일 레이아웃으로 열립니다.
- [fleet-console] 좁은 화면에서는 전용 셸이 Operation을 상태별로 보여 주고, 세션 하나를 전체 화면으로 열며, 뒤로가기로 목록에 돌아옵니다. 데스크톱 캔버스 배치는 그대로입니다.
- [fleet-console] 선별 처리 모드를 추가합니다. 모든 패널을 접고 대기 중인 Operation을 하나씩 세웁니다(대기 레일, Alt+T, Alt+Right 미루기). 대기란 입력 대기이거나 방금 유휴로 갔는데 아직 확인하지 않은 상태입니다.
- [fleet-console] 새 기능을 진입점에서 한 번 안내하는 기능 투어를 추가하고, 이미 본 안내는 다시 표시하지 않습니다.

#### Changed
- [fleet-console] 아직 열지 않은 유휴 Operation을 사이드바와 Map의 초록 테두리로 알립니다. 포커스하면 해제되며, 선별 처리 모드에서는 유지됩니다.
- [fleet-console] Formation 보기를 더 밝은 관측 격자, 슬롯 번호, 빈 슬롯 안내, 진입 연출을 갖춘 상황판으로 다시 만듭니다.
- [fleet-console] 최초 실행 안내 완료를 브라우저 저장소 대신 콘솔 설정에 기억해, 다른 브라우저나 기기에서 다시 나타나지 않습니다.

#### Fixed
- [fleet-console] 사이드바를 상태별로 정렬해도 Alt+방향키 패널 순환이 상태 구획을 건너뛰지 않고 캔버스 배치 순서를 따릅니다.
- [fleet-console] 펼친 Theater 사이드바를 바로 위 커맨드 밴드와 이어 붙여, 같은 열 이음매에 가로선이 두 겹으로 깔리지 않게 합니다.
- [fleet-console] maritime/carbon 테마에서 모달·메뉴·토스트·명령 팔레트·Float over Map Solid 프리셋에 캔버스가 비치지 않게 합니다.

### fleet-plugin

#### Added
- [fleet-console] 캐리어가 스트리밍하는 동안 Agent Operation에 몇 척이 나갔는지와 각자 하는 일을 담은 출격 리본이 나타납니다. 클릭하면 터미널을 줄이지 않고 Carrier Streams가 열립니다.

#### Changed
- [fleet-console] 캐리어 스트림 행이 고정 높이 대신 패널 높이를 나눕니다. 하나면 필요한 만큼 채우고, 늘어나면 읽히는 하한까지 고르게 줄어듭니다.

#### Fixed
- [fleet-console] 저장소 기록 그래프 레인을 로그가 빠뜨린 커밋에서 가르지 않고 실제 git 조상 관계대로 그립니다.
- [fleet-console] 필터나 행 상한이 중간 커밋을 가린 자리에서는 조상 관계를 주장하지 않고 레인을 잇지 않은 채 둡니다.
- [fleet-console] Session Analyst 핸들의 EXIT가 채팅 패널과 함께 아티팩트 패널도 닫습니다.
- [fleet-console] Skills 읽기 오버레이와 토스트, Session Analyst 아티팩트 메뉴와 슬래시 명령 목록을 maritime/carbon 테마에서 완전히 불투명하게 합니다.

## [1.34.0] - 2026-07-25

### fleet-cli

#### Changed
- [fleet-cli] 제독 호스트가 Fleet Plan을 직접 작성·변경하고, 태스크 완료 표시는 Ohio 전용으로 유지합니다.
- [fleet-cli] 로컬·원격 코드베이스 인텔리전스를 Vanguard의 읽기 전용 정찰 계약으로 통합합니다.
- [fleet-cli] Kirov를 퇴역시키고 선택적 Plan 보증을 Nimitz로 옮긴 기본 Carrier 5명을 표시합니다.
- [fleet-cli] Ohio를 퇴역시키고 직접 구현과 Plan 기반 구현을 Genesis로 옮긴 기본 Carrier 4명을 표시합니다.

### fleet-console

#### Added
- [fleet-console] Float over Map에 Solid/90/75/60 불투명도 프리셋을 추가해, 콘텐츠는 불투명하게 두고 패널 아래로 Map이 비치게 합니다. 선택은 유지됩니다.
- [fleet-console] 커맨드 팔레트에 Operation 동작(Resume(dormant 전용), Close, Minimize all, Formation 보기 전환, 상태축 전환)과 검색 행 상태 뱃지를 추가합니다.
- [fleet-console] Activity Rail 크기 조절 핸들을 키보드로 조작할 수 있습니다. 화살표로 조절하고 Shift+화살표는 더 크게, Home/End는 최소·최대 폭으로 이동합니다.
- [fleet-console] Operation 칩과 Theater 행 컨텍스트 메뉴를 Shift+F10 또는 ContextMenu 키로 열어, 그룹과 accent 지정에 우클릭이 더 이상 필요하지 않습니다.
- [fleet-console] 활성 Operation의 Rename, Assign group, Set accent, Minimize를 커맨드 팔레트에 추가합니다.
- [fleet-console] 닫은 Operation과 잊은 Theater를 8초 동안 보관한 뒤 삭제하며, 그동안 실행 취소 토스트와 Mod+Z로 되돌릴 수 있습니다. 복원된 Operation은 dormant로 돌아오며 다시 실행할 때만 기동합니다.
- [fleet-console] 커맨드 팔레트에서 Repository 커밋, Files 경로, Plans, Skills를 검색하고, 고르면 해당 패널이 그 위치에서 열립니다.
- [fleet-console] 플러그인이 패널·오퍼레이션·설정·알림 제목을 지역화할 수 있고, 레일 패널이 해석된 로케일을 받습니다.

#### Changed
- [fleet-console] Sort by Status의 4개 섹션을 항상 보여 주고 상태별 접기를 지원하며(빈 섹션은 흐리게 기본 접힘), 그룹 색 점 대신 그룹 이름을 보여주고 Theater 동작 아이콘을 ellipsis로 바꿉니다.
- [fleet-console] 빈 Operations 맵을 대기 중 Operation 칩, New Operation 버튼, 단축키 힌트가 있는 행동 가능한 표면으로 바꿉니다.
- [fleet-console] 컨트롤 라벨과 버튼을 하나의 문법으로 통일하고, 컨트롤·CTA 라벨은 문장형으로 되돌리되 대문자는 구조 섹션 라벨에만 남깁니다.
- [fleet-console] 강조를 낮출 때 투명도로 흐리는 대신 텍스트 단계를 낮춰, 흐린 라벨도 대비를 유지합니다.
- [fleet-console] Activity Rail 폭을 패널별로 기억합니다. Repository를 넓혀도 Alerts가 같은 폭을 차지하지 않습니다.
- [fleet-console] Activity Rail 패널을 각자의 기본 폭으로 엽니다. Repository와 Codex는 Plans, Files, Skills보다 넓습니다.
- [fleet-console] 이미 없는 Operation 삭제 요청에 성공을 돌려주어, 닫기를 반복해도 문제가 없습니다.
- [fleet-console] Carrier 로스터에서 Vanguard를 로컬·원격 통합 정찰 전문가로 안내합니다.
- [fleet-console] 사이드바 상태 섹션을 가장 최근 전이 순으로 정렬하고, 전이되지 않은 Operation은 기존 순서를 유지합니다.
- [fleet-console] IDLE로 전이된 Operation에 세션 한정 미확인 점과 섹션 헤더 수를 표시하고, 한 번 열면 해제합니다.
- [fleet-console] 첫 상태 섹션 헤더를 AWAITING INPUT에서 AWAITING으로 줄였습니다.
- [fleet-console] Settings > General의 언어 카드를 Display language로 바꾸고, 콘솔 전반의 표시 언어라고 설명합니다.
- [fleet-console] Nimitz가 선택적 Plan 보증을 담당하고 Kirov 항목이 없는 5명 Carrier 로스터를 제공합니다.
- [fleet-console] 표시 언어가 콘솔 본체를 지배합니다. 커맨드 밴드, 사이드바, 캔버스, 명령 팔레트, 단축키, Settings, What's New 크롬, Codex가 선택한 언어를 따르며 날짜도 로케일에 맞춥니다.
- [fleet-console] 언어를 바꾸면 새로 고침 없이 즉시 적용됩니다.
- [fleet-console] Genesis가 직접 구현과 Plan 기반 구현을 담당하고 Ohio 항목이 없는 4명 Carrier 로스터를 제공합니다.

#### Fixed
- [fleet-console] 세 테마 모두에서 흐린 인터페이스 텍스트를 올려, 패널 제목·섹션 라벨·메타데이터·컨트롤 문구가 읽히게 합니다.
- [fleet-console] Operation 상태 비콘 4종을 색과 함께 모양으로도 구분합니다.
- [fleet-console] Codex 읽기 시트와 내비게이터에서 의도한 글꼴과 라벨 크기가 표시됩니다.
- [fleet-console] 복원된 Operation을 사이드바 상태축과 Alt 순환에서 IDLE이 아니라 캔버스와 같이 DORMANT로 분류합니다.
- [fleet-console] 캔버스와 사이드바 우클릭 메뉴를 커서 위치에 열고, 아래쪽 공간이 없으면 커서 위로 펼칩니다.

### fleet-plugin

#### Added
- [fleet-console] Repository 워크스페이스 트리 섹션을 접고 펼 수 있습니다. Tags와 Stashes는 기본 접힘이며 접기 상태는 메모리에만 유지됩니다.
- [fleet-console] dormant Resume 클릭에 진행 중 Resuming... 상태, Try again / Start fresh 실패 카드, Resume failed Alerts로 응답합니다. Start fresh는 저장된 provider 세션 없이 새로 시작합니다.
- [fleet-console] 유휴 agent 터미널이 설정한 유휴 시간(기본 1시간)을 넘기면 DORMANT로 전이됩니다. 작업 중이거나 저장된 provider 세션이 없는 세션은 전이되지 않습니다.
- [fleet-console] Settings > Terminal > General에 Idle agent sessions 옵션(Off / 30분 / 1시간 / 2시간 / 4시간)을 추가하고, 값은 Console 서버에 저장됩니다.

#### Changed
- [fleet-console] 좁은 패널에서 Repository History 커밋 제목을 두 줄로 표시하고, 포인터를 올리면 전체 제목을 보여 줍니다.
- [fleet-console] Console Agent 세션에서 호스트 소유 Fleet Plan 작성을 제공하고, Carrier의 Plan 변경은 실패 시 차단합니다.
- [fleet-console] Terminal Carrier 설정과 작업 정체성 스타일에서 Tempest를 제거하고, 이전 정체성에는 중립 폴백을 둡니다.
- [fleet-console] 에이전트 오퍼레이션 하단 스트림 독을 Carrier Streams 컴패니언으로 교체합니다. ANALYZE 위의 STREAMS가 캐리어 행을 열고, 완료된 캐리어는 세션 동안 유지되는 한 줄 스트립으로 접힙니다.
- [fleet-console] Carrier Streams와 Session Analyst를 각자의 엣지 핸들에서 열고, 캐리어 스트리밍 중에는 STREAMS에, 분석 중에는 ANALYZE에 라이브 펄스를 표시합니다.
- [fleet-console] 이전 Kirov 설정과 스트림 소유권은 비활성으로 두고, 설정과 정체성 스타일에서는 Kirov를 빼습니다.
- [fleet-console] 내장 Terminal, Repository, Files, Skills 패널이 표시 언어 설정을 따릅니다.
- [fleet-console] 이전 Ohio 설정과 스트림 소유권은 비활성으로 두고, 설정과 정체성 스타일에서는 Ohio를 빼습니다.

#### Fixed
- [fleet-console] Repository, Skills, Files, Session Analyst 패널의 흐린 텍스트와 체크아웃된 브랜치 밖 커밋 행이 읽히게 합니다.
- [fleet-console] Files 패널 트리 라벨이 의도한 글꼴로 표시됩니다.
- [fleet-console] Files 트리를 화살표·Home·End로 이동하고, Tab은 트리 전체에서 한 번만 멈춥니다.
- [fleet-console] 입력 프롬프트 이후 에이전트가 작업을 재개하면 패널이 다시 실행 중이 되고, 턴이 중단되면 유휴로 내려갑니다.

#### Removed
- [fleet-console] Codex ACP/App Server 실행 모드 선택기를 제거하고, Console에서 시작하는 Codex 세션은 항상 App Server를 사용합니다.

### fleet-core

#### Added
- [core-unified-agent] GPT-5.6과 GPT-5.5용 Fast Codex 모델 자산을 추가하고 App Server `priority` 서비스 티어에 연결합니다.
- [fleet-admiral] Codex 세션이 입력 대기를 Console에 보고합니다.

#### Changed
- [fleet-carriers] Tempest의 GitHub 중심 인텔리전스를 Vanguard의 읽기 전용 로컬·원격 코드베이스 계약에 통합합니다.
- [fleet-admiral] 정찰 디스패치를 일반화된 `objective`, `search_space`, `hints`, `constraints`, `depth` 요청 블록과 함께 Vanguard로 보냅니다.
- [core-unified-agent] 연결이 끊기면 스레드를 아카이브하고 재개할 때 되살려, Codex 세션이 Codex CLI resume picker에 나타나지 않게 합니다.

#### Fixed
- [core-unified-agent] app-server가 아카이브 요청에 응답하기 전에 종료되어도 Codex 세션 정리가 멈추지 않습니다.

#### Removed
- [core-unified-agent] `ait` CLI와 Codex ACP 브리지 지원 및 GPT-5.4 모델 계열을 제거합니다.

#### Breaking Changes
- [fleet-admiral] Fleet Plan 작성·변경은 호스트 소유로 두고 선택적 Plan 보증은 Nimitz를 통해 제공하며, 태스크 완료 표시는 Ohio 전용으로 유지합니다.
- [fleet-carriers] Kirov를 별칭 없이 기본 Carrier 계약과 export에서 제거하고, Nimitz에 정확한 PlanRef 기반 선택적 보증을 추가합니다.
- [fleet-admiral] Kirov 디스패치 라우팅을 제거하고, 호스트와 Ohio의 소유권을 유지하면서 Nimitz를 통해 선택적 `plan_ref` 보증을 제공합니다.
- [fleet-carriers] Ohio를 별칭 없이 기본 Carrier 계약과 export에서 제거하고, Genesis가 Plan 상태를 바꾸지 않은 채 선택적 동일 Lane TaskRef를 실행하도록 확장합니다.
- [fleet-admiral] Plan 변경과 완료의 단독 소유권을 호스트에 주고, Artifact Inspection과 Lane QA 이후 사용할 `plan_mark_tasks`를 호스트에 제공합니다.
- [fleet-plans] 새 Plan에는 Carrier 중립 호스트 완료 정책을 요구하고, 정확한 이전 Ohio 정책은 기존 Plan에서만 lint 호환으로 유지합니다.

## [1.33.0] - 2026-07-23

### fleet-cli

#### Removed
- [fleet-cli] 기본 로스터에서 Chronicle을 제거하고 기본 Carrier 수를 7개로 표시합니다.

### fleet-console

#### Added
- [fleet-console] Theater 행에 상태축 전환(Alt+S)을 추가해 사이드바를 AWAITING INPUT / RUNNING / IDLE / DORMANT로 재편합니다. 세션 한정이며 다시 열면 항상 그룹 축으로 시작합니다.
- [fleet-console] 콘솔 Language 설정(Settings > General)이 이제 Session Analyst에도 적용됩니다.

#### Changed
- [fleet-console] 사이드바 상단 Add Theater 행을 제거하고, Theater 목록 말미에 New Theater 행을 둡니다.
- [fleet-console] Reset view와 Formation 레이아웃 토글을 사이드바에서 사이드바 바로 오른쪽 커맨드 밴드 묶음으로 옮겨, Formation 보기에서도 닿고 Settings에는 나오지 않습니다.
- [fleet-console] Theater 행의 Operation 수 표시를 상태 토글로 바꾸고, 접기를 행 클릭으로 흡수했으며, 액션과 새 Operation 버튼을 하나의 분할 컨트롤로 합칩니다.

#### Fixed
- [fleet-console] Float over Map을 바꿔도 우측 레일이 열리거나 닫히는 애니메이션을 재생하지 않습니다. 패널은 그대로이고 Map만 다시 배치됩니다.
- [fleet-console] 유휴 패널의 사이드바 상태 점을 패널 헤더 비콘과 같은 초록으로 표시합니다.
- [fleet-console] 실시간 목록 갱신 중 Plan 파일이 Loading에 멈추지 않습니다.

#### Removed
- [fleet-console] Chronicle 식별 스타일을 제거하고, 없거나 알 수 없는 Carrier 식별자에는 중립 brass 폴백을 씁니다.

### fleet-plugin

#### Added
- [fleet-console] Repository 레일을 Full Workspace 기본형으로 다시 만듭니다. 소스 트리 옆에 커밋 그래프가 상주하고, 커밋 상세와 diff는 하단에 도킹됩니다.
- [fleet-console] Session Analyst가 콘솔 Language 설정을 따라 패널 문구와 분석 답변을 영어 또는 한국어로 제공하며, auto는 브라우저 언어로 해석됩니다.
- [fleet-console] Session Analyst의 CLI/Model/Effort 선택이 다시 불러온 뒤에도 유지됩니다. Reset은 저장된 기본값을 복원하고, 초기화 중에는 선택기가 잠깁니다.

#### Fixed
- [fleet-console] 하나의 브라우저 이벤트 스트림을 공유해 여러 Operation에서 Session Analyst가 계속 응답합니다.

#### Removed
- [fleet-console] Terminal Carrier 설정과 상태 화면을 7개 Carrier 로스터에 맞춥니다.

### fleet-core

#### Added
- [fleet-analyst] Analyst 세션이 응답 언어 옵션을 받아들이고, 한국어를 고르면 한국어 응답 지시문을 붙입니다.

#### Breaking Changes
- [fleet-admiral] [fleet-carriers] [fleet-wiki] 기본 Carrier 카탈로그에서 Chronicle 페르소나를 제거합니다. 문서 종합과 Fleet Wiki 변경은 이제 호스트가 담당합니다.

## [1.32.0] - 2026-07-22

### fleet-console

#### Added
- [fleet-console] 우측 레일에 Float over Map 토글을 추가해, 패널이 Map 크기를 바꾸지 않고 위에 뜨게 할 수 있습니다. 선택은 유지됩니다.
- [fleet-console] Settings > General에 Reduce panel motion 토글을 추가합니다. 꺼짐은 OS를 따르고, 켜짐은 패널 애니메이션을 끕니다. Console 설정과 함께 저장됩니다.
- [fleet-console] SDK 브라우저 표면에 공용 테마 드롭다운을 추가해, 플러그인 설정도 Console과 같은 읽기 쉬운 팝업을 쓸 수 있습니다.

#### Changed
- [fleet-console] Console 설정, What's New, Cowork의 네이티브 드롭다운을 공용 테마 Select로 바꿔 다크 테마 팝업이 읽히게 합니다.
- [fleet-console] Terminal 캐리어·모델·effort·Task Force 설정과 Session Analyst 입력창, Repository 비교 컨트롤의 네이티브 드롭다운을 공용 테마 Select로 바꿉니다.

#### Fixed
- [fleet-console] Session Analyst, 최대화, Formation 보기가 켜져 있는 동안 Alt+Left와 Alt+Right가 최소화된 Operation 패널을 선택하거나 복원하지 않습니다.
- [fleet-console] 여러 작업이 비슷한 시점에 끝나도 Carrier 완료 System Reminder 메시지가 안정적으로 제출됩니다.

### fleet-plugin

#### Added
- [fleet-console] Session Analyst 입력창이 질문에 따라 최대 6행까지 늘어나고, 작성 중인 초안은 패널을 닫았다 열어도 유지됩니다.
- [fleet-console] 분석가가 작업 중에도 질문을 대기열에 쌓을 수 있습니다. Enter가 취소 가능한 QUEUED 칩으로 적재하고, 실행이 끝날 때마다 쌓인 순서대로 보내며, Stop이나 Reset은 대기열을 비웁니다.
- [fleet-console] 답변이 끝날 때마다 후속 제안 칩이 나타나, 첫 질문 이후에도 분석가의 기능을 계속 발견할 수 있습니다.
- [fleet-console] 입력창에 /를 치면 분석 명령 팔레트(/now /drift /brief /risks /timeline)가 열리고, 명령을 고르면 템플릿 질문이 채워집니다.
- [fleet-console] Session Analyst 채팅에 아티팩트 생성 중 라이브 저작 카드를 보여 주고, 게시되면 Open in Artifacts가 있는 카드로 바꿉니다.
- [fleet-console] Repository 패널의 Repositories 소스에 검색 바가 생깁니다. 일치 글자 강조, Enter로 첫 결과 열기, 스캔 깊이와 결과 건수가 한 줄에 있습니다.

#### Changed
- [fleet-console] 캐리어 설정이 캐리어별 Save/Discard 대신 변경 즉시 저장됩니다. Task Force 백엔드 제거는 비활성화될 때 경고하는 두 단계 확인을 거칩니다.
- [fleet-console] 캐리어 설정에 초안 상태가 없어, 캐리어 칩을 바꿔도 저장하지 않은 편집이 사라지지 않습니다.
- [fleet-console] Repository 패널 선택은 중립 워시를 쓰고, brass는 현재 브랜치와 HEAD 배지 같은 위치 신호에만 씁니다.
- [fleet-console] Repository 패널의 밴드와 입력이 각 테마 표면을 따라, 패널 내부가 Console 나머지와 맞습니다.
- [fleet-console] diff의 추가·삭제 행이 하나의 상태 색으로 읽히고, 구문 색은 context 행에만 남습니다.
- [fleet-console] Repository History는 전체 브랜치 그래프를 유지하면서 각 커밋 행을 그 시점의 활성 레인에 맞춰, 오래된 브랜치가 최신 HEAD 행을 넓히지 않습니다.
- [fleet-console] 저장소나 워크트리를 고르면 브랜치·태그와 같이 History로 이동하며, 하단 스캔 깊이 푸터는 제거됩니다.
- [fleet-console] 에이전트 패널의 캐리어 스트림 스트립이 작은 라이브 캡슐로 바뀝니다. 겹친 캡틴 표시, 라이브 활동, 최신 출력 줄이 경과 시간·토큰 추정과 함께 표시됩니다.
- [fleet-console] 펼친 스트림덱은 캐리어마다 단계 카드를 보여 줍니다. 정체성, Reasoning·Using a tool·Writing 같은 라이브 단계, 한 줄 미리보기가 로그 행을 대체합니다.
- [fleet-console] Details의 Activity 탭이 캐리어 출력을 스트리밍 마크다운과 대상이 적힌 도구 상태 칩으로 보여 줍니다. thinking은 접이식 폴드 뒤에 남습니다.

#### Fixed
- [fleet-console] 좁은 폭에서 넓은 마크다운이 자기 블록 안에서 스크롤되어 Session Analyst 채팅의 가로 스크롤이 사라집니다.
- [fleet-console] 터미널 세션이 닫히거나 쓰기에 실패하면 지연된 System Reminder 입력이 더 이상 제출되지 않습니다.

### fleet-core

#### Changed
- [fleet-admiral] 호스트는 선택적 전달 지연으로 PTY 메시지 텍스트와 제출 키를 분리할 수 있으며, 기존 호출자의 동작은 그대로입니다.

## [1.31.0] - 2026-07-21

### fleet-console

#### Added
- [fleet-console] 커맨드 팔레트에서 >를 치면 명령 모드로 전환되어 Theater 전환, 새 Operation 실행, 패널 열기, Settings 열기를 실행합니다.
- [fleet-console] 커맨드 밴드 경로에서 활성 Theater와 Operation을 전환합니다. 각 조각이 Theater 목록 또는 해당 Theater의 Operation 드롭다운을 열고, 키보드로 고를 수 있습니다.

#### Changed
- [fleet-console] 사이드바 그룹 정체성을 항상 보이는 색 표시와 은은한 그룹 존 워시로 표현하며, 그룹을 접으면 더 강해집니다.
- [fleet-console] 패널 전환이 하나의 모션을 공유합니다. Formation·최대화·복원은 미끄러지고, 최소화는 사이드바 칩 쪽으로 사라집니다. 모든 모션은 모션 감소 설정을 존중합니다.

#### Fixed
- [fleet-console] 사이드바에서 Theater를 바꾼 뒤 커맨드 밴드 경로가 다른 Theater의 Operation을 보여 주던 문제를 고치고, 대신 Select operation 트리거를 제공합니다.

### fleet-plugin

#### Added
- [fleet-console] Repository 패널에 읽기 전용 Compare 소스가 생깁니다. 두 ref를 골라 merge-base 기준 변경 파일 목록과 파일별 hunk를 살펴봅니다.

#### Changed
- [fleet-console] Repositories 목록을 중첩 저장소의 디렉터리 트리로 보여 주고, 알파벳 정렬과 단일 자식 폴더 압축을 지원하며 Theater 루트는 상단에 고정합니다.
- [fleet-console] Repository 패널 색을 다시 맞춥니다. 기록 그래프 레인과 diff 구문 강조가 테마별 정체성 톤을 쓰고, 장식용 신호색을 제거합니다.

#### Fixed
- [fleet-console] Repository 패널 테두리를 통일해 Maritime·Carbon 테마에서도 같은 굵기를 유지하고, 꺼져 있던 히스토리 탭 전환을 복원합니다.

### fleet-core

#### Changed
- [fleet-wiki] 이제 호스트가 Fleet Wiki 항목을 직접 스테이징하고 승인합니다. 위키 변경·스테이징·lint·스키마 도구는 호스트 전용이고, 읽기 전용 도구만 캐리어에 공유됩니다.
- [fleet-carriers] Chronicle 캐리어는 더 이상 Fleet Wiki 항목을 다루지 않고 코드베이스 문서화만 담당합니다.
- [fleet-admiral] Carrier dispatch가 확정된 결정값을 리터럴로 전달하도록 요구합니다.
- [core-unified-agent] OpenCode Go와 Cursor Agent 모델 카탈로그를 실제 CLI 기준으로 갱신합니다. OpenCode Go 기본값은 `opencode-go/deepseek-v4-flash`이고, Cursor Agent는 90개 모델을 지원하며 구세대는 퇴역합니다.

## [1.30.0] - 2026-07-20

### fleet-console

#### Added
- [fleet-console] Companion 패널이 기본 숨김 슬롯을 선언할 수 있고, 플러그인이 패널별 표시를 전환합니다. 캔버스는 슬롯 변화를 모션 감소 설정을 존중하며 보여 줍니다.

#### Fixed
- [fleet-console] 커맨드 팔레트로 Operation을 이동하면 대상 터미널이 바로 포커스를 받아 즉시 입력할 수 있습니다.
- [fleet-console] 새로고침 후 커맨드 팔레트로 처음 이동해도 선택한 Operation이 활성화되어 패널이 뜨고 바로 입력할 수 있습니다.

### fleet-plugin

#### Added
- [fleet-console] Settings for Kimi에 Kimi 기본 모델·effort 선택을 추가해, 캐리어 모델을 명시하지 않은 새 Kimi 세션에 적용합니다.
- [fleet-console] Repository 패널을 Repositories 목록의 Theater 하위 저장소나 Worktrees 목록의 활성 저장소 워크트리로 전환하고, 선택을 Theater별로 유지합니다.
- [fleet-console] 활성 저장소와 브랜치를 Repository 패널 상단에 표시하고, 하위 컨텍스트 선택 중임을 함께 나타냅니다.
- [fleet-console] Repositories 목록의 탐색 깊이를 1단계에서 8단계까지 고르고, 탐색 상한에 걸렸는지 확인합니다.

#### Changed
- [fleet-console] Session Analyst가 두 칸으로 열리고, 분석가가 첫 artifact를 발행할 때에만 Artifacts가 나타납니다.
- [fleet-console] 채팅의 ARTIFACTS 칩으로 Artifacts를 여닫고, 개수를 보여 주며, 숨김 중 새 artifact가 오면 알리고, 수동으로 닫은 뒤에는 비워질 때까지 자동으로 열지 않습니다.

#### Fixed
- [fleet-console] Session Analyst 세션 상한을 제거하고, 각 Analyst를 해당 Operation이 끝날 때까지 유지합니다.
- [fleet-console] Linux에서 열린 디렉터리만 감시해 File Explorer가 멈추지 않고 응답합니다.
- [fleet-console] 테마 토큰 때문에 투명하게 남아 있던 Repository 패널 소스 내비게이션 배경을 복원합니다.

### fleet-core

#### Added
- [core-agent] [core-infra] [fleet-admiral] [fleet-carriers] 캐리어별 모델이 없을 때 선택한 프로바이더 기본 모델에서 Kimi 실행 환경을 파생합니다.

#### Changed
- [core-unified-agent] Kimi 모델 레지스트리를 공식 Kimi Code 문서에 맞춥니다. K3 effort low/high/max(기본 high), fable 티어 매핑, 모델별 컨텍스트 윈도우.

#### Fixed
- [fleet-wiki] Windows에서 읽기 전용 파일 때문에 모든 위키 도구가 막히던 위키 워크스페이스 마이그레이션을 고칩니다.
- [fleet-wiki] Windows에서 wiki_read의 source 및 related 항목 경로를 정슬래시로 정규화해 store 인덱스 규약과 맞춥니다.

## [1.29.0] - 2026-07-19

### fleet-console

#### Added
- [fleet-console] 사이드바 Theater 헤더를 드래그해 순서를 바꾸고, 수동 정렬을 서버에 저장합니다.

#### Changed
- [fleet-console] Fleet Wiki Cowork 어시스턴트 응답을 스트리밍 업데이트, 코드 블록, 다이어그램을 지원하는 작은 Markdown으로 렌더합니다.
- [fleet-console] Cowork 주석의 편집 지시를 신뢰할 수 없는 Wiki 선택 텍스트와 구조적으로 분리합니다.

#### Fixed
- [fleet-console] 최소화된 사이드바 Operation 이름이 세 테마 모두에서 읽히는 대비를 갖도록 복원합니다.
- [fleet-console] Session Analyze에서 Operation을 바꿔도 분석 화면을 유지하고, 종료하면 이전 Map, Formation 또는 최대화 화면을 복원합니다.

#### Removed
- [fleet-console] Command Band와 Activity Rail에서 Context Chip과 Theater 하위 경로 컨텍스트 제어를 제거합니다.

### fleet-plugin

#### Changed
- [fleet-console] Session Analyst 아티팩트를 일반 웹페이지처럼 렌더해 SVG, canvas, 인라인 JavaScript가 브라우저처럼 동작합니다.
- [fleet-console] Session Analyst 어시스턴트 응답을 선택 가능한 작은 Markdown으로 렌더하고, 사용자 프롬프트는 일반 텍스트로 둡니다.
- [fleet-console] 모든 Activity Rail 플러그인을 활성 Theater 루트에서 동작시키고 Theater별 Shell 세션을 유지합니다.

#### Fixed
- [fleet-console] Kimi (Claude Code) 실행 항목에 Claude와 공유하던 아이콘 대신 전용 Kimi 심볼을 줍니다.
- [fleet-console] Session Analyst 아티팩트 캔버스가 활성 Console 테마를 따릅니다.

### fleet-core

#### Changed
- [fleet-admiral] 기본 Admiral 프롬프트를 간결하게 유지하면서 Fleet Wiki 운영 정책을 필요할 때 로드합니다.
- [fleet-carriers] Chronicle 라우팅 메타데이터를 간결하게 유지하고, 온디맨드 스킬에서 Wiki 권한 경계를 보존합니다.
- [fleet-analyst] Session Analyst가 의도 드리프트를 판정할 때 확정된 사용자 의도와 충돌 행동을 모두 인용하고, 증거가 부족하면 기권하며, 비구속적 조언으로만 보고합니다.
- [fleet-admiral] 새 worktree 안에 고정된 의존성을 설치하고, Carrier 위임 전에 패키지 typecheck와 build 사전 검증을 요구합니다.
- [fleet-analyst] Session Analyst가 정체성과 기능 질문에는 세션 히스토리를 읽지 않고 답하고, 세션 질문에는 증거 기반 분석을 유지합니다.
- [fleet-wiki] Fleet Wiki Cowork가 직접 질문에는 초안을 읽지 않고 답하며, 요청된 초안 작업에 필요한 도구만 사용합니다.

## [1.28.0] - 2026-07-19

### fleet-cli

#### Changed
- [fleet-cli] 소스에서 허용된 캐리어에만 Task Force 구성을 표시합니다.

#### Fixed
- [fleet-cli] 캐리어가 워크트리에서 실행되어도 Fleet Plan 저장소를 최초 실행 워크스페이스에 유지합니다.

### fleet-console

#### Added
- [fleet-console] Plans 패널이 실시간으로 갱신됩니다. 플랜 파일이 바뀌면 목록·진행률·열린 리더가 새로고침됩니다.
- [fleet-console] Plans 웨이브·레인 칩을 누르면 문서의 해당 섹션으로 이동합니다. 플랜 목록은 ArrowUp/ArrowDown, Enter, Escape를 지원합니다.
- [fleet-console] Plans 목록에 검색, ALL/IN PROGRESS/COMPLETE 상태 필터, REFRESH, 플랜별 상대 경로 복사가 추가됩니다.
- [fleet-console] Codex 위키 엔트리를 읽기 화면에서 AI로 편집합니다. 텍스트를 골라 주석을 달고 플로팅 도크에서 일괄 전송한 뒤, 문서 diff를 확인하고 그 자리에서 반영하거나 폐기합니다.
- [fleet-console] Cowork 편집을 터미널 없는 원샷 에이전트 실행으로 수행하고, 엔트리별 초안 하나에 누적한 뒤 버전 검증을 거쳐 위키 패치 파이프라인으로 한 번에 반영합니다.
- [fleet-console] Stream Deck Details에 Request와 Activity 탭을 추가하고, 브라우저 보안 경계를 지키며 Request Block을 표시합니다.
- [fleet-console] 컴패니언 패널 레이아웃을 추가합니다. 에이전트 Operation이 터미널 옆에 전용 패널을 열 수 있고, 레이아웃을 닫으면 Map 상태가 완전히 복원됩니다.
- [fleet-console] Codex 사이드바에서 Wiki 스키마 카탈로그를 탐색할 수 있습니다.

#### Changed
- [fleet-console] 저장된 Diff·History 패널 선택을 통합 repository 패널로 옮깁니다.
- [fleet-console] Cowork 수정 피드백을 접을 수 있는 스트리밍 활동 패널, 통일된 입력 컨트롤, 더 분명한 진행·정지 상태로 개선합니다.
- [fleet-console] Operation 정체성 색을 패널 테두리에서 좌측 스파인과 명판 마크로 옮기고, 테두리·글로우는 상태 신호 전용입니다. 기존 악센트·그룹 색 키는 자동 매핑됩니다.
- [fleet-console] 크롬 색을 가라앉힙니다. 환경·정보 뱃지는 중립 잉크, 스트림 팔로우 버튼은 brass 윤곽, 미니맵의 상시 발광은 사라지고, 버튼은 하나의 컨트롤 문법으로 모입니다.

#### Fixed
- [fleet-console] 패널 안 닫기 버튼의 포커스 링이 잘리지 않고 완전히 보입니다.
- [fleet-console] 에이전트가 캐리어 워크트리에서 실행되어도 Fleet Plan을 활성 Theater에서 계속 볼 수 있습니다.
- [fleet-console] 패널 안에서 이름을 편집할 때 입력 중인 내용이 보입니다.
- [fleet-console] Activity Rail 크기를 조절해도 Command Band의 점진 동작이 화면 폭만 따릅니다.

### fleet-plugin

#### Added
- [fleet-console] Repository 패널에서 로컬·원격 브랜치, 태그, 스태시, 워크트리를 현재 표시와 함께 나열합니다.
- [fleet-console] Repository 패널의 워크트리 행을 선택해 theater 경로 컨텍스트를 전환합니다.
- [fleet-console] Terminal Agent 스냅샷과 다시 불러오기를 거쳐도 Carrier 요청 관찰 정보를 보존합니다.
- [fleet-console] Session Analyst를 추가합니다. ANALYZE 핸들로 Claude, Kimi, Codex, OpenCode, Cursor 중 하나를 고른 트랜스크립트 기반 분석 채팅을 열고, 스트리밍 진행과 샌드박스 HTML 아티팩트를 제공합니다.

#### Changed
- [fleet-console] Diff·History 레일 패널을 WORKING·REFS 소스 내비게이션을 갖춘 읽기 전용 Repository 단일 패널로 통합합니다.
- [fleet-console] History 최상단에 미커밋 변경 행을 고정하고, 검증된 브랜치·태그 ref로 이력을 필터하는 제거 가능한 칩을 제공합니다.
- [fleet-console] Console의 Task Force 설정을 Nimitz, Vanguard, Tempest로 제한하고 지원하지 않는 변경을 거부합니다.
- [fleet-console] repository, skills, terminal carrier 표면을 중립 뱃지와 통일 컨트롤 문법에 맞춥니다.
- [fleet-console] Terminal 플러그인 설정을 Metaphor, Terminal Font, Terminal Renderer를 담는 General 섹션으로 묶고, Kimi 로그인 카드를 Settings for Kimi로 바꿔 Agent CLI Available 아래로 옮깁니다.

#### Fixed
- [fleet-console] 파일시스템 경로를 브라우저에 노출하지 않고 Terminal Agent의 Plan 저장소를 서버가 해석한 Theater에 묶습니다.
- [fleet-console] Session Analyst 채팅이 최신 스트리밍 메시지와 활동 업데이트를 따라갑니다.

### fleet-core

#### Added
- [fleet-wiki] 터미널 없는 AI 초안 편집을 위한 cowork 엔진을 추가하며, Fleet Console이 이를 사용합니다.
- [fleet-carriers] 실행기 입력이나 모델 사용량을 바꾸지 않고 구조화된 요청 관찰 정보를 전달합니다.
- [fleet-analyst] fleet-analyst 패키지를 추가합니다. 트랜스크립트 인덱싱, 자격증명 마스킹, 경계 있는 MCP 분석 도구, 멀티턴 분석 세션을 갖춘 세션 분석 런타임입니다.
- [fleet-wiki] 스키마 목록, 조회, 신규 템플릿 생성 전용 MCP 도구를 추가합니다.
- [fleet-admiral] 스키마 조회는 Chronicle에, 템플릿 생성은 Admiral에 배정합니다.

#### Changed
- [fleet-carriers] Task Force 구성, 상태, 출격에 명시적인 캐리어 기능 정책을 강제합니다.
- [fleet-admiral] Codex 리뷰 피드백을 고정된 제품 맥락에 따라 판정하고, 머지 전에 리뷰로 인한 범위 이탈을 롤백합니다.

#### Fixed
- [core-agent] 불변 서버 전용 바인딩을 전용 MCP 세션과 일회성 MCP 세션 모두에 전달합니다.
- [fleet-plans] Plan 저장소를 실행 디렉터리에서 추론하지 않고 호스트가 명시적으로 바인딩하도록 합니다.
- [fleet-admiral] Claude Console 패널 이름을 첫 프롬프트로 자동 지정한 뒤 provider summary로 갱신합니다.
- [fleet-analyst] 누락되거나 예상하지 못한 publish_artifact 인자를 거부하고 비어 있지 않은 html을 요구하며, HTML 및 대비 계약을 명확히 합니다.

## [1.27.0] - 2026-07-18

### fleet-cli

#### Added
- [fleet-cli] Kimi via Claude Code 세션, 공유 로그인 명령, System Menu 인증, Carrier 백엔드 선택을 추가합니다.
- [fleet-cli] 호스트에는 workspace 범위의 Fleet Plan 읽기·검증 도구를, Carrier executor에는 권한별 Plan 도구를 제공합니다.

#### Changed
- [fleet-cli] 프로젝트별 영구 워크스페이스에서 Wiki 도구와 지식 개수를 확인합니다.

### fleet-console

#### Added
- [fleet-console] Agent CLI 설정에 Kimi API 키 등록과 로그인 상태 기반 실행 가능 여부를 추가합니다.
- [fleet-console] Alt를 누르고 있는 동안 모든 가시 패널의 세션 제목을 한눈에 보여주는 glance HUD를 띄웁니다.
- [fleet-console] 전체 화면에서 명령 밴드를 자동으로 숨기고, 가장자리·키보드·고정·모션 감소 제어를 지원합니다.
- [fleet-console] 개발 채널 콘솔의 Command Band에 Local 칩을 표시하고, 버전·포트·데이터 루트·런타임 lock 경로를 담은 Environment 팝오버를 제공합니다. 프로덕션 채널에는 표시가 없습니다.
- [fleet-console] Desktop 셸의 Environment 팝오버에 파생 Desktop data 경로 행이 추가되고, macOS Desktop은 창 컨트롤 옆에 맞도록 칩 라벨을 Local로 줄입니다.

#### Changed
- [fleet-console] Carrier Settings를 System Menu 독립 페이지에서 Settings > Plugins > Terminal > Carriers로 옮깁니다. `/carrier-settings`는 한 릴리스 동안 리다이렉트로 유지하고, Settings에 `?section=` 딥링크를 지원합니다.
- [fleet-console] 비활성 Theater에서도 전체 Operations와 Group을 표시하고 Group 접힘 상태를 유지합니다.
- [fleet-console] Operations 패널 세션 정체성을 패널 테두리에 걸치는 명판으로 옮겨 활성 패널에서도 이름이 보이게 하고, 창 컨트롤은 hover나 키보드 포커스 때 꺼냅니다.
- [fleet-console] provider 세션 정체성 메타데이터로 Operation 이름을 지정하고, Console 사용자 이름 변경이 우선합니다.
- [fleet-console] Console Plan을 저장소 로컬 `.fleet/plans` 대신 공유 Fleet data directory의 Theater root workspace에서 읽습니다.
- [fleet-console] 기존 `.fleet/knowledge`를 삭제하지 않고 필요할 때 복사 마이그레이션하며, 프로젝트별 theater-wide Codex 지식을 제공합니다.

#### Fixed
- [fleet-console] Alt+Left와 Alt+Right 탐색이 최소화되지 않은 패널 안에서만 이동합니다.
- [fleet-console] 패널을 포커스하거나 복원·최소화할 때 기존 Map 또는 Formation 상태를 보존합니다.

### fleet-desktop

#### Added
- [fleet-console] SSH로 원격 런타임에 연결합니다. Desktop이 원격 머신에 Fleet Console을 설치·구동하고 SSH 터널로 접속하며, Linux와 macOS 호스트를 지원합니다.
- [fleet-console] 원격 연결 진행을 부트스트랩 화면에 표시하고, 메뉴에서 로컬 런타임으로 돌아가며, 연결 실패를 대화상자로 안내합니다.
- [fleet-console] 네이티브 창의 전체 화면 상태를 전달해 Console 크롬이 Desktop 전체 화면을 따릅니다.
- [fleet-console] macOS 메뉴 막대에 시그니처 트레이 아이콘을 두고, 클릭하면 Desktop 창을 보여 줍니다.

#### Fixed
- [fleet-console] 앱이 살아있는 상태에서 Desktop 창을 닫은 뒤 업데이트 대화상자가 파괴된 창을 부모로 삼지 않습니다.

### fleet-plugin

#### Added
- [fleet-console] Terminal 플러그인이 captain 칩 스트립과 단일 상세 카드로 런타임 편집기, Task Force 편집, 초안 저장·폐기를 갖춘 Carriers 설정 섹션을 제공합니다.
- [fleet-console] Kimi 인증 정보를 서버에만 보관하면서 브라우저에는 안전한 로그인 상태와 보호된 Terminal 실행을 제공합니다.
- [fleet-plans] Terminal에서 실행한 호스트 및 Carrier agent에 workspace 범위의 Fleet Plan 도구를 제공합니다.

#### Changed
- [fleet-console] provider 세션 정체성 마커를 서버 전용으로 두고, 턴과 세션 교체 사이에서 제목을 안전하게 갱신합니다.
- [fleet-console] Terminal 에이전트의 Wiki 도구를 동일한 프로젝트별 영구 워크스페이스로 연결합니다.

#### Fixed
- [fleet-console] Terminal agent runtime이 Carrier 상태를 읽을 때 Console 데이터 디렉터리 재정의를 따릅니다.
- [fleet-console] History 커밋 상세와 변경 파일 스크롤이 inspector 전체 높이를 사용합니다.

### fleet-core

#### Added
- [core-infra] [core-unified-agent] [fleet-admiral] 공유 claude-kimi 백엔드, 최신 모델 메타데이터, 인증 정보 검증, Carrier 인증 주입을 추가합니다.
- [core-infra] Fleet data directory 아래에서 안정적인 크로스 플랫폼 workspace 디렉터리를 해석하고, cwd identity의 충돌과 안전하지 않은 경로를 막습니다.
- [fleet-plans] 결정론적 PlanRef 및 TaskRef 저장, lint, compact 실행 조회, 태스크 완료 마킹, Plan 상태 검증 도구를 추가합니다.
- [fleet-carriers] Kirov가 Plan 도구로 검증된 Plan을 작성하고, Ohio가 dispatch마다 한 번의 compact Plan 조회로 명시적인 동일 Lane TaskRef를 실행합니다.
- [fleet-wiki] 호스트가 주입하는 Wiki 워크스페이스 resolver와 1회 복사 마이그레이션을 추가합니다.

#### Changed
- [fleet-admiral] [fleet-carriers] 항상 주입되는 Admiral 시스템 프롬프트를 더 줄입니다. 디스패치 조성 규칙은 온디맨드 carrier-operations skill(기존 carrier-contracts에서 이름 변경)로 이관됩니다.
- [core-unified-agent] Agent CLI 실행 시 provider 중립 세션 정체성 resolver를 결합하고 Codex prompt 자동 이름 지정을 유지합니다.

#### Removed
- [fleet-carriers] 캐리어 dispatch의 프로세스 전역 동시 실행 제한을 제거합니다.

## [1.26.2] - 2026-07-15

### fleet-core

#### Fixed
- [fleet-admiral] Windows의 Fleet 관리 Codex 훅에 `command_windows` PowerShell 재정의를 사용합니다.

## [1.26.1] - 2026-07-14

### fleet-desktop

#### Fixed
- [fleet-console] 터미널의 HTTP 및 HTTPS 링크를 외부 브라우저에서 열고, 웹이 아닌 스킴은 차단합니다.

### fleet-plugin

#### Changed
- [fleet-console] Console 터미널 렌더링을 xterm 6으로 올리면서 수동 스크롤백과 테마 기반 viewport 채움을 유지합니다.

#### Fixed
- [fleet-console] 탐색 경고를 유지하면서 검증된 OSC 8 목적지를 최초 브라우저 요청에 전달합니다.
- [fleet-console] 마우스 드래그 후 로컬 터미널 선택 내용을 클립보드에 복사합니다.
- [fleet-console] Windows에서 선택한 터미널 텍스트를 브라우저 DevTools 실행이나 활성 CLI 중단 없이 Ctrl+Shift+C로 복사합니다.

### fleet-core

#### Fixed
- [fleet-admiral] Windows의 Codex 세션에서 여러 줄 입력을 다시 사용할 수 있습니다.

## [1.26.0] - 2026-07-13

### fleet-cli

#### Fixed
- [fleet-cli] Windows에서 carrier-result 리마인더가 프롬프트에 남지 않고 Codex TUI에 제출됩니다.

### fleet-console

#### Added
- [fleet-console] 별도 Console 기능 모드 없이 선택적 Desktop 감독을 위한 안정적인 루프백 페어링 식별자를 제공합니다.
- [fleet-console] Formation view에 Grid / Columns / Rows 레이아웃 선택기를 추가합니다. Columns는 울트라와이드 모니터용으로 패널을 세로 칸으로 나누며, 선택은 기기별로 기억됩니다.

#### Changed
- [fleet-console] 미니맵 접기 컨트롤을 복원하고, Formation 및 패널 최대화 중에는 Map 화면을 숨깁니다.
- [fleet-console] 사이드바 액션 컨트롤을 hover 또는 키보드 포커스에서만 표시해 Operation 이름 공간을 넓힙니다.
- [fleet-console] 캐리어 스트림 독을 오버레이로 띄워, 펼쳐도 터미널 크기가 바뀌지 않습니다.
- [fleet-console] 레이아웃 버튼으로 Formation view에 들어가고, Reset view를 사이드바 인라인 버튼으로 옮기며, 캔버스 우클릭 메뉴를 사이드바와 같은 Launch 목록으로 통일합니다.

#### Fixed
- [fleet-console] 우측 레일의 긴 Codex Wiki 엔트리 목록에서 마우스 휠 스크롤을 복원합니다.
- [fleet-console] Console과 좌우 사이드 크롬의 크기를 조절할 때 Command Band 컨텍스트가 점진적으로 중앙에 머물게 합니다.

#### Removed
- [fleet-console] 최소화된 패널을 포함해 Formation view를 여는 Alt+Shift+F 단축키를 제거합니다.
- [fleet-console] 대화형 Operation 실행 컨트롤과 Console 세션 캡처에서 Cursor를 제거합니다.

### fleet-desktop

#### Added
- [fleet-console] Desktop이 정상 Console 시작을 완료한 뒤, macOS 앱 메뉴 또는 Windows와 Linux 트레이에서 실행 중인 로컬 Fleet Console에 연결합니다.

### fleet-plugin

#### Changed
- [fleet-console] 대규모 디렉터리 목록을 표시할 때 File Explorer의 응답성을 개선합니다.

#### Fixed
- [fleet-console] 터미널 helper가 실행 권한 없이 설치된 경우에도 macOS Terminal PTY 시작을 복구합니다.
- [fleet-console] Windows ConPTY에서 터미널 carrier-result 리마인더가 Codex에 안정적으로 제출됩니다.

### fleet-core

#### Removed
- [fleet-admiral] Carrier용 Cursor 백엔드는 유지하면서 Fleet Admiral의 Cursor 실행 주입과 플러그인 렌더링을 제거합니다.

## [1.25.0] - 2026-07-12

### fleet-console

#### Added
- [fleet-console] `Mod+B`와 `Mod+Alt+B` 단축키로 좌측 사이드바와 우측 Activity Rail을 토글합니다.
- [fleet-console] 좌측 사이드바 operation 칩에 최소화 버튼을 추가해 사이드바에서 패널을 최소화할 수 있습니다. 이미 최소화된 칩과 비활성 Theater의 preview 칩에는 숨깁니다.
- [fleet-console] 비활성 Operation 이름을 패널 상태 비콘 옆에 표시하고 인라인 이름 변경을 제공합니다.

#### Changed
- [fleet-console] 사이드바 Formation view 토글을 두 칸으로 나눠, 열린 패널만과 최소화 패널 포함 Formation을 각각의 버튼으로 제공합니다.
- [fleet-console] What's New를 Overview 및 제품 탭으로 구성하면서 레거시 및 혼합 릴리스 업데이트를 보존합니다.
- [fleet-console] Formation view의 열린 패널만 세그먼트는 최소화·최대화로 도크에 내려간 패널을 그대로 두고 현재 열린 패널만 정렬합니다. 최소화 포함 세그먼트는 여전히 모든 패널을 먼저 복원합니다.
- [fleet-console] 세션 중 각 Theater를 처음 열 때 기존 Operation 패널을 최소화된 상태로 시작하고, 선택한 패널만 단독으로 나타냅니다.
- [fleet-console] 기본 캔버스 화면에서 미니맵을 항상 표시하고 Map 접기 버튼을 제거합니다.

#### Fixed
- [fleet-console] 릴리스 노트가 모달 본문을 넘쳐도 What's New 제어 버튼이 계속 보입니다.

### fleet-desktop

#### Added
- [fleet-console] 저장된 Fleet Console 테마와 실시간 테마 변경에 맞춰 Fleet Desktop Windows 제목 표시줄 오버레이를 동기화합니다.
- [fleet-console] 영속 줌 수준을 사용하는 네이티브 Console 확대와 새로 고침 제어를 추가합니다.

#### Fixed
- [fleet-console] macOS 로그인 셸 PATH에서 Agent CLI 검색을 복원합니다.
- [fleet-console] Console 전환 후 뒤로가기로 부트스트랩 페이지가 다시 열리지 않습니다.

### fleet-plugin

#### Changed
- [fleet-console] Diff History 커밋 뷰를 Details 탭과 파일별 diff Changes 탭을 갖춘 Segmented Commit Inspector로 개편합니다.
- [fleet-console] 기본 History 레일 폭에서도 커밋 제목이 잘리지 않게 하고, 브랜치 그래프를 좌측 마스터로 유지하며, 그래프·인스펙터·파일 창 사이에 크기 조절 구분자를 추가합니다.
- [fleet-console] 새 Codex Agent CLI 설정의 기본값을 App Server로 지정하며, 명시적인 ACP 선택은 유지합니다.

#### Fixed
- [fleet-console] Diff History 커밋 그래프 노드를 해당 커밋 행에 맞춥니다.
- [fleet-console] 스트리밍 상태 패널이나 다른 레이아웃 변경으로 터미널 크기가 바뀌어도 스크롤 위치를 보존합니다.

### fleet-core

#### Changed
- [fleet-carriers] 완료된 캐리어 작업 정보를 6시간 동안 보존합니다.
- [core-unified-agent] 시작 모드 재정의가 없으면 Codex 연결에 App Server를 사용합니다.
- [core-unified-agent] Claude와 Codex의 Carrier 지침을 제출 프롬프트에 함께 전달하고, Kirov 디스패치가 플랜 파일을 지정하고 생성하도록 요구합니다.
- [fleet-carriers] 각 Carrier 디스패치를 새 CLI 프로세스에서 실행하고 `context_id`를 반환하며, 호출자가 이를 `resume_context_id`로 전달하면 실제 프로바이더 세션을 재개합니다.

## [1.24.0] - 2026-07-11

### fleet-cli

#### Added
- [fleet-cli] 새 캐리어 세션을 열 때 저장된 Codex 시작 선택을 적용합니다.

#### Removed
- [fleet-cli] 더 이상 지원하지 않는 `--native` 터미널 전용 시작 모드를 제거하여 Fleet CLI가 항상 임베디드 2개 패널 앱을 엽니다.

### fleet-console

#### Added
- [fleet-console] 원자적 UI 글꼴 및 크기 환경설정을 갖춘 Typography Font Browser를 추가합니다.
- [fleet-console] 엄선된 Manrope, JetBrains Mono, Source Code Pro 선택지를 제공하는 서버 저장 전역 UI 글꼴 환경설정을 추가합니다.
- [fleet-console] 활성 Theater의 실행 계획을 웨이브/작업 진행률과 병렬 레인 디스패치 준비 상태로 보여 주고, 인패널 마크다운 리더에서 여는 Plans 활동 레일 패널을 추가합니다.
- [fleet-console] 표시되는 Operations를 감독할 수 있는 임시 Formation 보기를 추가합니다.
- [fleet-console] Theater 루트, 워크트리 또는 디렉터리를 선택하는 공유 Activity Rail 경로 컨텍스트를 추가합니다.
- [fleet-console] 창 크롬을 Command Band로 통합합니다. 사이드바·Activity Rail 토글, Operation 검색, Formation view 토글이 패널을 접어도 움직이지 않는 고정 위치에 상주합니다.
- [fleet-console] 사이드바와 Activity Rail의 접힘/펼침에 폭 전환 애니메이션을 추가하며, 모션 감소 환경에서는 완전히 끕니다.
- [fleet-console] 플로팅 에지 펼침 탭과 사이드바 헤더 버튼 행을 퇴역시키고, Add Theater는 사이드바 최상단 전폭 행으로 이동합니다.
- [fleet-console] Fleet 브랜드 마크를 Command Band로 옮겨 Operations 홈 버튼으로 삼고, macOS 신호등 인셋을 넓힙니다.
- [fleet-console] 밴드 중앙을 활성 Theater, 더블클릭 이름 변경이 가능한 활성 패널 이름, 아이콘과 텍스트 태그로 표시되는 패널 CLI의 경로로 바꿉니다.
- [fleet-console] 패널 hover 컨트롤을 상태 표시와 최소화·최대화·닫기만으로 줄입니다.
- [fleet-console] Activity Rail의 경로 컨텍스트 칩을 Command Band에도 노출해 동기화된 두 번째 표면으로 제공합니다.
- [fleet-console] 사이드바 브랜드 푸터를 System Menu와 Help 드롭업으로 재편하고, Keyboard Shortcuts를 Help 모달로 옮기며 캔버스 컨텍스트 메뉴에서는 제거합니다.
- [fleet-console] Settings와 Carriers 화면에도 브랜드와 검색만 활성화된 Command Band를 유지하고 Operations로 돌아가기 링크를 퇴역시킵니다.
- [fleet-console] 서버에 저장되는 언어 환경설정과 함께 영어 및 한국어 What's New 릴리스 노트를 추가합니다.

#### Changed
- [fleet-console] Instrument 시각 체계와 전고형 점진 내비게이션으로 Fleet Console을 개편합니다.
- [fleet-console] 열려 있는 패널용 Formation 단축키와 최소화 패널을 복원하는 단축키를 분리합니다.
- [fleet-console] Operation 패널이 별도의 상단 바를 없앱니다. 평상시에는 상태 표시만 보이다가, 포인터를 올리면 이름·CLI 배지·창 조작이 나타나며 그 영역으로 패널을 옮길 수 있습니다.
- [fleet-console] 사이드바의 Canvas controls 버튼을 Formation view 토글로 바꾸고, 검색과 접기 버튼 위치를 서로 바꿉니다.

#### Fixed
- [fleet-console] General 설정에 테마 섹션을 복원하고 Instrument 기본값과 Maritime, Carbon 테마 선택을 지원합니다.
- [fleet-console] Command Band 배경이 테마별 크롬 팔레트를 따라, Maritime·Carbon에서 중앙·우측이 지나치게 어둡던 문제를 고칩니다.
- [fleet-console] Map Operation 패널의 사용자 선택 색상 외곽선과 Operations SideBar 칩의 사이드 틱을 복원합니다.
- [fleet-console] Activity Rail 크기 조절 드래그가 커서를 지연 추적하지 않고 1:1로 따라옵니다.
- [fleet-console] Map 세션 패널을 닫기 전에 확인을 요구합니다.
- [fleet-console] 활성 Fleet Console 프로세스를 방해하지 않으면서 시작 시 오래된 플러그인 번들을, 종료 시 현재 실행의 번들을 정리합니다.

#### Removed
- [fleet-console] 현지화된 What's New 릴리스 노트에서 English fallback 배지를 제거합니다.

### fleet-desktop

#### Added
- [fleet-console] 완전한 Fleet Console 경험을 네이티브 환경에서 제공하는 선택형 Fleet Console Desktop 0.1.0을 선보입니다.
- [fleet-console] 최초 실행 설치, 시작 시 업데이트, 오프라인 폴백, 중단된 설치의 안전한 복구를 포함해 관리형 Console 런타임을 자동으로 준비하고 유지합니다.
- [fleet-console] 단일 인스턴스 창 복원, 플랫폼 타이틀바 통합, 트레이·메뉴 액션, 업데이트 안내, 명확한 시작 충돌 가이드를 포함한 네이티브 데스크톱 수명주기 컨트롤을 제공합니다.
- [fleet-console] 필요할 때만 Console 코드를 내려받고, Fleet Console 데이터는 제거 가능한 관리형 런타임과 분리해 보존하여 데스크톱 셸을 가볍게 유지합니다.

### fleet-plugin

#### Added
- [fleet-console] 새 Codex 캐리어 세션에 ACP 또는 App Server를 선택하는 Agent CLI 설정을 추가하며, 기본값은 ACP입니다.
- [fleet-console] 기본 제공 및 설치된 고정폭 글꼴 선택, 미리보기, 영속 서버 설정을 갖춘 Terminal Font Browser를 추가합니다.
- [fleet-console] Diff 레일에서 변경 파일과 이력 커밋을 바로 필터링할 수 있습니다.
- [fleet-console] 지원되는 Activity Rail 패널이 선택된 Theater 루트, 워크트리 또는 디렉터리 컨텍스트를 따릅니다.

#### Changed
- [fleet-console] 모든 브랜치와 연결된 워크트리에 걸친 저장소 이력을 전용 오른쪽 레일 패널에서 볼 수 있습니다.

#### Fixed
- [fleet-console] Operation 터미널이 선택한 테마를 따르도록 테마별 터미널 색상 팔레트를 복원합니다.
- [fleet-console] Diff 파일 선택, Git이 아닌 Theater, 좁은 History 레이아웃 문제를 수정합니다.
- [fleet-console] 열지 않은 File Explorer 폴더를 재귀적으로 검색하고, 드래그로 History 상세 패널 크기를 조절합니다.
- [fleet-console] Hook이 제공하는 패널 이름은 실행 중인 패널마다 한 번만 적용되며, 사용자가 바꾼 이름은 유지됩니다.

### fleet-core

#### Added
- [core-infra] [core-unified-agent] [fleet-admiral] Codex ACP 또는 App Server 시작 선택을 저장하고 그에 따라 캐리어 세션을 연결하며, 기본값은 ACP입니다.
- [fleet-carriers] 실행 진행이 계획 파일과 Console Plans 화면에 보이도록, 계획 작성과 웨이브 완료 진행 기록에 기계 판독 가능한 체크박스 작업 계약을 추가합니다.

#### Changed
- [fleet-carriers] [fleet-admiral] 하나의 Kirov 계획에서 안전한 병렬 Ohio 레인을 선언할 수 있습니다. Ohio는 Parallel 레인 하나를 위한 execution_scope를 받고, Sequential 계획의 전체 순차 실행은 유지합니다.

#### Fixed
- [fleet-admiral] [fleet-carriers] 기본 프롬프트와 캐리어 메타데이터는 중립으로 유지하면서, 해군 역할극이 비유 옵션을 따르도록 합니다.

## [1.23.0] - 2026-07-10

### Changed
- [fleet-console] Carrier Stream은 추론 내용을 dock에서 제외하되 접을 수 있는 Details에는 보존하고, 생각 중 상태를 표시합니다.
- [fleet-console] Carrier Stream Details는 Follow 컨트롤로 최신 출력에 고정된 상태를 유지할 수 있습니다.
- [fleet-console] Carrier Stream은 완료된 작업을 Details와 잠시 동안 dock에 계속 표시합니다.
- [fleet-console] Carrier Stream은 오류 트랙을 coral로 표시하고 연결 중인 트랙을 live로 처리합니다.
- [fleet-admiral] Cursor Agent 세션은 alwaysApply rules 파일 대신 sessionStart hook의 additional_context 주입으로 Fleet doctrine을 받습니다.

### Fixed
- [core-unified-agent] Codex 시스템 프롬프트와 config 재정의가 이제 모든 플랫폼의 새 ACP 세션에 적용됩니다. 이전에는 조용히 누락되었습니다.
- [fleet-console] 새 console 릴리스가 감지되면 페이지 새로고침 없이 열린 탭에 Update Available 배지가 표시됩니다.

## [1.22.1] - 2026-07-10

### Added
- [core-unified-agent] Codex ACP를 공식 bridge로 전환하고, 갱신된 reasoning effort 수준과 함께 GPT-5.6 Codex 모델 지원을 추가하며, 마이그레이션을 위해 폐기된 ACP 모델 유형 별칭을 보존합니다.

## [1.22.0] - 2026-07-08

### Changed
- [fleet-console] Codex wiki 문서는 읽기 화면에서 평면적으로 표시됩니다. 문서 본문을 감싸던 카드가 제거되고 문서 헤더는 유지됩니다.
- [fleet-console] console의 기본 탐색을 하단 command status bar, Theater 트리, 오른쪽 레일 route 컨트롤로 교체합니다.

### Removed
- [fleet-console] Codex wiki 항목 하단의 copy-context 작업 버튼(Compact context, Provenance, Context pack, Why this matched)을 제거합니다.

## [1.21.0] - 2026-07-06

### Changed
- [fleet-admiral] [fleet-carriers] 항상 주입되던 Admiral 시스템 프롬프트를 줄입니다. 캐리어별 request-block 계약은 세션의 첫 dispatch 전에 로드되는 온디맨드 carrier-contracts skill로 옮겨집니다.
- [fleet-admiral] Downward Guard 트리거를 Protocol Gate로 통합하고, 규칙을 바꾸지 않은 채 Context Confidence와 Result Integrity Standing Orders를 압축하며, 캐리어 간 피드백 표를 frontline protocol skill로 옮깁니다.
- [fleet-admiral] Protocol Gate는 세션별 skill 로딩이 멱등적임을 선언하므로, 이미 로드된 skill 콘텐츠는 다시 로드하지 않고 적용됩니다.
- [fleet-carriers] 필수 request block 누락으로 거부된 dispatch 요청은 오류에 대상 캐리어의 전체 request-block 계약을 되돌려 주어, 사전 계약 조회 없이 재구성할 수 있습니다.

## [1.20.0] - 2026-07-06

### Added
- [fleet-admiral] Command Integrity Standing Order를 추가합니다. Admiral은 결함 있는 지시에 이의를 제기하고, 모호한 요구를 명확히 하며, 가정된 권한을 거부하고, 안전성·정확성·명확성·효율성 순으로 충돌을 조정합니다.
- [fleet-admiral] Admiral 시스템 프롬프트는 모든 protocol 모드에서 접촉한 모든 디렉터리에 대해 AGENTS.md doctrine을 재귀적으로 로드하도록 지시하며, 가장 깊게 적용되는 파일이 우선합니다.
- [fleet-console] Operations 사이드바에서 그룹 헤더를 드래그하여 Operation 그룹 순서를 바꿀 수 있습니다.

## [1.19.0] - 2026-07-05

### Added
- [fleet-admiral][fleet-cli][fleet-console] Cursor Agent를 rules로 전달되는 Fleet doctrine, MCP, 세션 캡처를 지원하는 일급 Agent CLI 런타임으로 실행할 수 있습니다.
- [fleet-console] Diff 패널은 Changes 아래에 접을 수 있는 History 섹션을 보여 줍니다. 커밋을 고르면 전체 패치가 렌더되고, Flat/Graph 토글로 HEAD에서 도달 가능한 히스토리를 봅니다.
- [fleet-console] 터미널 글리프 렌더링을 위한 번들 Nerd Font 심볼 폴백 지원을 추가합니다.
- [fleet-console] 오른쪽 레일에 Theater와 독립적인 Global Shell 패널을 추가합니다.

### Changed
- [core-process][core-agent][core-unified-agent] Windows 실행 파일 경로 확인과 자식 프로세스 console 창 억제는 이제 공유 내부 core-process 패키지에서 제공합니다.
- [core-unified-agent] Claude와 Codex provider 표시 이름을 Claude Code 및 Codex로 변경합니다.
- [core-infra] Fleet infrastructure 패키지를 도메인 비종속적 역할에 맞게 이름을 바꿉니다. 모든 소비자는 동작 변경 없이 업데이트됩니다.
- [fleet-admiral] [fleet-carriers] data directory 확인이 자체 완결적이므로, 어떤 host가 실행하든 캐리어 저장소와 marketplace 자산은 항상 단일 Fleet home directory로 확인됩니다.
- [fleet-console] [fleet-cli] host 측 data directory 주입을 제거하여, console에서 바꿀 때 지속되지 않던 중복 marketplace 렌더링과 캐리어 설정 문제를 고칩니다.
- [fleet-console] 플러그인이 더 이상 raw data directory 경로를 받지 않도록 plugin host 계약에서 이를 제거합니다.
- [fleet-console] Terminal 실행 메뉴와 SideBar chips가 Claude 및 Codex agent 세션에 서로 다른 공식 스타일 브랜드 아이콘을 표시합니다.
- [fleet-console] Terminal 실행 메뉴와 SideBar chips가 Cursor agent 세션에 공식 스타일 Cursor 브랜드 아이콘을 표시합니다.

### Fixed
- [fleet-console] File Explorer markdown 미리보기에서 README 이미지와 파일 링크를 복원합니다.
- [fleet-console] Terminal Shell 세션이 raw TUI 커서 이동을 보존하므로 nvim 스타일 전체 화면 앱이 안정적으로 다시 그려집니다.
- [fleet-console] Diff 및 File Explorer 레일 패널은 파일이나 diff를 열 때 오른쪽 목록·트리 열을 고정 너비로 두고, 추가 너비는 왼쪽 문서 패널에 줍니다.
- [fleet-console] 좁거나 과도하게 줄어든 레일 패널은 콘텐츠를 자르는 대신 보조 라벨과 배지를 숨깁니다.
- [fleet-console] File Explorer는 파일 미리보기가 열려 있는 동안에만 레일 패널을 확장하고, viewer가 닫히면 단일 열 트리로 돌아갑니다.

### Removed
- [fleet-cli][fleet-console] System Prompt Injection 옵션(`FLEET_REPLACE_SYSTEM_PROMPT`와 Append/Replace 토글)을 제거합니다. Fleet doctrine은 이제 항상 Claude Code 위에 계층화되고(Append), Codex에는 항상 profile developer instructions로 전달됩니다.

## [1.18.0] - 2026-07-04

### Added
- [core-unified-agent] OpenCode Go 모델 선택에 GLM-5.2 및 Kimi K2.7 Code가 포함됩니다.

### Changed
- [fleet-carriers] 캐리어는 최종 출력을 `<report>` 블록으로 감쌉니다. `carrier_jobs(format:"full")`은 해당 블록만 반환하고, 없으면 전체 archive로 폴백하며, `format:"raw"`는 처리되지 않은 archive를 반환합니다.
- [fleet-carriers] 응답 크기를 줄이기 위해 `carrier_jobs` 응답의 중복 echo 필드와 workspace-changes DTO의 파생 필드를 제거합니다.
- [fleet-console] Agent 패널의 캐리어 스트리밍은 라이브 출력, 경과 시간, 토큰 추정이 있는 접이식 하단 dock입니다. Details는 전체 스트림을 엽니다.
- [fleet-admiral][fleet-cli] Codex 실행은 세션 범위 profile 또는 hook trust 우회 대신 hooks가 켜진 고정 Fleet 관리 profile을 사용합니다.
- [core-unified-agent] Cursor Agent 모델 선택에 Kimi K2.7 Code 및 GLM 5.2가 포함되고, 이전 Sonnet 및 Opus 4.7 옵션은 제거됩니다.
- [fleet-console] Diff 패널에서 파일을 고르면 인라인 diff 문서와 함께 두 칸 보기로 확장됩니다.
- [fleet-console] Diff 패널 파일 행은 파일 이름을 앞에 두고, repository picker는 인라인 워크트리 행이 있는 불투명 인패널 덱으로 열립니다.
- [fleet-console] Diff 패널 repository dropdown은 연결된 워크트리를 접을 수 있는 공개 아래 parent repository별로 묶습니다.
- [fleet-admiral] Artifact Inspection Gate는 Admiral이 carrier-diff 이탈을 무해하다고 분류하기 전에 증거를 요구합니다.
- [fleet-console] 같은 Operation 패널의 반복 alert는 횟수를 누적하지 않고 이전 alert를 대체합니다. ALERTS 배지는 활성 alert가 있는 패널 수를 표시합니다.
- [fleet-console] Skills 패널은 하단의 접이식 status dock에 업데이트·설치 진행을 표시합니다. 성공 시 자동으로 사라지고 실패 시 Retry를 유지합니다.
- [fleet-console] Agent 패널 캐리어 stream dock을 단일 행 signal strip으로 압축하고, 트랙별 한 행으로 펼치며, 다중 캐리어 트랙은 captain 색 이름을 표시합니다.

### Fixed
- [fleet-admiral][fleet-cli] Fleet Codex profile 재작성은 변경되지 않은 hooks의 저장된 hook trust 상태를 보존합니다.
- [fleet-console] Diff 패널에서 diff 문서 칸과 변경 파일 목록 사이의 드래그 크기 조절 구분자를 복원합니다.
- [fleet-console] 세션 이름 변경이 유지됩니다. 사용자가 정한 이름은 auto-name으로 덮어쓰이지 않고, 페이지 새로고침 없이 브라우저에 실시간으로 반영됩니다.

### Removed
- [core-unified-agent][fleet-infra][fleet-admiral][fleet-cli][fleet-console] Claude Kimi, Claude GLM, Claude ZAI 별칭 공급자를 목록, 실행, 로그인에서 제거합니다. OpenCode Go는 그대로입니다.
- [fleet-carriers] [fleet-admiral] [fleet-cli] [fleet-console] Native Subagent 모드를 제거합니다. 캐리어는 항상 CLI dispatch로 실행됩니다. carrier_dispatch와 Task Force는 그대로입니다.

## [1.17.1] - 2026-07-03

### Fixed
- [fleet-console] Operations canvas의 터미널 글자가 패닝, 확대/축소, 최대화 후에도 선명합니다.

## [1.17.0] - 2026-07-02

### Added
- [fleet-console] Console 플러그인이 이제 서버에 설정을 저장해 브라우저를 바꾸거나 다시 시작해도 유지됩니다.
- [fleet-console] 터미널 글꼴 이름과 크기가 이제 브라우저와 재시작 후에도 유지되며, 기존 설정은 처음 열 때 옮깁니다.
- [fleet-console] File Explorer가 이제 수동 새로고침, 폴더를 펼 때, 디스크 변경 때 트리를 갱신합니다.
- [fleet-console] Activity Rail에 Skills 플러그인을 추가합니다. skills.sh를 검색하고, 설치 진행을 보며, 업데이트·제거하거나 SKILL.md를 읽을 수 있습니다.

### Changed
- [fleet-console] Diff 패널이 이제 Theater 아래 중첩 Git 저장소도 대상으로 할 수 있습니다. 도구 모음에서 고르면 브랜치와 검색 깊이가 있습니다.
- [fleet-console] Diff 패널이 이제 staged와 unstaged를 한 Changes 목록으로 보여줍니다.

### Fixed
- [fleet-console] Diff 패널의 저장소 선택 목록이 이제 클릭하면 열립니다.
- [fleet-console] Theater 루트가 Git이 아니면 Diff 패널이 더 이상 중첩 저장소를 자동 선택하지 않습니다. 도구 모음에서 고르세요.
- [core-unified-agent] [fleet-carriers] 실패한 캐리어 작업에 이제 가려진 Codex 오류 출력이 포함됩니다. 성공한 작업에는 넣지 않습니다.
- [fleet-console] Operation Controls에서 지원되지 않는 Claude Kimi 및 Claude GLM 실행 별칭을 숨깁니다.
- [fleet-console] Markdown의 YAML frontmatter(예: SKILL.md)가 이제 큰 제목 하나가 아니라 레이블 있는 메타데이터 카드로 렌더링됩니다.
- [fleet-console] Skills SKILL.md 읽기 화면이 이제 스크롤 가능한 본문과 함께 읽을 수 있는 높이로 열립니다.

## [1.16.1] - 2026-07-02

### Fixed
- [fleet-console] Codex Drydock 패치 검토를 복원합니다. 대기 패치 목록에서 Codex 읽기 화면을 열고, 이유와 함께 승인하거나 거절할 수 있습니다.
- [fleet-console] Operation Controls에서 지원되지 않는 Claude Kimi 및 Claude GLM 실행 옵션을 비활성화합니다.
- [fleet-console] 터미널 패널에서 Shift+Enter로 줄을 바꿀 때 Korean IME 조합 글자가 이제 함께 유지됩니다.

## [1.16.0] - 2026-06-30

### Added
- [fleet-console] Operations 지도 옆에 워크스페이스 도구용으로 접고 너비를 조절할 수 있는 오른쪽 Activity Rail을 추가합니다. 활성 도구, 너비, 열림 상태를 기억합니다.
- [fleet-console] 활성 Theater용 File Explorer를 추가합니다. 트리 옆에 코드, markdown, 이미지, 바이너리 미리보기가 있습니다.
- [fleet-console] 활성 Theater의 변경 파일용 Diff 도구를 추가합니다. working-tree와 staged를 전환할 수 있습니다.
- [fleet-console] Fleet Console이 이제 `~/.fleet/plugins`의 서드파티 플러그인을 Operation 패널과 Settings에 불러옵니다.
- [fleet-console] 플러그인은 `apiVersion`을 선언합니다. 맞지 않거나 실패한 플러그인은 Console을 멈추지 않고 건너뜁니다.
- [fleet-console] 외부 플러그인은 Console의 React와 SDK 런타임을 공유하고, 서버 경로는 Console 프로세스에서 실행됩니다.
- [fleet-console] 왼쪽 Operations SideBar에서 Operation 이름을 더블 클릭해 바꿀 수 있습니다.
- [fleet-console] Operations SideBar가 이제 이름 있는 그룹을 지원합니다. 만들기·이름 변경·색 변경·해제, 칩 드래그, 구성원 접기가 되며, 색 레일이 소속을 표시합니다.
- [fleet-console] 실행 중 신호는 유지한 채 Operation 패널 pulse를 켜고 끌 수 있는 사이드바 지도 설정을 추가합니다.
- [fleet-console] 왼쪽 Operations SideBar의 빈 곳을 오른쪽 클릭하면 커서 위치에 New Operation 실행기가 열립니다.

### Changed
- [fleet-console] 패널이나 사이드바의 상태 표시로 Operation accent 색을 정합니다. 전용 accent 버튼은 없어졌습니다.
- [fleet-console] Operation의 accent 색이 이제 최소화 포함 모든 상태에서 canvas 패널과 사이드바 항목 윤곽에 그려집니다.
- [fleet-console] 캐리어 출력이 이제 Agent 패널 상단 요약 배너로 흐릅니다. 별도 자식 패널 대신 클릭해 상세를 봅니다.
- [fleet-console] Codex가 내장 패널로 오른쪽 레일로 이동합니다. /console/codex 전체 경로, 가장자리 핸들, 보기 전환은 제거됩니다.
- [fleet-console] Codex가 더 이상 브라우저 기록을 바꾸거나 URL을 읽지 않습니다. 워크스페이스 선택은 활성 Theater를 따릅니다.
- [fleet-console] Codex가 더 이상 관리자 워크스페이스 등록이나 bearer-token 접근을 제공하지 않습니다. Theater 등록과 재시작 복원만 남은 연결 경로입니다.
- [fleet-console] 독립형 fleet-wiki CLI를 제거합니다. Codex는 Theater 범위의 오른쪽 레일 패널로만 들어갑니다.
- [fleet-console] 오른쪽 레일에 맞게 Codex 패널을 다시 설계합니다. 한 열 탐색기, 분할 읽기, 목차가 있는 넓은 가운데 보기(Expand)입니다.
- [fleet-console] Codex가 이제 search, entry, drydock, conflicts만 제공합니다. 폐기된 엔드포인트는 404를 반환합니다.
- [fleet-console] 하단 Operations 작업 표시줄을 접을 수 있는 왼쪽 Operation 칩 사이드바로 바꿉니다. 클릭해 포커스하고, 드래그 또는 Alt+Shift+Up/Down으로 순서를 바꾸며, "+ New"로 만듭니다.
- [fleet-console] Activity Rail과 File Explorer·Diff 분할이 이제 크기 조절 중 포인터를 바로 따릅니다.
- [fleet-console] 로컬 미게시 빌드(`pnpm`)에서는 radar sweep과 패널 pulse가 이제 기본으로 꺼집니다. 게시 빌드는 켜져 있고, 브라우저별 설정이 항상 이깁니다.
- [fleet-console] 폴더가 Git 저장소가 아니거나 Git이 없으면 Diff 패널이 이제 분명한 영어 메시지를 보여줍니다.
- [fleet-console] File Explorer 패널 문구가 이제 모두 영어입니다.
- [fleet-console] Console 백엔드 경로가 이제 `/api/v1` 아래이며, 설정은 `/api/v1/settings/*`, 업데이트는 `/api/v1/updates/*`입니다.
- [fleet-console] 캐리어 설정이 이제 `PATCH /api/v1/settings/carriers/:id`로 부분 업데이트를 받습니다.
- [fleet-console] Settings General의 Console 테마와 포트가 이제 서버에 저장되어 브라우저 사이에 공유되고 재시작 후에도 유지됩니다.
- [fleet-console] Settings가 이제 Console 컨트롤과 플러그인 컨트롤을 나눕니다. Agent CLI에 시스템 프롬프트, 모델 로그인, CLI 사용 가능 여부가 있습니다.
- [fleet-console] Appearance를 General(Theme와 Console Port)로 합칩니다. Settings 레일에서 Appearance를 제거합니다.
- [fleet-console] Terminal Font와 Terminal Renderer 설정을 Terminal 플러그인의 Agent CLI 섹션으로 옮깁니다. 모든 터미널이 바로 반영됩니다.
- [fleet-console] Fleet Console 자체 업데이트가 이제 터미널 세션이 열려 있어도 바로 적용됩니다.
- [fleet-console] Diff와 File Explorer가 이제 핵심 Console 경로 대신 플러그인 경로(`/plugins/diff/*`, `/plugins/file-explorer/files/*`)를 사용합니다.
- [fleet-console] Diff 패널이 이제 staged와 unstaged를 두 접힘 섹션으로 보여주고, untracked 파일도 포함하며, List / Tree 전환을 추가합니다.
- [fleet-console] Diff 패널의 파일 트리와 hunk 보기가 이제 드래그 칸막이로 나뉘고, 비율은 브라우저별로 유지됩니다.
- [fleet-console] Codex 읽기와 플러그인 markdown 미리보기가 이제 하나의 렌더러를 공유합니다.
- [fleet-console] File Explorer `.md` 미리보기가 이제 Mermaid를 포함해 Codex와 같은 markdown 엔진과 스타일을 사용합니다.
- [fleet-console] File Explorer가 이제 왼쪽 미리보기·오른쪽 트리 분할을 유지하고, Theater를 바꿔도 미리보기와 분할 위치를 기억합니다.
- [fleet-console] File Explorer가 이제 파일 종류별 색 아이콘과 열림/닫힘 폴더 아이콘을 쓰고, 잘 알려진 폴더에는 accent 색을 줍니다. 팔레트는 Maritime 또는 Carbon을 따릅니다.
- [fleet-console] 쓰이지 않던 live Operation 상태를 제거합니다. 캐리어 스트리밍은 이제 running으로 보입니다.
- [fleet-console] awaiting은 이제 aurora(청록), idle은 녹색입니다. idle 패널은 더 이상 테두리를 움직이지 않습니다.
- [fleet-console] 에이전트 턴이 끝나도 캐리어 작업이 스트리밍이면 Operation은 running을 유지하다 idle로 갑니다.
- [fleet-console] Operation이 idle 또는 awaiting이 되면 알림을 냅니다.
- [fleet-console] 터미널 세션 제목에 더 이상 `#N`을 붙이지 않아, 같은 Theater에서 같은 이름을 쓸 수 있습니다.
- [fleet-console] 이전 Console 버전의 Theater와 터미널 세션이 이제 업그레이드 때 초기화되지 않고 옮겨집니다.
- [fleet-console] 부모/자식 Operation 트리를 제거합니다. 모든 Operation이 최상위이며 명령 줄은 없습니다.
- [fleet-console] Console 상태가 이제 단일 `operations` 모음입니다. 기존 `state.json`은 첫 실행 때 초기화되고, 등록된 Theater와 Operation은 잊힙니다.
- [fleet-console] 플러그인 SDK API 버전을 올렸습니다. 이전 SDK로 만든 플러그인은 런타임 실패 대신 호환되지 않는 것으로 거절됩니다.
- [fleet-console] 왼쪽 사이드바의 Operation 그룹이 이제 연속 색 레일과 색이 도는 머리글로 한 묶음으로 읽힙니다.
- [fleet-console] Theater 폴더 선택이 이제 Console에 있습니다. Shell과 Agent의 Terminal 플러그인 세션은 자체적으로 돌아갑니다.
- [fleet-console] 외부 플러그인 API 호환 버전을 1로 다시 맞춥니다. 내장 플러그인도 apiVersion 1을 선언합니다.
- [fleet-console] Console 지속 상태 스키마 버전을 2로 다시 맞춥니다.

### Fixed
- [fleet-console] Agent 패널이 이제 AskUserQuestion을 포함해 입력을 기다리는 모든 프롬프트에서 awaiting이 됩니다. idle 프롬프트는 더 이상 막지 않습니다.
- [fleet-console] ALERTS가 이제 Operations canvas가 열려 있어도, 보고 있는 것만 빼고 모든 Operation의 Awaiting·Complete를 보여줍니다.
- [fleet-console] 최대화일 때 ALERTS 항목이나 빠른 검색 결과를 열면 최대화를 접지 않고 그 패널로 바꿉니다.
- [fleet-console] 최대화일 때 Operation을 만들면 새 패널이 최대화 보기를 이어받습니다.
- [fleet-console] 전역 설치한 Fleet Console이 npm install 후 React 모듈 누락으로 시작하지 못하던 문제를 고칩니다. 게시 패키지가 다시 자급됩니다.
- [fleet-console] Alt+Left/Right가 이제 드래그로 바꾼 순서를 포함해 사이드바 순서로 Operation을 순환합니다.
- [fleet-console] Operations canvas가 이제 활성 테마를 따르므로 Carbon은 중립적인 어두운 canvas입니다. Maritime은 그대로입니다.
- [fleet-console] Radar sweep가 이제 Panel pulse 없이, 켜져 있기만 하면 움직입니다.
- [fleet-console] 왼쪽 사이드바에서 Operation 이름을 더블 클릭하면 이제 이름 바꾸기가 안정적으로 열립니다.

### Removed
- [fleet-cli] Mission Control Wiki Server 패널과 `fleet wiki` 하위 명령을 제거합니다. 독립형 fleet-wiki 바이너리는 폐기되었습니다.
- [fleet-cli] 번들 `fleet-wiki` 패키지는 MCP와 Mission Control 상태 줄이 쓰는 wiki 도구 명세와 항목 수를 위해 유지합니다.

## [1.15.0] - 2026-06-26

### Added
- [fleet-console][fleet-infra] Settings -> General에 고정 Console 포트 핀을 추가합니다. 쓸 수 없으면 넘어가고 안내합니다.

### Changed
- [fleet-console] 진행 중 Operation이 이제 패널과 독 칩에서 동시에 running-light 고리를 돌리지 않습니다. 고리는 보이는 면에만 있고, 칩의 진행 빛은 유지됩니다.

## [1.14.0] - 2026-06-26

### Added
- [fleet-console] 열린 모든 터미널에 바로 적용되는 브라우저별 터미널 글꼴과 크기 컨트롤을 추가합니다.

### Fixed
- [fleet-console] Theater마다 최대화된 Operation 패널 상태를 서로 독립적으로 유지합니다.

## [1.13.0] - 2026-06-25

### Added
- [fleet-console] 브라우저 탭과 북마크에서 Console이 구별되도록 favicon을 추가합니다.
- [fleet-console] 칩을 드래그하거나 Alt+Shift+Arrow로 독 작업 표시줄 순서를 바꿀 수 있으며, 순서는 Theater별로 유지됩니다.
- [fleet-console] 16색 팔레트의 accent 색으로 독 패널을 표시합니다. 시스템 상태는 그대로이고, 선택은 Theater별로 유지됩니다.

### Changed
- [fleet-console] 독 작업 표시줄에서 닫으려면 이제 한 번 더 눌러야 해서 실수로 패널이 사라지지 않습니다.

### Fixed
- [fleet-admiral] Codex CLI 실행이 Codex 0.142.0 이상에서 더 이상 실패하지 않습니다.

## [1.12.0] - 2026-06-23

### Added
- [fleet-console] Theater 폴더 브라우저가 이제 다른 드라이브와 절대 경로로 이동할 수 있어, 홈 드라이브 밖 프로젝트도 등록할 수 있습니다.

### Fixed
- [fleet-console] 최대화일 때 Operation이나 셸을 추가하면 새 패널이 최대화 보기를 이어받습니다.

## [1.11.0] - 2026-06-23

### Changed
- [fleet-console] Operations 독 작업 표시줄이 이제 열린 패널을 모두 보여 주고 포커스된 칩을 강조하며, 칩을 눌러도 최대화 모드를 유지한 채 앞으로 가져옵니다.
- [fleet-console] 칩 페이저가 너무 일찍 나오지 않도록 Operations 독 작업 표시줄을 radar 미니맵까지 넓힙니다.
- [fleet-console] Operations canvas 왼쪽 아래에 새 패널 메뉴를 여는 "+" 실행 버튼을 추가하며, 최대화일 때도 쓸 수 있습니다.
- [fleet-console] Alerts 토글과 패널을 Codex 가장자리 핸들 위, 오른쪽 끝으로 옮깁니다.
- [fleet-console] 패널이 최대화이면 떠 있는 canvas 컨트롤을 숨기고, 최대화 패널의 최대화 버튼을 강조합니다.
- [fleet-console] Operations canvas 패널을 고정 작업 표시줄, 최대화, Shell 동등, 안정적인 순환이 있는 OS 스타일 창으로 바꿉니다.

## [1.10.2] - 2026-06-21

### Changed
- [fleet-console] 이미 켜진 Carriers 또는 Settings 탐색 버튼을 누르면 이제 Operations canvas로 돌아갑니다.
- [fleet-console] What's new가 이제 메인 changelog에서 릴리스 노트를 런타임에 불러옵니다.
- [fleet-console] What's new 버전 선택기가 이제 이전/다음과 함께 페이지당 10개 릴리스를 보여줍니다.

## [1.10.1] - 2026-06-21

### Fixed
- [fleet-console] Settings의 백엔드 API 목록 설명이 이제 모두 영어입니다.

## [1.10.0] - 2026-06-21

### Added
- [fleet-console] Settings가 이제 접을 수 있는 카드에 Console HTTP API를 나열합니다. 새 경로는 자동으로 나타납니다.
- [fleet-console] 접힌 Alerts 독이 이제 새 알림이 오면 펼치지 않고 한 번 깜빡입니다. awaiting은 호박색, completed는 에메랄드입니다.
- [fleet-console] Canvas 모드 Operation이 이제 패널이 보여도 Alerts를 냅니다.
- [fleet-carriers] carrier_dispatch가 이제 선택적 절대 `cwd`를 받아, 위임된 캐리어를 고른 작업 디렉터리에서 시작할 수 있습니다.

### Changed
- [fleet-console] 루트 경로 `/`와 알 수 없는 경로는 이제 `/operations`로 리다이렉트됩니다.
- [fleet-console] Console 버전이 이제 상단 바 "Research Preview" 레이블 아래에 표시됩니다.
- [fleet-console] 안내 가이드가 이제 첫 방문에만 자동으로 나타납니다.
- [fleet-console] Alerts 독이 이제 바깥 클릭, Escape, 다른 Operation으로 이동할 때 접힙니다.
- [fleet-console] Settings가 이제 Agent CLI 아래 Model Sign-in과 기본으로 펼친 Backend API 섹션이 있는 두 칸 레이아웃입니다.
- [fleet-console] Settings의 백엔드 API 목록 설명이 이제 영어입니다.

### Removed
- [fleet-console] Welcome 대시보드를 제거합니다. 이제 Operations가 유일한 진입 화면입니다.
- [fleet-console] 전역 탐색 바에서 Operation과 Codex를 제거합니다. Codex는 오른쪽 가장자리 핸들로만 갑니다.
- [fleet-console] 상단 바에서 Research Preview 표시 점을 제거합니다.
- [fleet-console] Helm(classic) Operations 모드와 Map/Helm 전환을 제거합니다. Operations는 이제 Map canvas만입니다.
- [fleet-console] Operations 왼쪽 사이드바(classic 세션 목록과 Map 떠 있는 세션 목록)를 제거합니다.
- [fleet-console] 전역 탐색 Shell 버튼, 오버레이, 로컬 셸 단축키를 제거합니다. canvas 안 셸 패널은 남습니다.

## [1.9.0] - 2026-06-20

### Added
- [fleet-console] Settings에 Model Sign-in을 추가해 Moonshot Kimi부터 공급자 API 키를 등록, 검증, 삭제합니다. 키는 로컬에 저장되고 다시 보여 주지 않습니다.
- [fleet-console] npm으로 Fleet Console을 전역 설치하면 끝나면 브라우저가 열립니다. 끄려면 `FLEET_CONSOLE_NO_AUTO_OPEN`을 설정하세요.
- [core-unified-agent][fleet-admiral][fleet-infra][fleet-cli] CLI와 Console에서 고를 수 있는 Claude 계열 공급자로 Claude GLM (ZhipuAI GLM)을 추가합니다. `fleet auth` 로그인/로그아웃을 포함합니다.
- [fleet-console] Settings Model Sign-in이 이제 Moonshot Kimi 옆에 ZhipuAI GLM도 나열합니다.
- [fleet-console] Settings가 이제 각 Agent CLI의 설치 여부와 감지된 버전을 보여줍니다.
- [fleet-console] Codex가 아닌 모든 보기에서 오른쪽 가장자리 핸들로 Codex Side 패널을 열 수 있습니다.
- [fleet-console] Codex Side 패널 머리글에서 왼쪽(Nav)과 오른쪽(ToC/Manifest) 칸을 접을 수 있습니다.
- [fleet-console] Side 보기에서 Codex Cmd+K 검색 팔레트는 전체 화면이 아니라 Side 패널로 범위가 제한됩니다.
- [fleet-console] Codex Wiki 읽기 화면이 이제 한 읽기 폭을 쓰고, 찾아보기/색인의 빈 레일을 없애며, 원문을 같은 셸로 접습니다.
- [fleet-console] Codex Wiki 읽기 레일이 이제 활성 섹션이 강조된 목차를 먼저 두고, 좁은 화면에는 목차 서랍과 breadcrumb을 제공합니다.
- [fleet-console] Console 자체 업데이트가 이제 npm 레지스트리 링크 대신 상단 바 버튼에서 전역 `fleet-cli` + `fleet-console` 업데이트를 적용합니다.
- [fleet-console] 자체 업데이트는 터미널 Operation에 활성 세션이 있으면 막고, 로컬/미게시 빌드는 거절한 뒤, 새 loopback 포트에서 재시작하고 새 브라우저 창을 엽니다.
- [fleet-cli] CLI 업데이트가 이제 공유 패키지 업데이터를 쓰며, 종료와 메시지는 이전과 같습니다.
- [core-agent] 패키지 관리자 감지, 설치 위치 확인, 버전 확인, 설치, 수동 폴백용 공유 전역 패키지 업데이터를 추가합니다.
- [fleet-console] Fleet Console이 이제 설치된 버전의 changelog를 What's new 팝업으로 보여 줍니다. 업데이트 후 한 번, 이후 탐색 바에서 다시 열 수 있습니다.
- [fleet-console] Research Preview 배지 옆에 GitHub 저장소 링크와 실시간 star 수를 추가합니다.
- [fleet-console] Operations Map 패널을 이제 하단 독으로 최소화하고 더블 클릭으로 복원할 수 있습니다. 바쁜 항목은 pulse합니다.
- [fleet-console] Operation 이름이 이제 첫 프롬프트의 첫 의미 있는 줄에서 정해집니다. 수동 이름 변경은 덮어쓰지 않고, 이름을 지우면 자동 이름이 다시 켜집니다.

### Changed
- [fleet-console] Fleet Console이 캐리어 작업이 아직 실행 중이면 더 이상 완료 토스트를 띄우지 않습니다.
- [fleet-console] 실행 중 Operation 주의 모션을 열린 패널과 최소화된 독 컨트롤의 일관된 테두리 신호로 바꿉니다.
- [fleet-console] 최소화된 Operation 레이블이 이제 열린 패널 제목 글꼴과 맞습니다.
- [fleet-console] 최소화 패널 독이 이제 가운데 하단 핸들에서 위로 펼쳐지고 넘치면 페이지로 넘겨, 개수가 바뀌어도 기존 항목이 밀리지 않습니다.
- [fleet-console] 바쁜 최소화 패널 항목이 이제 숨 쉬는 pulse 대신 윤곽을 도는 running light를 쓰고, 독 핸들 애니메이션은 접혀 있을 때만 재생됩니다.
- [fleet-console] 최소화된 Operation이 이제 독 칩을 한 번 클릭하면 복원됩니다. 칩별 복원 버튼은 제거했습니다.
- [fleet-console] 완료와 입력 대기 알림이 이제 Theater별로 왼쪽 독 패널에 묶이며, 지금 보이지 않는 Operation만 보여줍니다.
- [fleet-console] 알림이 Operation별 완료와 입력 대기를 구분하고 반복 대기를 세며, 전역 음소거, do-not-disturb, Theater별 음소거를 추가합니다.
- [fleet-console] 묶음 알림은 Operation이 재개되면 지워지고, 세션이나 Theater가 삭제되면 사라집니다.
- [fleet-console] Operation 시작 메뉴가 이제 설치되지 않았거나 로그인되지 않은 Agent CLI를 이유와 함께 비활성화하고, 서버도 해당 세션 생성을 거절합니다.
- [fleet-console] 테마와 터미널 렌더러 컨트롤을 탐색 바에서 Settings 상단 Appearance 섹션으로 옮깁니다.

### Fixed
- [fleet-console] 소스에서 실행한 Fleet Console(`pnpm fleet-console`)이 이제 공유 홈 대신 프로젝트 워크스페이스에 Theater, Operation, 캡처를 저장합니다.
- [fleet-console] Operation 자동 이름은 이제 이름을 지우지 않는 한 첫 제출 프롬프트에서만 바뀝니다.
- [fleet-console] 위임된 작업이 아직 실행 중일 때 carrier-dispatch idle pause가 더 이상 잘못된 "Awaiting orders" 알림을 내지 않습니다.
- [fleet-console] Map 보기의 Operations 사이드바 접힘은 이제 Codex 전체 모드로 갔다 와도 유지됩니다.
- [fleet-console] Operations Map 패널 위치와 크기가 이제 브라우저 새로고침 후에도 유지됩니다.
- [fleet-console] Map 셸 패널이 이제 브라우저 새로고침 후에도 사라지지 않습니다.
- [fleet-console] 터미널을 열거나 복원해도 다른 열린 Operations 터미널 화면이 더 이상 깨지지 않습니다.
- [fleet-console] 지도를 확대한 뒤에도 Operations Map 터미널의 클릭과 드래그 선택이 이제 올바른 칸에 닿습니다.

### Removed
- [fleet-console] Fleet Console이 캐리어가 sortie할 때 더 이상 토스트를 띄우지 않습니다. 활성 Operation 패널에 이미 작업이 보입니다.
- [fleet-admiral] Fleet이 더 이상 사용자 전역(`~/.fleet`)이나 프로젝트 로컬(`.fleet`) skills, agents, hooks를 Agent CLI 세션에 넣지 않습니다. 내장 Fleet 플러그인만 활성화됩니다.
- [fleet-admiral] 폐기된 사용자 전역·프로젝트 플러그인 등록과 남은 marketplace 디렉터리를 실행 때 Codex와 Fleet marketplace에서 정리합니다.
- [fleet-infra] [fleet-cli] [fleet-console] `~/.fleet/agent/auth.json`에서의 자동 이전을 제거합니다. Fleet은 이제 `~/.fleet/auth.json`만 읽습니다. 자격 증명은 `fleet auth login`으로 다시 넣으세요.

## [1.8.0] - 2026-06-18

### Added
- [fleet-console] 처음 쓰는 운영자에게 Theater 등록, Operation 열기, 캐리어 관찰 안내가 열립니다. Welcome에서 다시 열 수 있습니다.
- [fleet-console] 전역 Settings에서 시스템 프롬프트 Append 또는 Replace와 해군 비유 톤을 고를 수 있습니다. Fleet CLI와 같고, 새 세션에 적용됩니다.
- [fleet-console] 백그라운드 Claude Operation이 입력을 기다리면 이동 컨트롤이 있는 상단 중앙 토스트가 닫을 때까지 유지됩니다. 보고 있는 Operation은 빼습니다.
- [fleet-console] Codex (Fleet Wiki)를 이제 현재 보기 옆의 크기 조절 가능한 사이드 패널로 열 수 있습니다. 탐색 바에서 Full과 Side를 전환하며 선택을 기억합니다.

### Changed
- [fleet-console] Operations Map 전체 화면이 더 이상 Esc로 끝나지 않습니다. 최대화/복원 컨트롤을 쓰세요.
- [fleet-console] 전역 탐색 바가 이제 Settings와 캐리어별 설정을 나누고, 기존 캐리어 항목 이름을 "Carriers"로 바꿉니다.

### Removed
- [fleet-console] Operations Map 패널을 화면에 맞추던 포커스 버튼과 제목 표시줄 더블 클릭을 제거합니다.

### Fixed
- [fleet-console] Theater의 Codex (Fleet Wiki) 보기가 이제 Console 재시작 후에도 유지됩니다.

## [1.7.1] - 2026-06-17

Release v1.7.1

## [1.7.0] - 2026-06-17

### Added
- [fleet-console] Operations Map이 이제 휠로 부드럽게 확대/축소되고, 패닝은 즉시입니다.
- [fleet-console] Operations Map이 활성 Theater에서 일반 사용자 셸 터미널 패널을 열 수 있습니다. Operation으로 추적되지 않고 새로고침 후에도 유지되지 않습니다.
- [fleet-console] 커서 위치에서 새 Operation, 셸, 보기 재설정을 하는 canvas 오른쪽 클릭 메뉴를 추가합니다.
- [fleet-console] 포커스 버튼 외에 패널 제목 표시줄의 빈 곳을 더블 클릭해 포커스할 수 있습니다.
- [fleet-console] Operations Map에서 패널 이름을 더블 클릭하면 Operations 목록과 같은 흐름으로 이름을 바꿉니다.
- [fleet-console] 모든 패널과 현재 화면을 보여주는 오른쪽 하단 미니맵을 추가합니다. 드래그로 이동하고 버튼으로 접을 수 있습니다.
- [fleet-console] 활성 Theater에서 이전/다음 Operation에 포커스하는 Alt+Left / Alt+Right를 추가하며, Map과 Helm에서 동작합니다.
- [fleet-console] "?" 버튼으로 접히는 왼쪽 하단 단축키 안내를 추가합니다.
- [fleet-console] 탐색 바를 숨기고 Operations 사이드바를 접는 최대화 컨트롤을 추가합니다. 컨트롤 또는 Esc로 종료하며 브라우저별로 기억됩니다.
- [fleet-console] Operation 표시가 이제 첫 턴 전 회색, 처리 중 호박색, 턴이 끝나면 녹색, 캐리어 작업 중에는 live 색입니다.
- [fleet-console] 백그라운드 Operation이 캐리어 sortie나 대기를 보고하면 Go 컨트롤이 있는 상단 중앙 토스트가 뜹니다. 10초 후 또는 닫으면 사라집니다.

### Changed
- [fleet-console] Theater와 워크스페이스 폴더 선택이 이제 Console 안 디렉터리 브라우저를 쓰므로, 원격·headless 브라우저 세션에서도 동작합니다.
- [fleet-console] Operations radar sweep가 이제 CPU를 덜 쓰고, 브라우저 탭이 숨으면 멈춥니다.
- [fleet-console] Operations Map이 이제 진행 중 캐리어 스트림을 패널 위가 아니라 아래에 보여줍니다.
- [fleet-console] Operations Map 사이드바를 접으면 이제 패널 전체가 숨고 왼쪽 가장자리에 펼치기 컨트롤만 남습니다.

### Fixed
- [fleet-console] 전역 탐색 바가 이제 좁은 너비에서 Theater 선택기와 겹치지 않습니다. 도구 모음은 레이블을 아이콘으로 접고 필요하면 줄바꿈합니다.
- [fleet-console] Theater를 잊는 작업이 이제 디렉터리가 이미 없어도 항상 성공하고 목록에서 제거됩니다.

## [1.6.0] - 2026-06-16

### Added
- [fleet-console] 각 캐리어의 CLI, 모델, SubAgent 모드, Task Force 백엔드, 표시 이름을 편집하고 캐리어별로 저장하는 설정 페이지를 추가합니다.
- [fleet-console] Fleet Console Operations가 이제 활성 Operation 세션을 배치하는 자유형 터미널 canvas로 열립니다.
- [fleet-console] Operations canvas에 켜고 끌 수 있고 브라우저별로 기억되는 radar sweep가 있는 어두운 심해 배경이 있습니다.
- [fleet-console] 자유 배치 canvas(Map)와 고전 단일 터미널 레이아웃(Helm)을 전환하는 Map/Helm 보기 토글을 추가하며 브라우저별로 기억됩니다.
- [fleet-console] Operations Map이 이제 진행 중 캐리어 스트림을 터미널 위에 띄우고, 미리보기를 고르면 전체 스트림이 열립니다.
- [core-unified-agent] Claude 공급자 모델 목록에 Claude Opus 4.7 [1M]과 Opus 4.8 [1M]을 추가합니다.
- [fleet-console] Cmd/Ctrl+K로 모든 Theater의 Operation을 검색합니다. Theater를 바꾸고 Map을 해당 패널로 확대해 터미널에 포커스합니다. /codex에서는 Codex 검색이 단축키를 유지합니다.
- [fleet-console] 전역 탐색 바에 Console, Operations, Codex 단축키를 나열하는 키보드 단축키 버튼이 생깁니다.
- [fleet-console] Operation 이름을 바꾸면 이제 해당 세션의 Agent CLI에 `/rename <name>`을 보냅니다.
- [fleet-console] 재시작 후에도 Fleet Console Theater와 Operation을 유지하고, 열 때 잠든 Agent CLI 세션을 재개합니다.

### Changed
- [fleet-admiral] Agent CLI 세션이 이제 기본적으로 Admiral 시스템 프롬프트를 대체하지 않고 덧붙입니다. fleet-cli에서 여전히 replace 모드로 바꿀 수 있습니다.
- [fleet-console] 소스(pnpm/dev) Console이 이제 OS 임시 디렉터리 대신 프로젝트 `.fleet/console`에 런타임 데이터를 둡니다. `FLEET_CONSOLE_DIR`는 여전히 재정의하며 게시 빌드는 그대로입니다.

## [1.5.5] - 2026-06-15

### Added
- [fleet-console] Operations 사이드바에서 새 터미널 세션을 시작할 때 Claude, Claude Kimi, Codex 중 Agent CLI를 고를 수 있습니다.
- [fleet-console] 터미널 세션이 이제 fleet-cli와 같이 캐리어 작업 완료 알림을 원래 Agent CLI에 전달합니다.
- [fleet-console] Operations가 이제 터미널을 전체 너비로 넓혀 사이드바를 숨기고, 진행 중 캐리어 작업을 한 줄로 겹칩니다. 각 Operation은 확장 상태를 기억합니다.

### Changed
- [fleet-console] 터미널 세션이 이제 fleet-cli 래퍼 대신 공유 Admiral 런타임으로 Agent CLI를 시작합니다.

### Removed
- [fleet-cli][fleet-console][core-agent] `fleet --headless` 플래그와 Console fleet-cli 등록 채널을 제거합니다. 관찰은 이제 Console이 소유한 터미널 세션으로만 합니다.
- [core-agent] 공개 API에서 공유 CLI 등록 계약을 제거합니다.

## [1.5.4] - 2026-06-14

### Fixed
- [fleet-console] 로컬 셸 오버레이가 이제 닫았다가 다시 열어도 실행 중인 셸, 스크롤백, 작업 디렉터리를 유지합니다.

## [1.5.3] - 2026-06-14

### Added
- [fleet-console][core-agent] 더 새 Console 릴리스가 npm에 있으면 전역 탐색 바에 "Update available" 배지를 보여줍니다.

### Changed
- [fleet-console] 로컬 셸 오버레이가 이제 Console 너비의 대부분을 써서 넓은 화면에서 좁아 보이지 않습니다.

## [1.5.2] - 2026-06-14

### Fixed
- [fleet-console] 작업 오버레이가 더 이상 오른쪽에서 잘리지 않습니다. 긴 도구 호출 레이블은 카드 안에서 말줄임됩니다.
- [fleet-console] npm 설치 안정 패키지에서 Windows의 Fleet Console 터미널 세션이 시작하지 못하던 문제를 고칩니다.
- [fleet-cli][fleet-console] `fleet update`가 이제 재설치 전에 실행 중인 Fleet Console을 멈춰, Windows에서 파일이 잠겨 업데이트가 실패하지 않습니다.

## [1.5.1] - 2026-06-14

### Removed
- [fleet-admiral] redline과 frontline protocol 모드는 더 이상 시작 전에 작업 브랜치 격리 검사를 하지 않습니다.

### Fixed
- [fleet-console] Fleet Console이 이제 개발용 자리 표시 대신 실제 설치된 패키지 버전을 보고합니다.
- [fleet-console] Fleet Console이 브라우저 보기 연결이 끊겨도 더 이상 터미널 세션을 끝내지 않습니다. Operation 전환과 console-web를 닫아도 세션이 유지됩니다.

## [1.5.0] - 2026-06-14

### Added
- [fleet-console] Fleet Console이 이제 상단 바에서 고른 프로젝트 루트 Theater별로 Admiral 세션과 Codex wiki 맥락을 묶고, Operations에는 활성 Theater의 Admiral만 보여줍니다.
- [fleet-console] Fleet Console이 자체 런타임으로 제공됩니다. 워크스페이스·작업 레일, 추론 접기와 도구 호출이 있는 캐리어 스트리밍, 작업 요약, 원시 이벤트 타임라인입니다.
- [fleet-console] Fleet Console이 이제 Theater 준비 Bridge에서 열리고, 캐리어 스트리밍은 상단 바에서 가는 전용 Operations 경로에 있습니다.
- [fleet-console] Fleet Console이 이제 Carrier Readiness Matrix를 보여줍니다.
- [fleet-console] Fleet Console CLI에 `start`, `stop`, `restart`, `status`가 생깁니다. `fleet console`은 모든 하위 명령을 독립 바이너리로 전달합니다.
- [fleet-console] Cmd/Ctrl+` 또는 상단 바 동작으로 여는 무료 로컬 셸 오버레이를 추가합니다. 운영자의 login shell을 실행합니다.
- [fleet-console] Fleet Console이 이제 Codex/Fleet Wiki 웹 화면을 소유합니다. `fleet wiki`와 `fleet-wiki` 호환은 Console 패키지를 거칩니다.
- [fleet-cli] Agent CLI를 터미널에서 바로 실행하는 `fleet --native`를 추가합니다. 입력을 자식 CLI에 넘기고, 캐리어 작업이 끝나면 알림을 넣습니다.
- [fleet-cli][fleet-console] 실행 중인 Fleet Console에 세션을 등록해 실시간 관찰하는 `fleet --headless`를 추가합니다.
- [fleet-console] Fleet이 이제 Fleet Console을 로컬 제어 화면으로 실행하고 전역 gateway daemon을 제거하며, MCP를 CLI별 프로세스 안 서버로 되돌립니다.
- [core-agent] Fleet 런타임용 공유 CLI 등록 계약과 프로세스 안 MCP 서버 기본형을 추가합니다.
- [fleet-console] Operations 사이드바의 각 Operation 카드에 세션을 끝내고 레일에서 빼는 닫기 컨트롤이 있습니다.

### Changed
- [core-unified-agent] Moonshot Kimi를 쓰는 Claude Code가 이제 Kimi K2.7을 기본, 슬롯, 서브에이전트 모델로 실행합니다.
- [fleet-console] Fleet Console 서버가 이제 임의 loopback 포트에 붙습니다. `fleet-console start`는 정상 실행 중인 daemon을 브라우저에서 다시 열고 두 번째 서버를 만들지 않습니다.
- [fleet-console] Fleet Console이 이전 carbon-and-lime 모습 대신 해양 시각 정체성을 채택합니다.
- [fleet-console] 관측 이벤트가 이제 캐리어 출력 텍스트를 메모리에 남겨 Console이 실시간 스트림을 그릴 수 있습니다. 노출은 loopback만입니다.
- [fleet-console] `--headless`로 시작한 fleet-cli 세션만 Console에 등록됩니다. 일반 및 `--native` 실행은 더 이상 Console 워크스페이스에 나타나지 않습니다.
- [fleet-console] Admirals 사이드바가 이제 Console 소유 터미널 세션과 캐리어 작업 이력을 나열합니다. 작업을 고르면 해당 터미널 위에 스트리밍 오버레이가 열립니다.
- [fleet-console] Fleet Console이 이제 프로세스가 끝나면 터미널 세션을 종료하고 Admirals 레일에서 빼며, 재연결 때 다시 만들지 않습니다.
- [fleet-console] Admirals 레일이 이제 진행 중 캐리어 작업을 끝난 작업 위에 두고, 완료에는 녹색 표시, 타임스탬프 대신 캐리어 이름과 상태를 붙입니다.
- [fleet-console] 상단 바 연결 칩을, Console 연결이 끊길 때만 빨간 시길과 토스트로 바꿉니다. 둘 다 재연결 때 사라집니다.
- [core-agent] 범용 MCP 레지스트리, 라우팅, 도구 스냅샷 기본형이 `@dotobokuri/core-mcp-server`에서 `@dotobokuri/core-agent`로 옮겨집니다.
- [fleet-admiral] Admiral protocol-mode 스킬 이름을 `protocol-baseline`, `protocol-midline`, `protocol-redline`, `protocol-frontline`으로, gap-audit 스킬을 `assumption-audit`로 바꿉니다.
- [fleet-console] 기본 폴더 선택기가 이제 Windows와 WSL에서 최신 Windows Explorer 스타일 폴더 창을 열고, WSL에서는 Linux 파일시스템에서 시작합니다.
- [fleet-console] 브라우저 payload가 더 이상 원시 작업 디렉터리 경로를 노출하지 않습니다. 관찰 Theater와 워크스페이스 행에는 표시 레이블만 있습니다.
- [fleet-console] Operations 실행 (+) 컨트롤이 이제 Theater 선택기 스타일과 맞습니다.

### Removed
- [fleet-console] 독립형 `fleet-wiki-ui` 런타임 패키지를 제거합니다. Fleet Wiki 탐색은 이제 Fleet Console이 제공합니다.
- [fleet-wiki][fleet-console] Codex/Fleet Wiki 웹 화면에서 한/영 전환을 제거합니다. 인터페이스는 이제 영어 전용입니다.
- [fleet-console] Fleet Console에서 브라우저 토큰 게이트를 제거합니다. 로컬 loopback 접근에 관찰자나 터미널 토큰이 더 이상 필요 없습니다. CLI ingest 인증은 남습니다.
- [fleet-cli] raw-CLI Native 실행 모드를 제거합니다. 전용 CLI는 이제 항상 Fleet persona가 주입된 채로 실행됩니다.
- [fleet-cli] Fleet 전역 단축키(Ctrl+C, Ctrl+Q, Ctrl+T)와 MIRROR/DEDICATED 입력 모드 전환을 제거합니다. 종료는 이제 런처 Exit 또는 자식 CLI 종료입니다.
- [core-agent] `@dotobokuri/core-mcp-server` 패키지를 제거합니다. 범용 MCP API는 `@dotobokuri/core-agent`에서 import하세요.

### Breaking Changes
- [core-agent] `@dotobokuri/core-mcp-server` 패키지는 더 이상 게시되지 않습니다. import를 `@dotobokuri/core-agent`로 옮기세요.

### Fixed
- [fleet-cli] Windows의 Claude Code가 이제 커서를 보이고 Korean/CJK IME 입력을 받습니다. 끄려면 `--disable-cursor-sync` 또는 `FLEET_CURSOR_SYNC=0`을 쓰세요.
- [fleet-console] Fleet Console이 이제 명시적 재시작 전까지 로컬 서버 상태 새로고침 동안 활성 CLI 세션을 보존합니다.
- [fleet-wiki][fleet-console] 에이전트 CLI 세션이 시작될 때 Fleet Wiki 웹이 브라우저에서 자동으로 열리지 않게 합니다.
- [fleet-console] 기본 폴더 선택기가 이제 WSL에서 Windows 폴더 창을 열고 고른 경로를 Linux 경로로 바꿔 동작합니다.

## [1.4.0] - 2026-06-10

### Added
- [fleet-admiral] Protocol-mode 스킬이 이제 checkpoint 경계를 선언하고 두 번 보고 주기를 씁니다.
- [fleet-admiral] protocol sync 검사가 이제 protocol 모드 드리프트, 중복된 Downward Guard 문구, report-token 문법을 막습니다.
- [fleet-admiral] Admiral이 이제 변경을 수행하는 캐리어 작업 결과를 받기 전에 artifact 검사를 요구합니다.
- [fleet-carriers] 캐리어 작업 결과에 이제 검사용 워크스페이스 변경 manifest가 포함됩니다.
- [fleet-wiki] Wiki 항목 작성이 이제 본문 앞의 중복 frontmatter를 벗깁니다. `wiki_drydock`이 이를 감지하고 선택적으로 `fix`할 수 있습니다.

### Changed
- [fleet-admiral] Admiral 프롬프트 정책이 이제 실시간 캐리어 도구 설명을 캐리어 요청 방식의 권위로 봅니다.
- [fleet-carriers] 캐리어 명단이 이제 선택적 prior-jobs 맥락 힌트를 모든 캐리어 아래가 아니라 한 번만 보여줍니다.
- [fleet-admiral] Context Confidence 계획 임계값이 이제 protocol 모드에 따라 조정됩니다. 표준 작업에는 충분한 confidence가 필요합니다.
- [core-agent][core-unified-agent][fleet-infra][fleet-admiral][fleet-carriers][fleet-wiki][fleet-console][fleet-cli] 동작 변경 없이 워크스페이스 패키지 전반의 중복 헬퍼를 합치고 내부 import 순환을 풉니다.
- [fleet-cli][fleet-console][fleet-wiki] 루트 구조 맵, 개발 참고, 한영 README를 현재 워크스페이스 레이아웃과 공개 API에 맞춥니다.
- [core-unified-agent] npm에 실수로 게시되지 않도록 패키지를 private로 표시합니다.

### Fixed
- [fleet-console] 전역 `fleet-wiki` 명령이 이제 모노레포 체크아웃이나 git 워크트리 안에서 저장소 로컬 빌드를 다시 실행합니다.
- [fleet-console] 웹 클라이언트 요청 실패 메시지가 이제 항상 한국어가 아니라 선택한 인터페이스 언어를 따릅니다.
- [fleet-console] wiki daemon 상태 엔드포인트가 이제 실제 패키지 버전을 보고합니다.
- [core-unified-agent] 제어 문자가 있는 시스템 프롬프트가 이제 Codex TOML 프로필에 쓰일 때 완전히 escape됩니다.

### Removed
- [fleet-carriers] 워크스페이스 패키지 전반의 죽은 코드와 쓰이지 않는 공개 API를 제거합니다. CLI, MCP, 캐리어, 저장 설정 계약은 그대로입니다.

## [1.3.1] - 2026-06-07

### Fixed
- [fleet-cli][fleet-console] 전역 설치한 `fleet` 명령이 module-not-found 오류로 실행되지 않던 문제를 고칩니다.

## [1.3.0] - 2026-06-07

### Added
- [fleet-admiral] Fleet 세션에 이제 계획이 진행되기 전 의사결정형 계획 공백을 푸는 Context Confidence 경로가 포함됩니다.
- [core-agent] 전용 Agent CLI 세션이 이제 `.fleet/`의 프로젝트 로컬 Fleet 플러그인을 불러, 내장 플러그인과 함께 hooks, skills, agents, MCP 서버를 켭니다.
- [core-agent] 전용 Agent CLI 세션이 이제 모든 프로젝트에서 `~/.fleet/`의 사용자 전역 Fleet 플러그인을 불러옵니다.
- [core-agent] 프로젝트 로컬·사용자 전역 Fleet 플러그인이 이제 skills, agents, hooks, `.mcp.json`을 심링크로 노출해, 세션 시작을 늦추지 않고 변경이 바로 반영됩니다. 끊긴 링크는 건너뜁니다.
- [fleet-admiral] 전용 Agent CLI 세션이 이제 내장 protocol-mode 스킬을 제공합니다. Admiral 프롬프트는 protocol 게이트로 모드를 고릅니다.
- [fleet-admiral] 각 protocol-mode 스킬이 이제 워크플로 시작 전 해당 모드의 전제를 확인하는 준비 체크리스트로 열립니다.
- [fleet-admiral] 각 protocol-mode 스킬이 이제 계획, 준비 검사, 브리핑, 실행 시작 순으로 보고합니다.

### Changed
- [fleet-cli] 프로젝트 로컬 Fleet 플러그인이 이제 중첩 `marketplace/` 복사 대신 `CWD/.fleet/plugin/` 아래 심링크로 렌더링됩니다.
- [fleet-cli] `fleet update`가 이제 상황별 안내를 출력합니다. 로컬 개발 빌드는 업데이트할 것이 없고, 이미 최신이면 건너뛰며, 그 외는 수동 폴백 전에 별도 메시지입니다.
- [fleet-cli] 범용 에이전트 실행을 core-agent로 뽑고, unified-agent와 MCP 서버 워크스페이스 이름을 core 패키지로 바꿉니다.
- [fleet-wiki] 전용 Agent CLI 세션이 이제 캐리어와 wiki 도구 ID를 유지한 채 fleet이라는 단일 Fleet MCP 서버를 사용합니다.
- [fleet-wiki] 전용 Agent CLI 실행이 이제 생성된 marketplace 디렉터리로 Fleet을 활성화합니다. 캐리어와 wiki MCP 서버는 세션 시작 때 직접 주입된 채로 남습니다.
- [fleet-cli] 전용 Agent CLI 세션의 Fleet 시스템 프롬프트 주입이 이제 세션 플러그인 hook이 아니라 임시 프롬프트 파일과 Codex 프로필로 CLI 실행 때 이뤄집니다.
- [fleet-admiral] Claude 계열 전용 세션이 이제 정적 Admiral 시스템 프롬프트가 아니라 Fleet SessionStart hook으로 실시간 native-subagent 안내를 넣습니다.
- [fleet-carriers] Job Bar 캐리어 이름과 Task Force 백엔드 행이 이제 각 dispatch에 실제로 쓰인 모델과 effort를 보여줍니다.
- [fleet-cli] Fleet Codex 플러그인이 이제 Fleet이 연 Codex 세션에서만 활성화되고 전역 Codex 설정에서는 꺼져, 일반 Codex 세션에는 영향을 주지 않습니다.
- [fleet-cli] 세션별 Codex 프로필이 이제 Fleet 시스템 프롬프트를 여러 줄 TOML 문자열로 저장해 사람이 읽을 수 있습니다.

### Fixed
- [fleet-cli][fleet-console] Windows에서 `fleet update`가 자동 전역 npm 또는 pnpm 업데이트 대신 수동 설치 안내만 출력하던 문제를 고칩니다.

### Removed
- [fleet-admiral] Admiral 시스템 프롬프트가 더 이상 도구별 안내 블록을 인라인하지 않습니다. 도구별 안내는 Fleet MCP 도구 메타데이터에 남습니다.
- [fleet-cli] 전용 Agent CLI 세션에서 Codex role-file 생성과 직접 프롬프트·인라인 에이전트 주입을 제거합니다.
- [fleet-wiki] Fleet 플러그인 번들에서 fleet-usage 스킬을 제거하고 wiki-usage만 제공합니다.

### Fixed
- [fleet-carriers][core-unified-agent] Codex 캐리어 자식 프로세스가 이제 POSIX에서 Fleet CLI가 끝날 때 터미널 닫힘과 치명 오류 종료를 포함해 함께 끝나, 더 이상 남지 않습니다.
- [fleet-cli] Windows의 Claude 계열 전용 세션이 더 이상 SessionStart hook 모듈 로드 오류로 시작에 실패하지 않습니다.
- [fleet-cli] Windows의 Codex 전용 세션이 이제 Fleet 플러그인 활성화를 조용히 건너뛰지 않고 등록하고 켭니다.

## [1.2.0] - 2026-06-03

### Added
- [fleet-cli] Job Bar 백엔드 행이 이제 백엔드별 경과 실행 시간을 보여줍니다.

### Changed
- [fleet-cli] Mission Control이 이제 전역 옵션을 자동 저장하는 평면 LAUNCH/OPTION/SYSTEM 루트를 사용합니다.
- [fleet-cli] Mission Control이 이제 예전 `presets.json`을 무시합니다. 전역 옵션 도입 후 오래된 preset 파일은 직접 지울 수 있습니다.
- [fleet-cli] Mission Control 메뉴와 정보 행이 이제 한 정렬을 공유하며, key:value 행은 그룹 안에서 콜론 기준으로 맞습니다.
- [fleet-cli] Job Bar 토큰 추정이 이제 최종 값으로 점프하지 않고 부드럽게 올라갑니다.
- [fleet-carriers] Job Bar 캐리어 타일이 더 이상 백엔드별 진행 표시를 그리지 않습니다. 활동은 작업 수준 상태 아이콘만입니다.
- [fleet-cli] Job Bar의 활성 작업 표시가 이제 채워진 점과 빈 점을 번갈아 쓰지 않고 채워진 점을 깜빡입니다.

### Breaking Changes
- [fleet-cli] 전역 옵션을 위해 Fleet preset 저장소와 preset 공개 API를 제거했습니다.

## [1.1.3] - 2026-06-03

Release v1.1.3

## [1.1.2] - 2026-06-03

### Added
- [core-unified-agent] Cursor Agent가 이제 Claude Opus 4.8 (thinking) 모델을 지원합니다.
- [fleet-cli] Job Bar 작업 행이 이제 토큰 추정 왼쪽에 경과 시간을 보여 줍니다. 1분 미만은 초만, 그 이상은 분과 초입니다.

### Changed
- [fleet-carriers] Native(SubAgent) 모드 캐리어가 이제 native CLI 경로뿐 아니라 carrier_dispatch도 쓸 수 있으며, Task Force와는 계속 배타적입니다.
- [fleet-carriers] Native(SubAgent) Carrier Roster와 Job Bar 행이 이제 [SA] 배지는 유지한 채 전용 SA 색 대신 각 CLI 시그니처 색을 씁니다.
- [fleet-cli] Job Bar 토큰 추정이 이제 스트리밍 텍스트와 도구 레이블뿐 아니라 도구 결과 출력 크기도 반영합니다.

### Removed
- [fleet-carriers] Job Bar 타일에서 캐리어별 `[N:M]` 배지를 제거합니다. 실시간 활동은 숨 쉬는 아이콘이고 `[TF:N]`과 `[SA]`는 남습니다.

### Fixed
- [fleet-carriers] Carrier Roster와 다른 Mission Control 패널이 이제 터미널이 짧아져도 포커스된 항목을 계속 보여줍니다.

## [1.1.1] - 2026-06-02

### Changed
- [fleet-cli] Mission Control이 이제 도메인 단축키 없이 launcher-root와 action-list 탐색을 사용합니다.
- [fleet-cli] Fleet 그라데이션 배너가 이제 비활성 화면에서 오른쪽에서 왼쪽으로 흐릅니다.
- [fleet-cli] 비활성 Mission Control UI는 내용이 짧을 때 세로 가운데 정렬되고, 활성 Agent CLI 출력은 상단 정렬을 유지합니다.
- [fleet-cli] Mission Control 패널이 이제 accent 표시와 선택 행 강조로 일관된 모습을 공유합니다.
- [fleet-cli] 쓸 수 없는 조건부 동작은 비활성 행으로 보이지 않고 메뉴에서 빠집니다.
- [fleet-infra][fleet-carriers][fleet-cli] preset, auth, carriers 저장소가 이제 `fleet-infra`의 하나의 지속 파일 저장소(원자적 쓰기, 디렉터리 잠금, 파일시스템 가드)를 공유합니다.
- [fleet-cli] Auth 저장소가 이제 preset 저장소와 같이 원자적 쓰기, 디렉터리 잠금, 0600 파일 권한으로 보호됩니다.
- [fleet-cli] Auth가 이제 `createAuthService({ authPath })`로 만들어집니다. 모듈 싱글톤과 `setAuthPath`는 제거되었습니다.
- [fleet-cli] Auth 명령이 이제 호출마다 만들지 않고 공유 `AuthService`를 받습니다.
- [fleet-cli] `fs-store`가 이제 `sensitivity` 필드를 필수로 해, 민감한 파일이 0644로 만들어지지 않게 합니다.
- [fleet-carriers] `carriers.json` 쓰기 모드를 민감하지 않은 상태에 맞춰 명시적으로 0644로 둡니다.

### Fixed
- [fleet-cli] 터미널 크기를 바꾸거나 나눌 때 host TUI가 낡은 글자와 행을 남기던 문제를 고칩니다.

### Removed
- [fleet-infra] `@dotobokuri/fleet-infra/log` 공개 경로, 런타임 로그 저장소, Mission Control 로그 뷰어를 제거합니다.

## [1.1.0] - 2026-06-01

### Added
- [fleet-carriers] job-bar 스트립이 이제 Native(SubAgent) 모드 캐리어에 `[SA]` 배지를 보여줍니다.
- [fleet-cli] Claude 계열 native subagent 세션용 `claude-kimi` 전용 Agent CLI 프로필을 복원합니다.
- [fleet-carriers] Claude 계열 전용 CLI 세션에 캐리어별 Native(SubAgent) 토글을 추가합니다.
- [fleet-carriers] 시작 에이전트 payload에 캐리어별 Claude Native(SubAgent) effort 기본값을 추가합니다.
- [fleet-carriers] carrier_dispatch가 이제 native subagent 모드 캐리어를 거절하고, host AI에게 해당 CLI의 native subagent 경로로 호출하라고 알립니다.
- [fleet-carriers] Codex 전용 CLI host가 이제 Native(SubAgent) 모드를 지원해, 켠 캐리어를 자체 모델과 effort의 native Codex subagent로 실행합니다.

### Changed
- [fleet-carriers] TaskForce 캐리어 job-bar 레이블, 타일, 머리글이 이제 백엔드별 행 색은 유지한 채 TaskForce 파란색으로 렌더링됩니다.
- [fleet-carriers] Native(SubAgent)를 켜는 것과 TaskForce 설정을 커밋하는 것은 이제 서로 배타적이며, 한쪽이 다른 쪽을 덮으면 경고합니다.
- [fleet-carriers] Carrier Status는 이제 Mission Control의 `C` 단축키로 Carrier Roster에 갑니다.
- [fleet-carriers] 기본 캐리어 persona 설정이 이제 각 persona 모듈에 있습니다. 등록 순서는 그대로입니다.
- [fleet-cli] 시작 때 주입되는 Claude 계열 Agent CLI native subagent가 이제 기본적으로 `background: true`입니다.

### Fixed
- [fleet-cli] 기존 Fleet 스크롤 폴백은 유지한 채 Agent CLI app-mouse 드래그 전달을 켭니다.

### Removed
- [fleet-carriers] 캐리어 설정을 여는 `Alt+O` host 단축키를 제거합니다.
- [fleet-carriers] 쓰이지 않는 기본 persona 레지스트리 export와 캐리어 설정 렌더러 hook을 제거합니다.
- [fleet-cli] 위 칸 선택에서 `claude-zai` 전용 Agent CLI 프로필을 제거합니다. auth와 공급자 백엔드는 계속 지원됩니다.
- [fleet-infra] 쓰이지 않는 `@dotobokuri/fleet-infra/settings` 패키지 경로와 남은 settings.json 저장을 제거합니다.

## [1.0.2] - 2026-05-26

### Added
- [core-unified-agent] Cursor Composer 2.5와 Composer 2.5 Fast 모델을 추가합니다.

### Changed
- [fleet-cli][fleet-console] 릴리스 파이프라인을 `main`으로 합칩니다. 버전 범프, CHANGELOG 승격, npm publish, GitHub Release가 한 워크플로입니다.
- [fleet-cli][fleet-console] Mission Control 환영 화면이 이제 게시 빌드를 `stable`, 미게시 작업본을 `local`로 표시합니다.
- [fleet-console] `fleet wiki --help`를 Fleet 브랜드 영어 도움말 스타일과 `fleet wiki` 명령 표기에 맞춥니다.

### Removed
- [fleet-cli][fleet-console] `canary` npm dist-tag와 `canary`에 push할 때마다 돌던 자동 게시를 제거합니다. `canary` 브랜치는 PR 통합 대상으로 남지만 더 이상 게시하지 않습니다.
- [fleet-cli][fleet-console] `canary` 브랜치를 대상으로 하던 수동 릴리스 워크플로를 제거합니다.
- [fleet-cli][fleet-console] Fleet CLI 릴리스 유형, 업데이트 채널, 환영 레이블, prerelease 감지에서 `canary` 런타임 채널을 제거합니다.

## [1.0.1] - 2026-05-25

### Fixed
- [fleet-cli] `@dotobokuri/fleet-cli` 전역 설치가 `ERR_MODULE_NOT_FOUND: @xterm/headless`로 시작에 실패하던 문제를 고칩니다.

### Changed
- [fleet-cli] README에서 `@dotobokuri/fleet-cli`를 전역 전용 CLI로 문서화하고 `npm`, `pnpm`, `yarn` 설치 명령을 넣으며, `package.json`에 `preferGlobal`을 추가합니다.

## [1.0.0] - 2026-05-25

Release v1.0.0

## [0.22.2] - 2026-05-25

### Added
- [core-agent] 종료 후 위 Agent CLI를 시작하거나 다시 여는 Mission Control을 추가합니다.
- [core-agent] 인증, wiki 서버 제어, 진단, about용 native Mission Control Fleet Menu 패널을 추가합니다.
- [fleet-cli] Mission Control에서 옵션을 고치고 저장/초기화할 수 있는 영속 Fleet CLI 시작 preset을 추가합니다.
- [fleet-cli] fleet CLI 종료 전 Ctrl+C 두 번 확인을 추가합니다.
- [fleet-cli][fleet-console] Mission Control이 이제 사용자 채널의 최신 버전을 npm에서 확인하고 환영 화면에 업데이트 가능 안내를 보여줍니다.
- [fleet-cli][fleet-console] 전역 `fleet-cli`와 `fleet-wiki-ui`를 함께 올리는 `fleet update`를 추가합니다. 설치 범위를 확인할 수 없으면 설치 명령을 출력합니다.

### Changed
- [core-agent] Carrier Status가 이제 활성 Agent CLI 입력 통과를 유지한 채 Mission Control 패널로 열립니다.
- [fleet-cli] Mission Control 유휴 화면이 이제 배너, 캐리어/wiki/큐 현황, `local`·`canary`·`stable` 버전 줄이 있는 Fleet 브랜드 환영을 보여줍니다.
- [fleet-cli] CLI 실행/프로필 용어를 Agent CLI로 바꿉니다. `agent-cli` 경로와 `FLEET_AGENT_CLI` 선택자를 포함합니다.
- [fleet-admiral] HUD 레이블이 이제 단일 Fleet Action Protocol에 묶인 컴파일 상수입니다. protocol 전환은 제거되었습니다.
- [core-agent] fleet CLI가 이제 알 수 없는 하위 명령과 옵션을 stderr 오류로 거절하고 status 1로 종료합니다.
- [fleet-admiral] Admiral 프롬프트와 Fleet 도구 정책을 `@dotobokuri/fleet-admiral`로 뽑습니다. fleet CLI가 이제 이를 의존합니다.
- [core-agent] `createExecutorSessionManager(deps)`와 `Executor*` 세션 타입을 추가합니다. 예전 `createDedicatedMcpSession` 헬퍼는 범용 MCP 서버 패키지로 옮겨집니다.
- [fleet-carriers] `fleet-carriers` 내부를 `personas/`, `store/`, `dispatch/`, `stream/`, `jobs/`로 다시 짭니다.
- [fleet-cli] Mission Control 환영 배너를 `fleet --help` ASCII 배너와 맞춰 둘 다 하나의 Fleet 워드마크를 쓰게 합니다.
- [fleet-wiki][fleet-console] Wiki Server 패널이 이제 정상 백그라운드 daemon을 재사용하고, Enter로 브라우저를 열며, `S`로 daemon을 멈추고, 기본 포트를 `fleet wiki`와 맞춥니다.

### Fixed
- [core-agent] 바쁜 세션의 executor 풀 격리, 낡은 풀 클라이언트, MCP 도구 시그니처 어긋남을 고칩니다.
- [fleet-wiki][fleet-console] 이전 daemon이 lock을 쥐고 있을 때 Wiki Server 패널이 조용히 실패하고, 실행 중 daemon을 멈춘 것으로 잘못 보고하며, 종료 중 권한 오류를 삼키던 문제를 고칩니다.

### Removed
- [fleet-carriers] 쓰이지 않는 캐리어 런타임, TUI 기본형, 에이전트 모델 헬퍼 API를 제거합니다.
- [core-agent] 캐리어 세션 지속을 제거합니다. 세션 재사용은 이제 메모리 안 executor 풀 상태만입니다.
- [fleet-cli] 최상위 `-rsp` / `--replace-system-prompt` 플래그를 제거합니다. Mission Control 옵션, `FLEET_REPLACE_SYSTEM_PROMPT`, 또는 저장된 preset으로 바꿉니다.
- [fleet-cli] 최상위 `-n` / `--native` 및 `-em` / `--enable-metaphor` 플래그를 제거합니다. Mission Control 옵션, `FLEET_NATIVE` / `FLEET_ENABLE_METAPHOR`, 또는 저장된 preset으로 바꿉니다.

### Breaking Changes
- [core-agent] `@dotobokuri/fleet-tui/input`과 `@dotobokuri/fleet-tui/pty`를 제거합니다. 대신 `@dotobokuri/fleet-tui/components`와 `@dotobokuri/fleet-tui/layout`를 쓰세요.
- [core-agent] 트리 안 Grand Fleet 정책 모듈을 제거합니다. 이 코드는 이미 fleet CLI 런타임에서 참조되지 않았습니다.

## [0.22.1] - 2026-05-24

Release v0.22.1

## [0.22.0] - 2026-05-24

Release v0.22.0

## [0.22.1] - 2026-05-24

Release v0.22.1

## [0.22.0] - 2026-05-24

### Added
- [fleet-infra] auth, data-dir, job, log, settings 서비스용 host-agnostic 패키지로 `@dotobokuri/fleet-infra`를 추가합니다.
- [fleet-carriers] 캐리어별 내장 외부 MCP 허용 목록을 추가합니다. Tempest가 이제 grep.app 코드 검색 MCP를 노출합니다.
- [core-agent] 이전된 auth 저장소와 Claude 계열 대체 백엔드를 갖춘 auth login, list, logout 명령을 추가합니다.
- [core-agent] 고른 전용 CLI로 모델 이름을 넘기는 `--model`을 추가하고, `--help`를 Fleet Agent와 기반 CLI 옵션 그룹으로 나눕니다.
- [fleet-console] 명령 팔레트를 이제 Cmd+K(또는 Ctrl+K)로 열고 닫을 수 있습니다.
- [fleet-console] 명령 팔레트가 이제 열려 있는 동안 페이지 스크롤을 잠그고 닫을 때 되돌립니다.
- [fleet-console] 키보드 포커스가 이제 명령 팔레트가 열려 있는 동안 그 안에 머물고, 닫을 때 이전으로 돌아갑니다.
- [fleet-console] 검색 결과 위에 올리면 이제 활성 선택이 따라갑니다.
- [fleet-console] 결과 제목의 검색 일치가 이제 시각적으로 강조됩니다.
- [fleet-console] 검색 결과가 이제 표시를 벗긴 본문 일치 발췌를 보여줍니다.
- [fleet-console] 명령 팔레트 결과가 이제 최근과 일치 항목의 구역 머리글 아래로 묶입니다.

### Changed
- [fleet-console] 인라인 mermaid 다이어그램이 이제 작은 개요로 맞춰집니다. lightbox는 여전히 전체 크기로 패닝·줌합니다.
- [fleet-console] 명령 팔레트 검색 결과에서 원 관련도 점수를 제거합니다.
- [fleet-carriers] Fleet 내부 MCP 접근을 격리된 토큰의 독립 `fleet-carriers`와 `fleet-wiki` 서버로 나눕니다.
- [fleet-carriers] 자동 승격된 Task Force 작업의 carrier_jobs 전체 응답이 이제 CLI 유형으로 키를 단 백엔드별 결과를 반환합니다.
- [fleet-carriers] 캐리어 런타임, dispatch, jobs, store, Task Force를 `@dotobokuri/fleet-carriers`로 옮기는 작업을 마칩니다.

### Fixed
- [core-agent] CJK IME 조합을 전용 CLI 입력 커서에 고정하고, 끌 터미널을 위해 `--disable-cursor-sync`를 추가합니다.

### Breaking Changes
- [core-agent][core-unified-agent][fleet-infra][fleet-admiral][fleet-carriers][fleet-wiki][fleet-console][fleet-cli] 독립형 Fleet Admiral과 Fleet Admiralty 워크스페이스 패키지를 제거합니다. 이후 Fleet Agent가 통합 정책 모듈을 소유했습니다.
- [fleet-infra] 폐기된 루트 인프라 재수출을 제거합니다. 인프라 API는 `@dotobokuri/fleet-infra`에서 import하세요.
- [fleet-carriers] carrier_taskforce 도구를 제거합니다. carrier_dispatch가 이제 설정된 Task Force가 있는 캐리어를 다중 백엔드 실행으로 자동 승격합니다.
- [fleet-carriers][core-agent] sortie 토글을 제거합니다. 캐리어 상태 오버레이의 'd' 키를 포함해, 개별 캐리어를 더 이상 오프라인으로 둘 수 없습니다.
- [core-agent] Fleet-world 톤 오버레이가 이제 기본으로 꺼집니다. `--disable-metaphor`는 `--enable-metaphor`로 바뀝니다.
- [core-unified-agent] Gemini CLI 공급자 지원을 제거합니다. 사용자와 API 소비자는 다른 지원 CLI 백엔드로 옮겨야 합니다.

## [0.21.0] - 2026-05-20

### Added
- [core-agent] Claude 전용 CLI를 실행할 때 시스템 프롬프트를 덧붙이지 않고 덮어쓰는 `--replace-system-prompt` (`-rsp`)를 추가합니다.
- [fleet-cli] 전용 CLI MCP 세션에 Fleet Wiki 도구를 추가합니다.
- [fleet-admiral][fleet-cli] 전용 CLI 실행이 이제 Claude와 Codex용 Fleet Admiral 프롬프트, Fleet MCP 접근, native 권한 우회 플래그를 주입합니다.
- [fleet-wiki] 대기 중인 wiki 패치를 승인 게이트 아래 제자리 편집하는 `wiki_patch_edit`를 추가합니다.
- [fleet-cli] 동적 작업 상태 섹션과 활성 작업 전용 프레임 티커를 포함해 Job Bar를 fleet-agent로 흡수합니다.
- [fleet-wiki] Wiki 승인이 이제 내용 해시와 버전 검사로 낡은 기반을 막습니다.
- [fleet-wiki] `rawSourceRefs`를 자동으로 쌓고 중복을 제거해 항목 업데이트 전반의 출처를 보존합니다.
- [fleet-wiki] 경로 탈출과 심링크 별칭 공격을 막기 위해 POSIX 대상 검증과 `realpath` 기반 승인 lock을 적용합니다.
- [fleet-wiki] 일괄 작업을 위해 `wiki_compile_source`의 업데이트 출처와 관련 항목 추적을 개선합니다.
- [fleet-wiki] Wiki 도구 프롬프트, 스키마, 가이드가 이제 영어입니다.

### Fixed
- [fleet-carriers] 같은 캐리어의 동시 dispatch가 더 이상 Job Bar 한 줄로 합쳐지지 않습니다.
- [fleet-wiki] 패치 해시가 이제 전체 `patch.md`를 덮어, `summary` frontmatter 변경이 `changed_fields`, `patch_hash`, `base_patch_hash`에 반영됩니다.
- [fleet-wiki] 같은 패치의 동시 `wiki_patch_edit`, `approve`, `reject`가 더 이상 경합하지 않습니다.
- [fleet-wiki] 낡은 기반 감지가 이제 기록된 패치 해시로 `lastEditHash`를 써서, 교차 편집을 일관되게 잡습니다.
- [fleet-console] 큰 Mermaid 다이어그램이 더 이상 문서 컨테이너 너비에 잘리지 않습니다.
- [fleet-cli] 프롬프트를 한 번도 받지 않은 에이전트 세션은 더 이상 세션 파일을 쓰지 않아, 세션 선택기에 "(no messages)"가 쌓이지 않습니다.
- [fleet-cli] 세션 커밋이 이제 다른 세션의 낡은 업데이트를 무시합니다.
- [core-agent] 에이전트 세션이 이제 치명 도구 호출 오류를 순서대로 처리해, 실패한 호출이 나머지 큐를 멈추지 않습니다.
- [fleet-cli] 바인딩된 세션이 바뀌거나 소켓이 끊긴 뒤 클라이언트가 다시 연결되면 Grand Fleet가 이제 Admiralty에 다시 등록합니다.
- [fleet-cli] status overlay 테스트의 타입 검사를 고칩니다.
- [core-agent] `runtime.shutdown()`이 이제 executor 풀 연결을 끊어 세션이 새지 않습니다.

### Changed
- [core-agent] 캐리어 스트립은 항상 보입니다. Job Bar 상세는 이제 캐리어 작업이 하나 이상 활성일 때만 자동으로 나타납니다.
- [fleet-carriers] 펼친 Job Bar를 캐리어 머리글과 독립 dispatch 하위 줄의 계층으로 다시 설계합니다.
- [fleet-carriers] 같은 캐리어에서 병렬 `carrier_dispatch`를 허용합니다. 동시 요청이 더 이상 "carrier busy"로 거절되지 않습니다.
- [fleet-carriers] `fleet-store`의 `squadronEnabled` 키를 폐기합니다. 이 필드는 이제 런타임에 무시됩니다.
- [fleet-console] Fleet Wiki Web이 이제 워크스페이스 범위 URL로 등록된 여러 워크스페이스를 열 수 있는 사용자별 단일 daemon으로 실행됩니다.
- [fleet-console] `fleet-wiki` CLI가 이제 워크트리를 알아, git 워크트리 안에서는 워크트리 로컬 배포를 실행합니다.
- [fleet-console] 목차를 고정 레일 카드로 옮깁니다. 비면 숨고, 모바일에서는 콘텐츠 위로 올라갑니다.
- [fleet-console] Mermaid 다이어그램을 클릭하면 줌(25-400%), 드래그 패닝, 마우스 휠·키보드 단축키, 열 때 맞춤이 있는 lightbox가 열립니다.
- [fleet-cli] 프롬프트 템플릿이 이제 `/skill:{name}`과 맞춰 `/prompt:{name}`으로 호출됩니다.
- [fleet-admiral][core-agent] Fleet MCP 서버 내부를 1MiB 본문 한도와 5m 타임아웃의 `@dotobokuri/core-mcp-server`로 뽑습니다. `MIGRATION.md`를 보세요.
- [core-agent] 세션과 executor 엔진이 이제 상태 변경 때 origin 토큰을 검증해, 업데이트가 잘못된 세션에 적용되지 않습니다.
- [fleet-cli] Grand Fleet 등록이 이제 합성 ID 대신 진행 중 세션 식별자를 가드합니다.
- [fleet-cli] Grand Fleet 등록이 이제 명시적 status 필드를 추적합니다.

### Removed
- [fleet-cli] 쓰이지 않는 예전 패널 힌트 상수를 제거합니다.
- [fleet-cli] status 소스에서 쓰이지 않는 `visibleRunIdByCli` payload와 status 업데이트의 `_streams` 매개변수를 제거합니다.
- [fleet-carriers] squadron UI를 제거합니다. `[SQ]` 배지, `->SQ` 필터, `S` 토글 특수 처리, Sortie-Squadron 상호 배제입니다.
- [fleet-cli] 전용 CLI 지원에서 Gemini와 Cursor Agent를 제거합니다.
- [fleet-cli] 'metaphor' 도메인(세계관, Operation 이름, 지시 다듬기)과 'request_directive' 도구를 제거합니다.
- [fleet-console] Constellation(backlinks) 패널과 Outgoing references, 백링크 인덱서를 제거합니다.
- [core-agent] 서비스 상태 UI와 새로고침 로직을 제거합니다.

## [0.20.0] - 2026-05-16

### Added
- [fleet-carriers] 기본 캐리어 persona 목록과 자체 등록 패키지로 `@dotobokuri/fleet-carriers`를 추가합니다.
- [fleet-carriers] 도구 중심 등록은 유지한 채 캐리어 메타데이터 기반 executor MCP 도구 범위를 추가합니다.
- [core-unified-agent] Cursor 목록에 1M 컨텍스트 모델을 추가하고, effort와 reasoning 매개변수를 함께 씁니다.

### Changed
- [fleet-carriers] 같은 캐리어에서 병렬 `carrier_dispatch`를 허용합니다. 동시 요청이 더 이상 "carrier busy"로 거절되지 않습니다.
- [fleet-carriers] `fleet-store`의 `squadronEnabled` 키를 폐기합니다. 이 필드는 이제 런타임에 무시됩니다.
- [fleet-carriers] 캐리어의 이전 작업 접근이 이제 명시적 persona `carrier_jobs`와 `<prior_jobs?>` 선언을 요구합니다. `CarrierMetadata.commonRequestBlocks`는 제거되었습니다.
- [fleet-carriers] `PRIOR_JOBS_REQUEST_BLOCK`을 fleet-admiral에서 fleet-carriers로 옮깁니다.
- [fleet-wiki] 다섯 읽기 전용 wiki 도구(`wiki_briefing`, `wiki_orient`, `wiki_query`, `wiki_read`, `wiki_resolve`)가 이제 전역 등록되어, 기본적으로 모든 캐리어가 wiki를 씁니다.
- [fleet-cli] `canary`만 허용되는 PR base로 강제합니다. fork 포함 non-canary PR은 안내와 함께 자동으로 닫힙니다.
- [fleet-cli] `main`에 push할 때마다 `canary`를 `main`에 맞춰 자동 fast-forward합니다.
- [fleet-cli] `fleet-dev` 바이너리를 제거합니다. CWD 라우팅 개발 실행에는 `pnpm dev`를 쓰세요.
- [fleet-wiki] Wiki 도구 렌더링이 이제 캐리어 도구와 같고, TUI에서 투명 배경을 씁니다.

### Fixed
- [fleet-wiki] 캐리어 executor MCP 도구 허용 목록이 더 이상 wiki 모듈 로드 순서에 묶이지 않아, fleet-wiki를 import하지 않아도 fleet-admiral이 throw하지 않습니다.
- [fleet-cli] pr-creates 스킬이 로드되지 않게 하던 누락 frontmatter를 고칩니다.

### Removed
- [fleet-carriers] squadron UI를 제거합니다. `[SQ]` 배지, `->SQ` 필터, `S` 토글 특수 처리, Sortie-Squadron 상호 배제입니다.
- [fleet-cli] 모델 순환 범위 맞춤용 `/scoped-models` 슬래시 명령과 관련 단축키(`Ctrl+S`, `Ctrl+A`, `Ctrl+X`, `Alt+Up/Down`)를 제거합니다.

## [0.19.0] - 2026-05-13

Release v0.19.0

## [0.18.5] - 2026-05-12

### Fixed
- [fleet-console] 쓰이지 않는 `canvas` 의존성을 제거해, 미리 빌드된 바이너리가 없는 플랫폼(예: Windows arm64 + Node 25)에서 `pnpm install`이 실패하지 않게 합니다.

## [0.18.4] - 2026-05-12

### Fixed
- [core-unified-agent] Codex legacy app-server 종료가 이제 graceful, intentional, abnormal로 분류되어, 잘못된 턴 완료 크래시는 숨깁니다.

## [0.18.3] - 2026-05-12

Release v0.18.3

## [0.18.2] - 2026-05-12

### Added
- [core-unified-agent] `CODEX_USE_ACP`로 Codex 이중 전송을 추가해 npx 브리지(`codex-acp`)와 legacy app-server 연결을 모두 켭니다.

### Changed
- [fleet-carriers] 같은 캐리어에서 병렬 `carrier_dispatch`를 허용합니다. 동시 요청이 더 이상 "carrier busy"로 거절되지 않습니다.
- [fleet-carriers] `fleet-store`의 `squadronEnabled` 키를 폐기합니다. 이 필드는 이제 런타임에 무시됩니다.
- [core-unified-agent] ACP npx 브리지의 Windows 수정이 끝날 때까지 기본 Codex 전송을 legacy app-server 경로로 되돌립니다.
