---
op: "create_wiki"
target: "wiki/admiral-agent-domain-tree.md"
summary: "admiral.agent Domain Tree — Public Surface and Internal Engines"
proposer: "claude-pi-fleet-extension"
created: "2026-05-03T02:33:59.277Z"
---
{"id":"admiral-agent-domain-tree","title":"admiral.agent Domain Tree — Public Surface and Internal Engines","body":"# admiral.agent Domain Tree — Public Surface and Internal Engines\n\n`packages/fleet-core/src/admiral/agent/` is the canonical agent domain that consolidates all CLI backend orchestration. It exposes two complementary surfaces (streaming + callback) backed by shared internal infrastructure.\n\n## Public Surfaces (9 modules)\n\n| Module | Purpose | Pattern |\n|--------|---------|---------|\n| `session.ts` | `ensure` / `sendMessage` / `deliverToolResults` / `resolveSession` | Streaming — events flow through `events.ts` module channel |\n| `events.ts` | `register` / `unregister` / `emit` / `clear` for `AgentStreamEvent` | Module-level emit/register, `carrier-job-events.ts` doppelgänger |\n| `tools.ts` | `list` / `invoke` / `registerExtraTools` / `unregisterExtraTools` | Default fleet tool specs auto-registered; host extras scoped by `scopeKey` |\n| `executor.ts` | `executeWithPool` / `executeOneShot` (returns `CarrierExecResult`) | Callback pattern — closed-loop, carrier-tier consumers only |\n| `lifecycle.ts` | `bindHostSession` / `shutdownAllSessions` | Pi `session_start` / `session_shutdown` integration point |\n| `connections.ts` | `disconnect` / `disconnectAll` / `cleanIdle` / `getSessionIdFor` | Carrier-id-keyed pool operations |\n| `models.ts` | `parseId` / `buildId` / `listProviders` / `getProviderIds` / `getThinkingLevels` | CLI/model codec |\n| `service-status.ts` | `read` / `refresh` / `events` | Unified-agent service status delegation |\n| `bridge.ts` | `buildLaunchCommand` (get-only) | Alt+T bridge launch data |\n\n## Internal Engines (under `internal/`)\n\nThese modules are **never re-exported**. They support the public surface above:\n\n| Module | Responsibility |\n|--------|---------------|\n| `state.ts` | `AgentSessionState` / `AgentProviderState` / `ActivePromptState` / launch + bridge maps |\n| `session-runtime.ts` | The single persistence store (`<piSessionId>.json` cache), `ResumeFailureKind` classifier, `migrateLegacyKeys` |\n| `session-engine.ts` | `ensureSession` / `sendMessage` / `deliverToolResults` engine; firstPromptSent dispatch to `buildInitialPrompt` or `buildRuntimeContextPrompt` |\n| `event-normalizer.ts` | unified-agent → `AgentStreamEvent` translation; preserves `emitCliToolLine` text-delta path |\n| `mcp-router.ts` | MCP token routing + FIFO + tool registration; sole owner of `setOnToolCallArrived` |\n| `executor-engine.ts` | Pool + drift detection for the callback path; module-level Maps replace legacy `globalThis` |\n| `post-connect.ts` | Single `applyPostConnectConfig` shared by `session-engine` and `executor-engine` |\n\n## SessionHandle Contract\n\n`SessionHandle = { readonly sessionId: string }` — no other fields. Adding fields is forbidden by Decision 1.\n\n## SendMessageRequest Contract\n\n`SendMessageRequest = { userRequest: string; history?: ConversationHistoryEntry[] }`. The host passes raw user input; fleet-core's `session-engine.ts` decides whether to apply `buildInitialPrompt(history, userRequest)` (firstPromptSent=false) or `buildRuntimeContextPrompt(userRequest)` (follow-up) based on internal session state.\n\n## AgentStreamEvent Discriminated Union\n\nEvery event includes `sessionId` (the ACP session ID — never the MCP token):\n\n```\ntext | thought | toolCall | toolCallUpdate | mcpToolCall | complete | error | exit\n```\n\n`complete` carries `done: \"stop\" | \"toolUse\"` for PI agent-loop round-trip semantics.\n\n## CarrierExecuteOptions / CarrierExecResult\n\nThe callback-path executor uses a closed-loop options bag with five callbacks (`onMessageChunk`, `onThoughtChunk`, `onToolCall`, `onConnected`, `onStatusChange`) and returns a `CarrierExecResult` containing `responseText`, `thoughtText`, `toolCalls`, `status: TrackStatus`, `error?`, `sessionId?`. Status uses the SSoT `TrackStatus` enum.\n\n## Public Consumer Rule\n\nThere is no `@sbluemin/fleet-core/admiral/agent` subpath. Consumers reach this domain only through the `@sbluemin/fleet-core` root barrel re-exports.","tags":["architecture","admiral","agent","fleet-core","domain-tree","api-surface"],"created":"2026-05-03T02:33:59.277Z","updated":"2026-05-03T02:33:59.277Z","version":1,"rawSourceRef":"raw/2026-05-03-admiral-agent-domain-tree-source.md"}