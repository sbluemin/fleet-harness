---
op: "create_wiki"
target: "wiki/fleet-wiki-future-improvements.md"
summary: "Fleet Wiki — Future Improvements (P3 Roadmap)"
proposer: "tool:admiral"
created: "2026-05-05T10:14:17.366Z"
---
{"id":"fleet-wiki-future-improvements","title":"Fleet Wiki — Future Improvements (P3 Roadmap)","tags":["fleet-wiki","roadmap","future-work","draft"],"created":"2026-05-05T10:14:17.366Z","updated":"2026-05-05T10:14:17.366Z","version":1,"body":"## Summary\n\n본 entry 는 fleet-wiki 의 P0+P1+P2 PR 완료 이후 도출된 향후 개선 후보 (P3+) 를 정리한다. 원본 spec 에는 없는 admiral 의 즉흥 제안이며, 본 PR acceptance 와 무관한 별도 후속 작업이다. 운영 한계 / dogfood 검증 / Sentinel 리뷰에서 도출.\n\n## Facts\n\n### 카테고리 A — 대규모 신기능 (P3 후보, 별도 spec 필요)\n\n- **wiki_audit(change_summary, scope_hint)** — 자연어 변경 요약 + 범위 힌트만으로 영향 entry 후보 + 권장 patch_set 자동 stage. \"위키문서 현행화 진행\" 한 마디로 LLM 이 잘 수행하려면 필수. 사용자가 변화 전후 키워드만 알려주면 LLM 의 self-reasoning 으로 영향 그래프를 채움.\n\n- **Decision lineage 그래프** — supersedes / supersededBy 그래프 추적. drydock 가 cycle / orphan / dead branch 자동 감지. 현재는 schema 의 권고만 있고 graph 검증 미흡.\n\n- **Claim sidecar 자동 추출** — body 의 claim 을 자동 분리하여 sidecar (.claims/{id}.json) 에 저장. fact-level update 가능 (entry 전체 rewrite 없이 claim 단위 갱신). wiki_query stage_answer_page 의 claim sidecar 자동 stage 가 자동으로 풀림.\n\n- **Diff-aware ingest** — 새 source 와 기존 entry 의 의미적 diff 를 LLM 이 자동 추출, 사용자에게 visual diff 제시 후 partial accept. 현재는 통째 update_wiki 만 가능.\n\n- **Scheduled drydock + 알림** — 정기 audit 자동화. stale_entry / contradiction_marker 누적 시 Slack/email 알림. 현재는 사용자가 manual 호출.\n\n### 카테고리 B — 소규모 polish (admiral 이 본 PR 에서 유보 결정)\n\n- **#1 extractWikiLinks inline code false positive** — body 의 inline code (` ` `) 안에 링크 syntax 메타 인용 시 link extractor 가 link 로 오인. 메타 entry 작성 시 발생. 정규식 fix 한 wave 로 끝남. 사용자가 schema 참조 표기로 회피 가능해 유보됨. 만약 메타 entry 가 늘어나면 재검토.\n\n- **#2 safety.ts 메타 인용 차단** — prompt-injection 키워드를 학습/문서 목적으로 인용하는 entry 가 자기 safety scanner 에 거부. 우회 표현으로 즉시 해결 가능해 유보. safety_exempt 옵션은 prompt-injection 우회 escape hatch 위험.\n\n- **#5 LLM fallback 패턴 명시 (usage_hints)** — orient 반복 호출 가설. 본 admiral 의 dogfood 에서는 1회만 호출 — 가설적 엣지케이스로 유보. 실 운영 데이터 누적 후 재검토.\n\n- **한국어→영문 query bridging** — wiki_query 가 한국어 query 로 영문 entry id/title 를 매칭 못 함. embedding / cross-lingual alias 가 P3 필요 항목. enhanced ranker 도 substring 기반이라 cover 안 됨.\n\n- **Queue auxiliary file 지원** — wiki_query mode=stage_answer_page 의 claim sidecar 자동 stage 의 prerequisite. queue/{patch-id}/ 에 patch.md + meta.json 외 sidecar 파일도 함께 stage 가능하게 확장. approvePatch 가 sidecar 도 archive 로 함께 mv.\n\n- **Resolve summary fallback 확장** — 현재 wiki_resolve 의 entries[].summary 가 첫 H2 헤더만 잡음 (예: \"## Summary\"). 첫 헤더 뒤의 본문 한 단락까지 가져가야 더 유용. 한 줄 polish.\n\n### 운영 한계 분석 결과\n\n- \"위키문서 현행화\" 한 마디만으로 LLM 이 모든 영향 entry 를 정확히 찾아 일괄 갱신하기는 어려움 — 변화 전후 + 키워드 + 영향 범위를 사용자가 명시하는 것이 신뢰도 높은 운영 패턴.\n- 100+ entries 워크스페이스에서 wiki_orient 토큰 비용 ~2000, 1000+ entries 면 ~12000 (max_tokens 한도). 정기 archive / log 분기별 rename 같은 운영 hygiene 필요.\n- Patch 승인 부담 — 다중 entry 갱신 시 사용자가 N 번 approve. compile_source 의 patch_set + approve_set 으로 일부 완화되었지만 update 패턴에는 미적용.\n\n## Decisions\n\n- 본 entry 는 **draft** 상태로 stage. 실제 P3 spec 으로 진행하려면 원본 draft 처럼 별도 .md 작성 + plan_file 분할 + Kirov plan 작성 + Ohio 실행 흐름을 다시 거쳐야 함.\n- admiral 즉흥 제안은 사용자 우선순위 결정 영역. 본 entry 는 후보 목록일 뿐 commitment 아님.\n- 카테고리 A 의 wiki_audit 가 \"위키 현행화\" 시나리오에 가장 큰 ROI. 다른 4 항목은 정량적 운영 데이터 누적 후 재평가 권장.\n- 카테고리 B 의 #1/#2/#5 는 admiral 의 본 PR 유보 결정 유지. 다음 dogfood 또는 사용자 보고에서 재현되면 재검토.\n\n## Evidence\n\n- 직전 admiral 보고: \"이제 위키 ingest시에 업데이트도 가능한가?\" 답변에서 운영 한계 분석.\n- dogfood 결과: Wave 15 fix 검증 시 query/resolve/briefing 모두 hit. summary placeholder 발견.\n- Sentinel 리뷰 보고서 (P0 + P1+P2 두 차례) 의 HIGH/MEDIUM 항목 중 admiral 유보 결정.\n- 코드 위치: packages/fleet-wiki/src/{links,safety,briefing,patch}.ts 의 현재 구현.\n\n## Related\n\n- [[wiki:fleet-wiki-9-tools]]\n- [[wiki:fleet-wiki-user-operations]]\n- [[wiki:fleet-wiki-ai-usage]]\n- [[wiki:fleet-wiki-drydock-codes]]","rawSourceRef":"raw/2026-05-05-fleet-wiki-future-improvements-source-9fc29ea7.md"}