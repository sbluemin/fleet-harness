---
op: "create_wiki"
target: "wiki/type-migration-matrix-agent-runtime-removal.md"
summary: "Legacy agent-runtime Type Migration Matrix — 11 Type Resolutions"
proposer: "claude-pi-fleet-extension"
created: "2026-05-03T02:36:35.599Z"
---
{"id":"type-migration-matrix-agent-runtime-removal","title":"Legacy agent-runtime Type Migration Matrix — 11 Type Resolutions","body":"# Legacy agent-runtime Type Migration Matrix — 11 Type Resolutions\n\nWhen the legacy `_shared/agent-runtime.ts` module was retired, eleven type/interface declarations had to be resolved. This matrix records each one's classification, target location, and reasoning. The pattern is reusable for future legacy module retirements: scan exports, classify, then plan migration per class.\n\n## Classification Buckets\n\n| Bucket | Definition | Action |\n|--------|-----------|--------|\n| **Complete duplicate** | Identical definition exists in a newer canonical module | Delete legacy definition |\n| **SSoT consolidation** | Multiple types describe the same domain concept; one wins | Pick the SSoT, delete the rest, remove conversion functions |\n| **Host concern** | Type is a render/UI artifact, not a domain type | Move to host (`pi-fleet-extension/src/agent/ui/panel/`) |\n| **Dead concept** | Type exists only because legacy module needed it; no external consumers | Delete |\n| **Genuine new home** | Type still has consumers, must live somewhere | Define afresh in the appropriate new module |\n\n## The Matrix\n\n| # | Legacy Type | Bucket | Target | Action |\n|---|-------------|--------|--------|--------|\n| 1 | `SessionMapStore` | Complete duplicate | `internal/session-runtime.ts` | Delete legacy; new module already had identical interface |\n| 2 | `ResumeFailureKind` | Complete duplicate | `internal/session-runtime.ts` | Delete legacy; new module already had identical type + classifier helpers |\n| 3 | `AgentStatus` (`connecting/running/done/error/aborted`) | SSoT consolidation | `_shared/carrier-job-events.ts:TrackStatus` (`queued/conn/stream/done/err/aborted`) | Map `connecting→conn`, `running→stream`, `error→err`. Delete `toCarrierJobStatus`/`toTrackStatus` conversion functions in carrier tool-specs |\n| 4 | `ColStatus` (`wait/conn/stream/done/err`) | SSoT consolidation | `TrackStatus` (`queued/conn/stream/done/err/aborted`) | Map `wait→queued`. Host already had a local copy; switch to TrackStatus |\n| 5 | `ColBlock` (`thought/text/tool` discriminated union) | Host concern | `pi-fleet-extension/src/agent/ui/panel/types.ts` | Define locally in host panel. Update render/test imports |\n| 6 | `ConnectionInfo` (`{protocol?, sessionId?, model?}`) | Dead concept | — | Inline as callback payload `{sessionId?, model?}` in `CarrierExecuteOptions.onConnected`. Drop `protocol` (ACP implementation detail) |\n| 7 | `CollectedStreamData` | Dead concept | — | Was the executor's panel-data container. With `ExecuteResult.streamData` removed, no consumer remains |\n| 8 | `PooledClient` | Dead concept | `internal/executor-engine.ts` (private) | Keep private; never exported. Implementation detail of the pool |\n| 9 | `ToolCallInfo` (with `rawOutput`/`timestamp`) | Genuine new home | Inline in `CarrierExecResult.toolCalls[]` | `rawOutput`/`timestamp` absorbed into the executor result shape |\n| 10 | `ExecuteOptions` | Genuine new home | `executor.ts:CarrierExecuteOptions` | Preserve fields/callbacks; rename only |\n| 11 | `ExecuteResult` | Genuine new home | `executor.ts:CarrierExecResult` | Keep `responseText`/`thoughtText`/`toolCalls`/`status: TrackStatus`/`error?`/`sessionId?`. Drop `streamData`/`connectionInfo` |\n\n## Removed Functions (collateral)\n\nThe legacy module exported helper functions that died with the types:\n\n| Function | Replacement |\n|----------|-------------|\n| `getClientPool()` | `internal/executor-engine.ts` private module-level Map |\n| `disconnectClient()` | `connections.disconnect(carrierId)` |\n| `disconnectAll()` | `connections.disconnectAll()` |\n| `cleanIdleClients()` | `connections.cleanIdle()` |\n| `getSessionStore()` (legacy) | `internal/session-runtime.ts:getSessionStore()` (single SoT) |\n| `initRuntime()` (legacy) | `internal/session-runtime.ts:initRuntime` exposed as `initAgentSessionRuntime` to `public/runtime.ts` |\n| `onHostSessionChange()` | `lifecycle.bindHostSession(piSessionId)` |\n| `getSessionId(carrierId)` | `connections.getSessionIdFor(carrierId)` |\n| `classifyResumeFailure()` | `internal/session-runtime.ts` (single implementation) |\n| `isDeadSessionError()` | `internal/session-runtime.ts` (single implementation) |\n| `applyPostConnectConfig()` | `internal/post-connect.ts` (single shared module) |\n| `attachCoreStderrLogging`, `hasSystemPromptDrift`, `debugSystemPromptDrift`, `buildConnectOptions` | absorbed into `executor-engine.ts` and `post-connect.ts` |\n\n## Decision Pointers\n\n- Decision 28 forbids a `./admiral/agent` package subpath. The matrix's \"target\" column points to internal modules, but consumers reach them via the root barrel.\n- Decision 34 fixes the public connections/lifecycle surface to exactly six names: `disconnect`, `disconnectAll`, `cleanIdle`, `getSessionIdFor`, `bindHostSession`, `shutdownAllSessions`.\n- The `_shared/` directory after cleanup contains exactly three files: `carrier-job-events.ts` (SSoT events), `cli-tool-types.ts` (third-party `Acp*` aliases), `mcp.ts` (MCP server singleton).\n\n## Reusable Audit Pattern\n\nWhen retiring a legacy module:\n\n1. Enumerate every export (types, functions, constants).\n2. For each export, classify by the bucket table above.\n3. For \"Genuine new home\" items, define them in the appropriate new module **before** deleting the legacy file.\n4. For \"Complete duplicate\" items, ensure the new home is the only callsite before deletion.\n5. For \"Dead concept\" items, prove zero external consumers via grep, then delete.\n6. Wave-split the migration: build new homes first, switch callers second, delete legacy last.\n\nThis pattern applied here yielded: 1500+ lines deleted, 750 lines added with clearer responsibility, zero functional regression, and four AGENTS.md doctrine updates documenting the result.","tags":["architecture","types","migration","admiral","agent","audit","lessons-learned"],"created":"2026-05-03T02:36:35.599Z","updated":"2026-05-03T02:36:35.599Z","version":1,"rawSourceRef":"raw/2026-05-03-type-migration-matrix-agent-runtime-removal-source.md"}