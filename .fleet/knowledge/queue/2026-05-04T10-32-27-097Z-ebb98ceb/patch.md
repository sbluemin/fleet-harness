---
op: "create_wiki"
target: "wiki/build-vs-typecheck-verification-gap.md"
summary: "빌드 vs typecheck 검증 갭 — Genesis sortie 의무 검증 항목"
proposer: "admiral-session-019df205"
created: "2026-05-04T10:32:27.097Z"
---
{"id":"build-vs-typecheck-verification-gap","title":"빌드 vs typecheck 검증 갭 — Genesis sortie 의무 검증 항목","body":"# 빌드 vs typecheck 검증 갭\n\n`pnpm -w build` 통과 ≠ TypeScript type 정합성. esbuild/tsup 같은 fast bundler 는 type 에러를 stripping 단계에서 놓치므로, **type 회귀를 빌드만으로 잡지 못한다**.\n\n## 사고 사례 (Session 019df205)\n\nWave D 전면 인터페이스 통합 sortie (`AgentToolSpec` shape 변경 — `name/label/promptGuidelines` → `id/title/whenToUse[]/...`) 직후:\n\n- Genesis 자가 검증: `pnpm -w build` 통과 + 169+101 tests 통과 + 폐기 심볼 grep 0 건\n- **누락**: `typecheck` 미실행\n\nSentinel 최종 검증에서 HIGH finding:\n- `fleet-harness-extension/src/provider.ts:505` `piToolToAgentSpec()` 가 legacy shape (`name`, `label: undefined`, `promptGuidelines: undefined`, …) 그대로 반환\n- `pnpm --filter @sbluemin/fleet-harness-extension typecheck` 실행 → `error TS2322: Type 'undefined' is not assignable to type 'string'`로 명확히 실패\n- **빌드는 통과**: esbuild 가 type-stripping 후 transform → type 에러 미감지\n\n## 영향\n\n이 회귀는 다음 경로에서 silent 손상을 일으킴:\n- `streamAcp` 가 등록하는 host extra tools 가 `undefined` id/name 으로 저장\n- MCP tools list 에서 host native tools 가 사라지거나 충돌\n\n빌드 산출물은 정상이지만 런타임에서 도구가 사라지는 형태의 회귀. 발견하기 매우 어려움.\n\n## doctrine — Genesis sortie instruction 의무 검증 항목\n\n대규모 인터페이스/시그니처 변경 sortie 의 `<constraints>` 에는 다음 검증을 **모두 명시 의무**:\n\n```\n빌드/테스트 검증 필수:\n- pnpm --filter <package> typecheck   ← 최우선 (type 정합성 보장)\n- pnpm --filter <package> build       ← 빌드 산출물 확인\n- pnpm -w build                       ← 워크스페이스 전체 빌드\n- pnpm --filter <package> test        ← 테스트\n```\n\n**typecheck 우선 이유**:\n- type 회귀는 빌드만으로 안 잡힘\n- esbuild/tsup 의 한계는 알려진 사실\n- type 회귀는 런타임 silent 손상으로 이어지는 경우 다수\n\n## Sentinel 의 역할 강화\n\n이 사고를 계기로 Sentinel 의 최종 검증 sortie 에는 항상 다음 항목 포함 권고:\n- `typecheck` 직접 실행\n- 변경된 인터페이스의 모든 호출처 grep\n- 옵셔널 필드 → required 변경 시 모든 호출자에서 새 필드를 채우는지 확인\n\n## 일반화\n\n이 갭은 다음 도구에 동일하게 존재:\n- esbuild\n- tsup (esbuild 기반)\n- swc-loader\n- Vite (esbuild 기반 prebundling)\n\n**원칙**: 인터페이스 변경 sortie 후에는 무조건 `tsc --noEmit` (= `typecheck`) 직접 실행. 빌드 통과만으로 PASS 판정 금지.","tags":["genesis","verification","esbuild","typecheck","regression","instruction-pattern"],"created":"2026-05-04T10:32:27.097Z","updated":"2026-05-04T10:32:27.097Z","version":1,"rawSourceRef":"raw/2026-05-04-build-vs-typecheck-verification-gap-source.md"}